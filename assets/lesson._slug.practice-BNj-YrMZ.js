import{w as k,v as u,p as r,t as d,a as c}from"./chunk-EPOLDU6W-BGD713rL.js";import{M as p,r as P,a as g}from"./stackoverflow-light-B9T71yH0.js";import{g as T}from"./courseNavigation-D6c0ZrpE.js";import"./courseData-BnFNstkl.js";const x=`# Практическая работа 2.2. Первая версия To-Do списка\r
\r
## Тема\r
\r
Основные типы, константы, переменные, ввод и вывод в Go.\r
\r
## Мини-проект\r
\r
В этой практической работе начинается разработка мини-проекта — консольного приложения **To-Do List**. В следующих главах приложение будет постепенно развиваться: появятся условия, циклы, списки задач, функции, структуры и методы. Пока программа будет устроена проще: она создаст одну задачу, сохранит данные о ней в переменных и выведет результат в консоль.\r
\r
На этом этапе важно не сделать полноценный менеджер задач, а понять, как программа хранит данные и как взаимодействует с пользователем через консоль.\r
\r
После этой практики у вас получится первая версия To-Do приложения: пользователь сможет ввести название задачи, её приоритет и примерное время выполнения, а программа выведет аккуратно оформленную карточку задачи.\r
\r
## Цель работы\r
\r
Цель практической работы — закрепить материал главы 2.2 на примере небольшого мини-проекта.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- использовать базовые типы данных \`string\`, \`int\`, \`float64\` и \`bool\`;\r
- отличать литеральные значения от именованных значений;\r
- объявлять константы с помощью \`const\`;\r
- объявлять переменные через \`var\`;\r
- использовать вывод типа;\r
- применять краткий синтаксис \`:=\`;\r
- использовать нулевые значения переменных;\r
- выводить данные через \`fmt.Println\`, \`fmt.Print\` и \`fmt.Printf\`;\r
- читать данные с клавиатуры через \`fmt.Scan\`;\r
- читать строку с пробелами через \`bufio.Reader\`;\r
- формировать строку через \`fmt.Sprintf\`;\r
- использовать пустой идентификатор \`_\` для временного игнорирования ошибки.\r
\r
## Что должно получиться\r
\r
Программа должна попросить пользователя ввести данные о задаче:\r
\r
\`\`\`text\r
Введите название задачи: Изучить переменные в Go\r
Введите приоритет задачи: 1\r
Введите примерное время выполнения в часах: 1.5\r
\`\`\`\r
\r
После этого программа должна вывести карточку задачи:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
Версия: 1\r
\r
Задача: Изучить переменные в Go\r
Статус: false\r
Приоритет: 1\r
Время выполнения: 1.50 ч.\r
Метка: [1] Изучить переменные в Go — 1.50 ч.\r
\`\`\`\r
\r
Статус задачи пока будет задан в коде. Новая задача считается невыполненной, поэтому для неё используется значение \`false\`.\r
\r
## Подготовка проекта\r
\r
Создайте отдельную папку для практики, например:\r
\r
\`\`\`bash\r
todo-basic\r
\`\`\`\r
\r
Перейдите в неё через терминал и выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-basic\r
\`\`\`\r
\r
После этого создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("=== Go To-Do Manager ===")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если в консоли появился заголовок, значит проект создан правильно.\r
\r
## Задание 1. Добавить константы приложения\r
\r
Сначала опишем данные, которые не должны изменяться во время работы программы.\r
\r
В To-Do приложении такими данными могут быть название приложения и номер версии. Пока пользователь работает с программой, эти значения остаются постоянными, поэтому их логично хранить в константах.\r
\r
Добавьте в функцию \`main\` две константы:\r
\r
\`\`\`go\r
const appName = "Go To-Do Manager"\r
const version = 1\r
\`\`\`\r
\r
Теперь выведите их в консоль:\r
\r
\`\`\`go\r
fmt.Println("===", appName, "===")\r
fmt.Println("Версия:", version)\r
\`\`\`\r
\r
Промежуточный вариант программы:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    const appName = "Go To-Do Manager"\r
    const version = 1\r
\r
    fmt.Println("===", appName, "===")\r
    fmt.Println("Версия:", version)\r
}\r
\`\`\`\r
\r
Здесь используются нетипизированные константы. Go сам понимает, что \`"Go To-Do Manager"\` — это строковое значение, а \`1\` — целое число.\r
\r
В этой ситуации нет необходимости писать тип явно, потому что значение очевидно из литерала.\r
\r
## Задание 2. Создать переменные для задачи\r
\r
Теперь добавим данные одной задачи.\r
\r
Пока пользователь ещё ничего не ввёл, часть переменных можно объявить без начального значения. Тогда Go автоматически присвоит им нулевые значения.\r
\r
Добавьте переменные:\r
\r
\`\`\`go\r
var taskTitle string\r
var taskPriority int\r
var taskHours float64\r
var taskCompleted bool\r
\`\`\`\r
\r
Эти переменные будут хранить:\r
\r
- \`taskTitle\` — название задачи;\r
- \`taskPriority\` — приоритет задачи;\r
- \`taskHours\` — примерное время выполнения;\r
- \`taskCompleted\` — статус выполнения.\r
\r
Переменная \`taskCompleted\` получит нулевое значение \`false\`. Это удобно: новая задача по умолчанию считается невыполненной.\r
\r
Можно вывести начальный статус задачи:\r
\r
\`\`\`go\r
fmt.Println("Статус новой задачи:", taskCompleted)\r
\`\`\`\r
\r
На этом этапе программа показывает важную особенность Go: переменная без начального значения не остаётся пустой или неопределённой. Она получает нулевое значение своего типа.\r
\r
Для \`bool\` это \`false\`, для \`int\` — \`0\`, для \`float64\` — \`0\`, для \`string\` — пустая строка.\r
\r
## Задание 3. Считать приоритет и время выполнения через fmt.Scan\r
\r
Теперь добавим ввод числовых данных.\r
\r
Для чтения данных с клавиатуры можно использовать \`fmt.Scan\`. Эта функция записывает введённое значение в уже существующую переменную. Поэтому перед именем переменной нужно поставить оператор \`&\`.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
fmt.Print("Введите приоритет задачи: ")\r
fmt.Scan(&taskPriority)\r
\r
fmt.Print("Введите примерное время выполнения в часах: ")\r
fmt.Scan(&taskHours)\r
\`\`\`\r
\r
Здесь используется \`fmt.Print\`, а не \`fmt.Println\`, потому что приглашение ко вводу удобно оставить в той же строке, где пользователь будет вводить значение.\r
\r
Например:\r
\r
\`\`\`text\r
Введите приоритет задачи: 1\r
\`\`\`\r
\r
Важно обратить внимание на \`&taskPriority\` и \`&taskHours\`. Функция \`Scan\` должна не просто увидеть значение переменной, а изменить его. Для этого ей передаётся адрес переменной.\r
\r
В этой главе это можно воспринимать как техническое правило: если \`Scan\` должен записать данные в переменную, перед переменной ставится \`&\`.\r
\r
## Задание 4. Считать название задачи с пробелами\r
\r
Название задачи часто состоит из нескольких слов:\r
\r
\`\`\`text\r
Изучить переменные в Go\r
\`\`\`\r
\r
Если использовать \`fmt.Scan\`, в переменную попадёт только первое слово — \`Изучить\`. Это происходит потому, что \`Scan\` считает пробел разделителем.\r
\r
Для чтения полной строки с пробелами удобнее использовать \`bufio.Reader\`.\r
\r
Измените импорт:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strings"\r
)\r
\`\`\`\r
\r
Теперь добавьте чтение названия задачи:\r
\r
\`\`\`go\r
reader := bufio.NewReader(os.Stdin)\r
\r
fmt.Print("Введите название задачи: ")\r
taskTitle, _ = reader.ReadString('\\n')\r
taskTitle = strings.TrimSpace(taskTitle)\r
\`\`\`\r
\r
Здесь происходит несколько действий.\r
\r
Сначала создаётся \`reader\` — объект, который умеет читать данные из стандартного ввода \`os.Stdin\`.\r
\r
Затем метод \`ReadString('\\n')\` читает всю строку до нажатия Enter. В отличие от \`fmt.Scan\`, он не останавливается на первом пробеле.\r
\r
После этого используется \`strings.TrimSpace\`. Он убирает лишние пробелы по краям строки и символ переноса строки, который остаётся после нажатия Enter.\r
\r
Пустой идентификатор \`_\` используется для временного игнорирования ошибки:\r
\r
\`\`\`go\r
taskTitle, _ = reader.ReadString('\\n')\r
\`\`\`\r
\r
В реальных программах ошибки нужно проверять, но в первой учебной практике можно сосредоточиться на основной логике.\r
\r
## Задание 5. Вывести карточку задачи через fmt.Printf\r
\r
Теперь данные задачи можно вывести в консоль.\r
\r
Для простого вывода можно использовать \`fmt.Println\`, но в этой практике лучше применить \`fmt.Printf\`, потому что он позволяет точнее управлять форматом.\r
\r
Добавьте вывод:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("===", appName, "===")\r
fmt.Println("Версия:", version)\r
fmt.Println()\r
\r
fmt.Printf("Задача: %s\\n", taskTitle)\r
fmt.Printf("Статус: %t\\n", taskCompleted)\r
fmt.Printf("Приоритет: %d\\n", taskPriority)\r
fmt.Printf("Время выполнения: %.2f ч.\\n", taskHours)\r
\`\`\`\r
\r
В этом коде используются разные форматные глаголы:\r
\r
- \`%s\` — строка;\r
- \`%t\` — логическое значение;\r
- \`%d\` — целое число;\r
- \`%.2f\` — дробное число с двумя знаками после точки;\r
- \`\\n\` — перенос строки.\r
\r
Для времени выполнения используется \`%.2f\`, чтобы значение выглядело аккуратно. Например, если пользователь введёт \`1.5\`, программа выведет \`1.50\`.\r
\r
Такой вывод выглядит более профессионально, чем обычная печать числа без форматирования.\r
\r
## Задание 6. Сформировать метку задачи через fmt.Sprintf\r
\r
Иногда строку нужно не сразу вывести на экран, а сначала сохранить в переменную. Для этого используется \`fmt.Sprintf\`.\r
\r
Добавьте переменную \`taskLabel\`:\r
\r
\`\`\`go\r
taskLabel := fmt.Sprintf("[%d] %s — %.2f ч.", taskPriority, taskTitle, taskHours)\r
\`\`\`\r
\r
Теперь выведите её:\r
\r
\`\`\`go\r
fmt.Println("Метка:", taskLabel)\r
\`\`\`\r
\r
\`fmt.Sprintf\` работает похоже на \`fmt.Printf\`, но не печатает результат сразу. Он возвращает готовую строку.\r
\r
Это удобно, если строку нужно сохранить, передать дальше или использовать несколько раз.\r
\r
В нашем случае метка задачи объединяет приоритет, название и время выполнения:\r
\r
\`\`\`text\r
[1] Изучить переменные в Go — 1.50 ч.\r
\`\`\`\r
\r
## Задание 7. Использовать краткий синтаксис :=\r
\r
В программе уже появился пример краткого синтаксиса:\r
\r
\`\`\`go\r
reader := bufio.NewReader(os.Stdin)\r
taskLabel := fmt.Sprintf("[%d] %s — %.2f ч.", taskPriority, taskTitle, taskHours)\r
\`\`\`\r
\r
Оператор \`:=\` используется тогда, когда переменная создаётся впервые внутри функции.\r
\r
Например, \`reader\` не был объявлен заранее. Поэтому его можно создать через \`:=\`.\r
\r
То же самое с \`taskLabel\`: эта переменная появляется только после того, как пользователь ввёл данные о задаче.\r
\r
Но для уже существующих переменных \`:=\` использовать не нужно. Например, \`taskTitle\` была объявлена раньше:\r
\r
\`\`\`go\r
var taskTitle string\r
\`\`\`\r
\r
Поэтому при чтении строки используется обычное присваивание:\r
\r
\`\`\`go\r
taskTitle, _ = reader.ReadString('\\n')\r
\`\`\`\r
\r
Если здесь написать \`:=\`, программа будет работать иначе или может вызвать ошибку, потому что переменная уже существует.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strings"\r
)\r
\r
func main() {\r
    const appName = "Go To-Do Manager"\r
    const version = 1\r
\r
    var taskTitle string\r
    var taskPriority int\r
    var taskHours float64\r
    var taskCompleted bool\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    fmt.Print("Введите название задачи: ")\r
    taskTitle, _ = reader.ReadString('\\n')\r
    taskTitle = strings.TrimSpace(taskTitle)\r
\r
    fmt.Print("Введите приоритет задачи: ")\r
    fmt.Scan(&taskPriority)\r
\r
    fmt.Print("Введите примерное время выполнения в часах: ")\r
    fmt.Scan(&taskHours)\r
\r
    taskLabel := fmt.Sprintf("[%d] %s — %.2f ч.", taskPriority, taskTitle, taskHours)\r
\r
    fmt.Println()\r
    fmt.Println("===", appName, "===")\r
    fmt.Println("Версия:", version)\r
    fmt.Println()\r
\r
    fmt.Printf("Задача: %s\\n", taskTitle)\r
    fmt.Printf("Статус: %t\\n", taskCompleted)\r
    fmt.Printf("Приоритет: %d\\n", taskPriority)\r
    fmt.Printf("Время выполнения: %.2f ч.\\n", taskHours)\r
    fmt.Println("Метка:", taskLabel)\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
Введите название задачи: Изучить переменные в Go\r
Введите приоритет задачи: 1\r
Введите примерное время выполнения в часах: 1.5\r
\r
=== Go To-Do Manager ===\r
Версия: 1\r
\r
Задача: Изучить переменные в Go\r
Статус: false\r
Приоритет: 1\r
Время выполнения: 1.50 ч.\r
Метка: [1] Изучить переменные в Go — 1.50 ч.\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В итоговой программе используются почти все основные элементы главы 2.2.\r
\r
Константы \`appName\` и \`version\` хранят данные, которые не изменяются во время выполнения программы. Они задаются один раз и затем используются при выводе заголовка приложения.\r
\r
Переменные \`taskTitle\`, \`taskPriority\`, \`taskHours\` и \`taskCompleted\` описывают состояние задачи. Эти значения могут меняться, поэтому они хранятся именно в переменных.\r
\r
Переменная \`taskCompleted\` специально не получает начального значения. Go автоматически присваивает ей нулевое значение \`false\`. Это хорошо подходит по смыслу: новая задача ещё не выполнена.\r
\r
Название задачи считывается через \`bufio.Reader\`, потому что оно может содержать пробелы. Если бы использовался только \`fmt.Scan\`, название из нескольких слов считалось бы неправильно.\r
\r
Приоритет и время выполнения считываются через \`fmt.Scan\`, потому что это отдельные значения без пробелов.\r
\r
Для вывода используется \`fmt.Printf\`, так как он позволяет управлять форматом. Особенно это заметно на строке с временем выполнения:\r
\r
\`\`\`go\r
fmt.Printf("Время выполнения: %.2f ч.\\n", taskHours)\r
\`\`\`\r
\r
Даже если пользователь введёт \`1.5\`, программа выведет \`1.50\`.\r
\r
Метка задачи создаётся через \`fmt.Sprintf\`, потому что это не просто вывод, а формирование новой строки на основе уже существующих данных.\r
\r
## Самостоятельное задание\r
\r
Измените программу так, чтобы она дополнительно запрашивала имя пользователя.\r
\r
Для этого создайте переменную:\r
\r
\`\`\`go\r
var userName string\r
\`\`\`\r
\r
Считайте имя пользователя через \`fmt.Scan\`:\r
\r
\`\`\`go\r
fmt.Print("Введите ваше имя: ")\r
fmt.Scan(&userName)\r
\`\`\`\r
\r
Затем выведите имя в начале карточки:\r
\r
\`\`\`go\r
fmt.Println("Пользователь:", userName)\r
\`\`\`\r
\r
Пример результата:\r
\r
\`\`\`text\r
Пользователь: Maria\r
Задача: Изучить переменные в Go\r
Статус: false\r
Приоритет: 1\r
Время выполнения: 1.50 ч.\r
\`\`\`\r
\r
Подумайте, почему имя пользователя лучше хранить в переменной, а не в константе.\r
\r
## Вопросы для самопроверки\r
\r
1. Почему название приложения удобно хранить в константе?\r
2. Почему название задачи нужно хранить в переменной?\r
3. Какой тип подходит для названия задачи?\r
4. Какой тип подходит для статуса выполнения?\r
5. Какой тип подходит для приоритета?\r
6. Почему примерное время выполнения лучше хранить в \`float64\`, а не в \`int\`?\r
7. Какое нулевое значение получает переменная типа \`bool\`?\r
8. Почему для чтения значения через \`fmt.Scan\` используется оператор \`&\`?\r
9. Почему \`fmt.Scan\` плохо подходит для чтения строки с пробелами?\r
10. Для чего используется \`strings.TrimSpace\`?\r
11. Чем отличается \`fmt.Println\` от \`fmt.Print\`?\r
12. Чем отличается \`fmt.Printf\` от \`fmt.Sprintf\`?\r
13. Что означает форматный глагол \`%s\`?\r
14. Что означает форматный глагол \`%d\`?\r
15. Что означает форматный глагол \`%.2f\`?\r
16. Почему в строке \`taskTitle, _ = reader.ReadString('\\n')\` используется \`_\`?\r
17. В каких случаях можно использовать краткий синтаксис \`:=\`?\r
18. Почему переменную \`taskCompleted\` можно не инициализировать вручную?\r
`,y=`# Практическая работа 2.3. Указатели: копия и оригинал\r
\r
## Тема\r
\r
Указатели, оператор взятия адреса \`&\`, разыменование через \`*\`, изменение значения через указатель и нулевые указатели \`nil\`.\r
\r
## Описание работы\r
\r
В этой практической работе не будет продолжения To-Do проекта. Указатели — достаточно важная и непростая тема, поэтому лучше отработать её отдельно, на небольших изолированных примерах. Такой подход помогает сначала понять сам механизм: где находится значение, что копируется при присваивании и как указатель позволяет работать не с копией, а с оригиналом.\r
\r
В Go указатель не является «магическим» объектом. Это обычная переменная, которая хранит адрес другой переменной. Главное отличие в том, что через указатель можно обратиться к исходному значению и изменить его.\r
\r
В рамках работы вы напишете несколько коротких программных фрагментов в одном файле \`main.go\`. Каждый фрагмент будет показывать отдельную идею: копирование значения, получение адреса, чтение значения по адресу, изменение значения через указатель и проверку указателя на \`nil\`.\r
\r
## Цель работы\r
\r
Цель практической работы — закрепить понимание того, как Go работает со значениями и указателями.\r
\r
После выполнения работы вы должны уметь:\r
\r
- объяснять, почему при обычном присваивании значение копируется;\r
- получать адрес переменной с помощью оператора \`&\`;\r
- объявлять переменную-указатель;\r
- читать значение по адресу с помощью оператора \`*\`;\r
- изменять исходное значение через указатель;\r
- отличать сам указатель от значения, на которое он указывает;\r
- проверять указатель на \`nil\`;\r
- понимать, почему разыменование \`nil\` приводит к ошибке;\r
- объяснять разницу между копией значения и работой с оригиналом.\r
\r
## Что должно получиться\r
\r
В результате выполнения практической работы в консоли должны появиться несколько блоков вывода. Они покажут, как меняется значение переменной при обычном копировании и при работе через указатель.\r
\r
Пример общего результата:\r
\r
\`\`\`text\r
=== Копирование значения ===\r
First: 101\r
Second: 100\r
\r
=== Адрес переменной ===\r
First: 101\r
Pointer: 0xc0000120a0\r
Value by pointer: 101\r
\r
=== Изменение через указатель ===\r
Score: 15\r
Value by pointer: 15\r
\r
=== Nil pointer ===\r
Pointer is nil\r
Pointer is initialized\r
Value: 200\r
\r
=== Указатель и массив ===\r
Copied name: Charlie\r
Name by pointer: Bob\r
\`\`\`\r
\r
Адрес в памяти у вас будет другим. Это нормально: Go может размещать переменные по разным адресам при разных запусках программы. Важен не конкретный адрес, а сам факт, что указатель хранит адрес.\r
\r
## Подготовка проекта\r
\r
Создайте отдельную папку для практической работы, например:\r
\r
\`\`\`bash\r
pointers-practice\r
\`\`\`\r
\r
Перейдите в неё через терминал и выполните команду:\r
\r
\`\`\`bash\r
go mod init pointers-practice\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Pointers practice")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если в консоли появился текст, значит проект создан правильно.\r
\r
## Задание 1. Показать копирование значения\r
\r
Начнём с ситуации, где указатели не используются.\r
\r
Добавьте в функцию \`main\` следующий код:\r
\r
\`\`\`go\r
fmt.Println("=== Копирование значения ===")\r
\r
first := 100\r
second := first\r
\r
first++\r
\r
fmt.Println("First:", first)\r
fmt.Println("Second:", second)\r
\`\`\`\r
\r
Запустите программу.\r
\r
\`\`\`output\r
=== Копирование значения ===\r
First: 101\r
Second: 100\r
\`\`\`\r
\r
На первый взгляд может показаться, что \`second\` должен измениться вместе с \`first\`, ведь изначально он получил значение из \`first\`. Но в Go при таком присваивании копируется само значение.\r
\r
Когда выполняется строка:\r
\r
\`\`\`go\r
second := first\r
\`\`\`\r
\r
Go берёт текущее значение переменной \`first\`, то есть \`100\`, и записывает его в новую переменную \`second\`. После этого переменные становятся независимыми. У них одинаковые значения в момент присваивания, но это не одна и та же ячейка памяти.\r
\r
Затем строка:\r
\r
\`\`\`go\r
first++\r
\`\`\`\r
\r
увеличивает только \`first\`. Переменная \`second\` остаётся равной \`100\`.\r
\r
Эта часть работы нужна для того, чтобы увидеть исходную проблему: иногда копия значения подходит, а иногда нужно работать именно с оригиналом.\r
\r
## Задание 2. Получить адрес переменной\r
\r
Теперь посмотрим, как получить адрес переменной.\r
\r
Добавьте следующий блок кода ниже первого задания:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Адрес переменной ===")\r
\r
value := 100\r
pointer := &value\r
\r
value++\r
\r
fmt.Println("Value:", value)\r
fmt.Println("Pointer:", pointer)\r
\`\`\`\r
\r
Запустите программу.\r
\r
\`\`\`output\r
=== Адрес переменной ===\r
Value: 101\r
Pointer: 0xc0000120a0\r
\`\`\`\r
\r
Значение после \`Pointer:\` у вас, скорее всего, будет другим.\r
\r
Оператор \`&\` означает «взять адрес». В строке:\r
\r
\`\`\`go\r
pointer := &value\r
\`\`\`\r
\r
переменная \`pointer\` получает не число \`100\`, а адрес той ячейки памяти, где хранится \`value\`.\r
\r
Если обычная переменная похожа на коробку со значением, то указатель похож на записку, где написано, где эта коробка находится.\r
\r
Важно понимать: \`value\` и \`pointer\` имеют разные типы. Переменная \`value\` имеет тип \`int\`, а переменная \`pointer\` имеет тип \`*int\`, то есть «указатель на \`int\`».\r
\r
Чтобы увидеть типы прямо в консоли, добавьте:\r
\r
\`\`\`go\r
fmt.Printf("Type of value: %T\\n", value)\r
fmt.Printf("Type of pointer: %T\\n", pointer)\r
\`\`\`\r
\r
Пример вывода:\r
\r
\`\`\`output\r
Type of value: int\r
Type of pointer: *int\r
\`\`\`\r
\r
Звёздочка в типе \`*int\` показывает, что переменная хранит адрес значения типа \`int\`.\r
\r
## Задание 3. Прочитать значение через указатель\r
\r
Адрес сам по себе редко нужен пользователю программы. Чаще нужно получить значение, которое лежит по этому адресу.\r
\r
Для этого используется оператор \`*\` перед именем указателя.\r
\r
Добавьте в блок из предыдущего задания строку:\r
\r
\`\`\`go\r
fmt.Println("Value by pointer:", *pointer)\r
\`\`\`\r
\r
Полный фрагмент может выглядеть так:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Адрес переменной ===")\r
\r
value := 100\r
pointer := &value\r
\r
value++\r
\r
fmt.Println("Value:", value)\r
fmt.Println("Pointer:", pointer)\r
fmt.Println("Value by pointer:", *pointer)\r
fmt.Printf("Type of value: %T\\n", value)\r
fmt.Printf("Type of pointer: %T\\n", pointer)\r
\`\`\`\r
\r
Пример вывода:\r
\r
\`\`\`output\r
=== Адрес переменной ===\r
Value: 101\r
Pointer: 0xc0000120a0\r
Value by pointer: 101\r
Type of value: int\r
Type of pointer: *int\r
\`\`\`\r
\r
Выражение:\r
\r
\`\`\`go\r
*pointer\r
\`\`\`\r
\r
означает: «перейти по адресу, который хранится в \`pointer\`, и взять значение оттуда».\r
\r
Если \`pointer\` указывает на переменную \`value\`, то \`*pointer\` даёт доступ к текущему значению \`value\`.\r
\r
Здесь важно не перепутать три разные вещи:\r
\r
\`\`\`go\r
value    // обычная переменная типа int\r
pointer  // указатель типа *int\r
*pointer // значение int, полученное по адресу\r
\`\`\`\r
\r
\`pointer\` — это адрес.  \r
\`*pointer\` — это значение, которое лежит по этому адресу.\r
\r
## Задание 4. Изменить значение через указатель\r
\r
Теперь проверим, что через указатель можно не только прочитать значение, но и изменить его.\r
\r
Добавьте новый блок кода:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Изменение через указатель ===")\r
\r
score := 10\r
scorePointer := &score\r
\r
*scorePointer = 15\r
\r
fmt.Println("Score:", score)\r
fmt.Println("Value by pointer:", *scorePointer)\r
\`\`\`\r
\r
Запустите программу.\r
\r
\`\`\`output\r
=== Изменение через указатель ===\r
Score: 15\r
Value by pointer: 15\r
\`\`\`\r
\r
Сначала переменная \`score\` содержит значение \`10\`.\r
\r
Затем создаётся указатель:\r
\r
\`\`\`go\r
scorePointer := &score\r
\`\`\`\r
\r
После этого строка:\r
\r
\`\`\`go\r
*scorePointer = 15\r
\`\`\`\r
\r
означает: «перейти по адресу, который хранится в \`scorePointer\`, и записать туда новое значение \`15\`».\r
\r
Так как \`scorePointer\` указывает именно на \`score\`, меняется сама переменная \`score\`.\r
\r
Это ключевая идея указателей: указатель позволяет работать не с копией значения, а с оригиналом.\r
\r
Можно также увеличить значение через указатель:\r
\r
\`\`\`go\r
*scorePointer++\r
\`\`\`\r
\r
После такой строки значение \`score\` увеличится на единицу, потому что операция выполняется с тем же местом в памяти.\r
\r
## Задание 5. Передать адрес в другую переменную-указатель\r
\r
Указатель — это обычная переменная. Его значение тоже можно скопировать в другую переменную.\r
\r
Добавьте следующий код:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Несколько указателей на одно значение ===")\r
\r
counter := 1\r
firstPointer := &counter\r
secondPointer := firstPointer\r
\r
*firstPointer++\r
*secondPointer++\r
\r
fmt.Println("Counter:", counter)\r
fmt.Println("First pointer value:", *firstPointer)\r
fmt.Println("Second pointer value:", *secondPointer)\r
\`\`\`\r
\r
Пример вывода:\r
\r
\`\`\`output\r
=== Несколько указателей на одно значение ===\r
Counter: 3\r
First pointer value: 3\r
Second pointer value: 3\r
\`\`\`\r
\r
В этом примере \`firstPointer\` и \`secondPointer\` указывают на одну и ту же переменную \`counter\`.\r
\r
Когда выполняется строка:\r
\r
\`\`\`go\r
secondPointer := firstPointer\r
\`\`\`\r
\r
копируется не само число \`1\`, а адрес переменной \`counter\`. Поэтому оба указателя смотрят в одно и то же место.\r
\r
Сначала \`*firstPointer++\` увеличивает \`counter\` с \`1\` до \`2\`. Затем \`*secondPointer++\` увеличивает то же самое значение с \`2\` до \`3\`.\r
\r
Именно поэтому все три вывода показывают \`3\`.\r
\r
## Задание 6. Проверить nil-указатель\r
\r
Указатель можно объявить без начального значения. В этом случае он получит нулевое значение \`nil\`.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Nil pointer ===")\r
\r
var numberPointer *int\r
\r
if numberPointer == nil {\r
    fmt.Println("Pointer is nil")\r
}\r
\r
number := 200\r
numberPointer = &number\r
\r
if numberPointer != nil {\r
    fmt.Println("Pointer is initialized")\r
    fmt.Println("Value:", *numberPointer)\r
}\r
\`\`\`\r
\r
Пример вывода:\r
\r
\`\`\`output\r
=== Nil pointer ===\r
Pointer is nil\r
Pointer is initialized\r
Value: 200\r
\`\`\`\r
\r
Переменная:\r
\r
\`\`\`go\r
var numberPointer *int\r
\`\`\`\r
\r
объявлена как указатель на \`int\`, но адрес ей пока не присвоен. Поэтому её значение равно \`nil\`.\r
\r
\`nil\` означает, что указатель никуда не указывает. Такой указатель можно сравнивать с \`nil\` и выводить на экран, но нельзя разыменовывать.\r
\r
То есть этот код безопасен:\r
\r
\`\`\`go\r
fmt.Println(numberPointer)\r
\`\`\`\r
\r
А этот код приведёт к аварийному завершению программы:\r
\r
\`\`\`go\r
fmt.Println(*numberPointer)\r
\`\`\`\r
\r
Проверка:\r
\r
\`\`\`go\r
if numberPointer != nil {\r
    fmt.Println(*numberPointer)\r
}\r
\`\`\`\r
\r
защищает программу от попытки перейти по адресу, которого нет.\r
\r
## Задание 7. Сравнить копию элемента массива и указатель на элемент массива\r
\r
Теперь посмотрим пример, где разница между копией и указателем становится особенно заметной.\r
\r
Для этого понадобится пакет \`sort\`. Измените импорт в начале файла:\r
\r
\`\`\`go\r
import (\r
    "fmt"\r
    "sort"\r
)\r
\`\`\`\r
\r
Добавьте код:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("=== Указатель и массив ===")\r
\r
names := [3]string{"Alice", "Charlie", "Bob"}\r
\r
copiedName := names[1]\r
namePointer := &names[1]\r
\r
sort.Strings(names[:])\r
\r
fmt.Println("Copied name:", copiedName)\r
fmt.Println("Name by pointer:", *namePointer)\r
fmt.Println("Array:", names)\r
\`\`\`\r
\r
Пример вывода:\r
\r
\`\`\`output\r
=== Указатель и массив ===\r
Copied name: Charlie\r
Name by pointer: Bob\r
Array: [Alice Bob Charlie]\r
\`\`\`\r
\r
В строке:\r
\r
\`\`\`go\r
copiedName := names[1]\r
\`\`\`\r
\r
Go копирует значение \`"Charlie"\` в отдельную переменную. После этого \`copiedName\` живёт отдельно от массива.\r
\r
В строке:\r
\r
\`\`\`go\r
namePointer := &names[1]\r
\`\`\`\r
\r
создаётся указатель не на строку \`"Charlie"\` как абстрактное значение, а на конкретную позицию массива — \`names[1]\`.\r
\r
После сортировки массив меняется:\r
\r
\`\`\`go\r
[Alice Bob Charlie]\r
\`\`\`\r
\r
На позиции \`1\` теперь находится \`"Bob"\`. Поэтому \`copiedName\` остаётся равным \`"Charlie"\`, а \`*namePointer\` показывает \`"Bob"\`.\r
\r
Этот пример хорошо показывает разницу между двумя подходами:\r
\r
\`\`\`go\r
copiedName := names[1]   // копия значения\r
namePointer := &names[1] // адрес позиции в массиве\r
\`\`\`\r
\r
Копия сохраняет старое значение. Указатель продолжает смотреть на исходное место в памяти.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "fmt"\r
    "sort"\r
)\r
\r
func main() {\r
    fmt.Println("=== Копирование значения ===")\r
\r
    first := 100\r
    second := first\r
\r
    first++\r
\r
    fmt.Println("First:", first)\r
    fmt.Println("Second:", second)\r
\r
    fmt.Println()\r
    fmt.Println("=== Адрес переменной ===")\r
\r
    value := 100\r
    pointer := &value\r
\r
    value++\r
\r
    fmt.Println("Value:", value)\r
    fmt.Println("Pointer:", pointer)\r
    fmt.Println("Value by pointer:", *pointer)\r
    fmt.Printf("Type of value: %T\\n", value)\r
    fmt.Printf("Type of pointer: %T\\n", pointer)\r
\r
    fmt.Println()\r
    fmt.Println("=== Изменение через указатель ===")\r
\r
    score := 10\r
    scorePointer := &score\r
\r
    *scorePointer = 15\r
\r
    fmt.Println("Score:", score)\r
    fmt.Println("Value by pointer:", *scorePointer)\r
\r
    fmt.Println()\r
    fmt.Println("=== Несколько указателей на одно значение ===")\r
\r
    counter := 1\r
    firstPointer := &counter\r
    secondPointer := firstPointer\r
\r
    *firstPointer++\r
    *secondPointer++\r
\r
    fmt.Println("Counter:", counter)\r
    fmt.Println("First pointer value:", *firstPointer)\r
    fmt.Println("Second pointer value:", *secondPointer)\r
\r
    fmt.Println()\r
    fmt.Println("=== Nil pointer ===")\r
\r
    var numberPointer *int\r
\r
    if numberPointer == nil {\r
        fmt.Println("Pointer is nil")\r
    }\r
\r
    number := 200\r
    numberPointer = &number\r
\r
    if numberPointer != nil {\r
        fmt.Println("Pointer is initialized")\r
        fmt.Println("Value:", *numberPointer)\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("=== Указатель и массив ===")\r
\r
    names := [3]string{"Alice", "Charlie", "Bob"}\r
\r
    copiedName := names[1]\r
    namePointer := &names[1]\r
\r
    sort.Strings(names[:])\r
\r
    fmt.Println("Copied name:", copiedName)\r
    fmt.Println("Name by pointer:", *namePointer)\r
    fmt.Println("Array:", names)\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
=== Копирование значения ===\r
First: 101\r
Second: 100\r
\r
=== Адрес переменной ===\r
Value: 101\r
Pointer: 0xc0000120a0\r
Value by pointer: 101\r
Type of value: int\r
Type of pointer: *int\r
\r
=== Изменение через указатель ===\r
Score: 15\r
Value by pointer: 15\r
\r
=== Несколько указателей на одно значение ===\r
Counter: 3\r
First pointer value: 3\r
Second pointer value: 3\r
\r
=== Nil pointer ===\r
Pointer is nil\r
Pointer is initialized\r
Value: 200\r
\r
=== Указатель и массив ===\r
Copied name: Charlie\r
Name by pointer: Bob\r
Array: [Alice Bob Charlie]\r
\`\`\`\r
\r
Адрес переменной может отличаться от примера. Это не ошибка.\r
\r
## Самостоятельное задание\r
\r
Измените пример с переменной \`score\`.\r
\r
Создайте переменную:\r
\r
\`\`\`go\r
points := 50\r
\`\`\`\r
\r
Затем создайте указатель на неё:\r
\r
\`\`\`go\r
pointsPointer := &points\r
\`\`\`\r
\r
Через указатель увеличьте значение до \`75\` и выведите оба варианта:\r
\r
\`\`\`go\r
fmt.Println(points)\r
fmt.Println(*pointsPointer)\r
\`\`\`\r
\r
После выполнения задания оба вывода должны показать одно и то же значение.\r
\r
## Дополнительное задание\r
\r
Создайте две переменные:\r
\r
\`\`\`go\r
price := 100\r
discountPrice := price\r
\`\`\`\r
\r
Затем измените \`price\`:\r
\r
\`\`\`go\r
price = 80\r
\`\`\`\r
\r
Выведите обе переменные и объясните, почему \`discountPrice\` не изменилась.\r
\r
После этого создайте указатель:\r
\r
\`\`\`go\r
pricePointer := &price\r
\`\`\`\r
\r
Измените значение через указатель:\r
\r
\`\`\`go\r
*pricePointer = 60\r
\`\`\`\r
\r
Снова выведите \`price\` и \`*pricePointer\`.\r
\r
Это задание помогает ещё раз сравнить работу с копией и работу через указатель.\r
\r
## Вопросы для самопроверки\r
\r
1. Что происходит при присваивании \`second := first\`?\r
2. Почему после изменения \`first\` переменная \`second\` не меняется?\r
3. Что делает оператор \`&\`?\r
4. Что означает тип \`*int\`?\r
5. Чем отличается \`pointer\` от \`*pointer\`?\r
6. Что делает оператор \`*\` перед именем указателя?\r
7. Почему через \`*pointer\` можно изменить исходную переменную?\r
8. Что хранится в переменной-указателе: значение или адрес?\r
9. Почему два указателя могут изменять одно и то же значение?\r
10. Что означает значение \`nil\` для указателя?\r
11. Почему нельзя разыменовывать \`nil\`?\r
12. Как проверить указатель перед разыменованием?\r
13. Почему \`copiedName\` не изменился после сортировки массива?\r
14. Почему \`*namePointer\` изменился после сортировки массива?\r
15. В чём основная разница между копией значения и указателем на значение?\r
`,b=`# Практическая работа 2.4. To-Do список: операции, проверки и преобразования\r
\r
## Тема\r
\r
Операции, сравнения, логические выражения, явное преобразование типов и парсинг строк в Go.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В версии из практики 2.2 программа уже умела получать название задачи, приоритет и примерное время выполнения, а затем выводила карточку задачи в консоль.\r
\r
Теперь программа станет немного умнее. Она не просто сохранит введённые данные, но и выполнит над ними несколько действий: преобразует введённые строки в числа и логическое значение, рассчитает примерное время выполнения в часах и минутах, вычислит остаток рабочего дня, проверит приоритетность задачи и сформирует текстовую метку.\r
\r
Эта практика всё ещё не превращает программу в полноценный менеджер задач. В ней пока нет меню, циклов, списка задач и выбора действий. Эти возможности появятся позже. На этом этапе главная задача — научиться выполнять операции над данными и понимать, почему в Go важно явно работать с типами.\r
\r
## Цель работы\r
\r
Цель практической работы — закрепить материал главы 2.4 на примере продолжения мини-проекта.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- использовать арифметические операторы \`+\`, \`-\`, \`*\`, \`/\` и \`%\`;\r
- применять операторы \`++\` и \`+=\`;\r
- объединять строки с помощью \`+\`;\r
- сравнивать числовые значения;\r
- составлять логические выражения с \`&&\`, \`||\` и \`!\`;\r
- явно преобразовывать \`int\` в \`float64\`;\r
- преобразовывать строки в \`int\`, \`float64\` и \`bool\` через пакет \`strconv\`;\r
- преобразовывать числа и логические значения обратно в строки;\r
- использовать \`fmt.Printf\` и \`fmt.Sprintf\` для форматированного вывода.\r
\r
## Что должно получиться\r
\r
Программа должна попросить пользователя ввести данные о задаче:\r
\r
\`\`\`text\r
Введите название задачи: Подготовить практику по Go\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
Задача выполнена? (true/false): false\r
\`\`\`\r
\r
После этого программа должна вывести карточку задачи и результаты вычислений:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
Версия: 2\r
\r
Задача #1: Подготовить практику по Go\r
Статус: false\r
Приоритет: 1\r
Плановое время: 1.50 ч.\r
Это примерно: 1 ч. 30 мин.\r
Остаток рабочего дня после задачи: 6.50 ч.\r
Условный балл важности: 31.50\r
Метка: [1] Подготовить практику по Go — 1.50 ч.\r
\r
Проверки:\r
Приоритет корректен: true\r
Высокий приоритет: true\r
Короткая задача: false\r
Поместится в рабочий день: true\r
Можно начать сегодня: true\r
Требует внимания: true\r
Хороший кандидат для быстрого старта: false\r
\`\`\`\r
\r
Отдельные значения могут отличаться, если вы введёте другие данные.\r
\r
## Подготовка проекта\r
\r
Продолжайте работать с проектом из практики 2.2 или создайте новую папку:\r
\r
\`\`\`bash\r
todo-operations\r
\`\`\`\r
\r
Если создаёте новый проект, выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-operations\r
\`\`\`\r
\r
Создайте файл \`main.go\` и добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импортируемые пакеты\r
\r
В этой практической работе понадобится несколько стандартных пакетов.\r
\r
Замените импорт на следующий:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "math"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
Пакет \`bufio\` нужен для чтения строки с пробелами. Пакет \`os\` позволяет читать данные из стандартного ввода. Пакет \`strings\` используется для удаления лишних пробелов и символа переноса строки. Пакет \`strconv\` нужен для преобразования строк в числа и логические значения. Пакет \`math\` понадобится для округления количества минут.\r
\r
На этом этапе может показаться, что импортов стало много. Это нормально: программа начинает не только выводить текст, но и обрабатывать введённые данные.\r
\r
## Задание 2. Добавить константы приложения\r
\r
В начале функции \`main\` добавьте константы:\r
\r
\`\`\`go\r
const appName = "Go To-Do Manager"\r
const version = 2\r
const defaultTaskID = 1\r
const workDayHours = 8.0\r
\`\`\`\r
\r
Константа \`appName\` хранит название приложения. \`version\` показывает, что это уже вторая версия мини-проекта. \`defaultTaskID\` нужен как номер первой задачи. Пока в программе создаётся только одна задача, поэтому её номер можно задать константой.\r
\r
Константа \`workDayHours\` обозначает длину условного рабочего дня. В этой практике она нужна для расчёта: сколько времени останется после выполнения задачи.\r
\r
Здесь важно увидеть разницу между постоянными и изменяемыми данными. Название приложения и длина рабочего дня в рамках этой программы не меняются. Поэтому их можно хранить в константах. А данные задачи будут вводиться пользователем, поэтому они должны храниться в переменных.\r
\r
## Задание 3. Создать переменные для ввода\r
\r
В прошлой практике числовые значения можно было считывать сразу в \`int\` и \`float64\` через \`fmt.Scan\`. Теперь мы сделаем иначе: сначала считаем все данные как строки, а затем явно преобразуем их в нужные типы.\r
\r
Такой подход ближе к реальным программам. Данные из консоли, файла, формы на сайте или сети часто приходят именно в виде текста. Прежде чем складывать, сравнивать или умножать такие значения, их нужно преобразовать.\r
\r
Добавьте переменные:\r
\r
\`\`\`go\r
var taskTitle string\r
var priorityInput string\r
var hoursInput string\r
var completedInput string\r
\`\`\`\r
\r
Эти переменные будут хранить исходный пользовательский ввод. Пока это просто строки.\r
\r
Затем создайте \`reader\`:\r
\r
\`\`\`go\r
reader := bufio.NewReader(os.Stdin)\r
\`\`\`\r
\r
С его помощью можно читать целые строки, включая пробелы.\r
\r
## Задание 4. Считать данные задачи как строки\r
\r
Добавьте ввод названия задачи:\r
\r
\`\`\`go\r
fmt.Print("Введите название задачи: ")\r
taskTitle, _ = reader.ReadString('\\n')\r
taskTitle = strings.TrimSpace(taskTitle)\r
\`\`\`\r
\r
Название задачи читается через \`ReadString('\\n')\`, потому что оно может состоять из нескольких слов. Например, строка «Подготовить практику по Go» должна попасть в переменную целиком.\r
\r
Теперь считайте приоритет:\r
\r
\`\`\`go\r
fmt.Print("Введите приоритет задачи (1-3): ")\r
priorityInput, _ = reader.ReadString('\\n')\r
priorityInput = strings.TrimSpace(priorityInput)\r
\`\`\`\r
\r
Затем считайте примерное время выполнения:\r
\r
\`\`\`go\r
fmt.Print("Введите примерное время выполнения в часах: ")\r
hoursInput, _ = reader.ReadString('\\n')\r
hoursInput = strings.TrimSpace(hoursInput)\r
\`\`\`\r
\r
И наконец считайте статус выполнения:\r
\r
\`\`\`go\r
fmt.Print("Задача выполнена? (true/false): ")\r
completedInput, _ = reader.ReadString('\\n')\r
completedInput = strings.TrimSpace(completedInput)\r
\`\`\`\r
\r
После этого все введённые значения находятся в строковых переменных. Даже если пользователь ввёл \`1\` или \`1.5\`, для программы это пока текст, а не число. Поэтому следующая задача — преобразовать эти строки в нужные типы.\r
\r
## Задание 5. Преобразовать строки в нужные типы\r
\r
Приоритет задачи должен быть целым числом. Для преобразования строки в \`int\` используется \`strconv.Atoi\`.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
taskPriority, err := strconv.Atoi(priorityInput)\r
if err != nil {\r
    fmt.Println("Ошибка: приоритет должен быть целым числом")\r
    return\r
}\r
\`\`\`\r
\r
Функция \`Atoi\` возвращает два значения: результат и ошибку. Если пользователь ввёл корректное целое число, ошибка будет равна \`nil\`. Если вместо числа введён текст, например \`high\`, программа выведет сообщение об ошибке и завершится.\r
\r
Время выполнения должно быть дробным числом. Для этого используется \`strconv.ParseFloat\`:\r
\r
\`\`\`go\r
taskHours, err := strconv.ParseFloat(hoursInput, 64)\r
if err != nil {\r
    fmt.Println("Ошибка: время выполнения должно быть числом")\r
    return\r
}\r
\`\`\`\r
\r
Второй аргумент \`64\` означает, что результат будет иметь тип \`float64\`.\r
\r
Статус выполнения нужно преобразовать в \`bool\`:\r
\r
\`\`\`go\r
taskCompleted, err := strconv.ParseBool(completedInput)\r
if err != nil {\r
    fmt.Println("Ошибка: статус нужно ввести как true или false")\r
    return\r
}\r
\`\`\`\r
\r
\`ParseBool\` понимает значения \`"true"\`, \`"false"\`, \`"1"\`, \`"0"\` и некоторые варианты с разным регистром. Но для учебной практики лучше вводить именно \`true\` или \`false\`.\r
\r
Здесь впервые появляется проверка ошибки через \`if\`. Подробно условные конструкции будут разбираться в следующей главе, но для парсинга важно уже сейчас увидеть основной паттерн Go: функция возвращает результат и ошибку, а программа явно проверяет, можно ли продолжать работу.\r
\r
## Задание 6. Использовать арифметические операции\r
\r
Теперь данные уже имеют подходящие типы, и с ними можно выполнять операции.\r
\r
Создайте переменные для подсчёта количества задач и общего планового времени:\r
\r
\`\`\`go\r
taskCount := 0\r
totalPlannedHours := 0.0\r
\r
taskCount++\r
totalPlannedHours += taskHours\r
\`\`\`\r
\r
Сначала \`taskCount\` равен \`0\`. Затем оператор \`++\` увеличивает его на единицу, потому что пользователь добавил одну задачу.\r
\r
Переменная \`totalPlannedHours\` сначала равна \`0.0\`. Затем оператор \`+=\` прибавляет к ней время текущей задачи. Сейчас задача одна, поэтому общее время совпадает с \`taskHours\`. Позже, когда появятся срезы и циклы, эта идея станет полезнее: программа сможет суммировать время всех задач.\r
\r
Теперь рассчитайте, сколько времени останется в условном рабочем дне:\r
\r
\`\`\`go\r
remainingHours := workDayHours - totalPlannedHours\r
\`\`\`\r
\r
Здесь используется оператор \`-\`. Если рабочий день длится \`8.0\` часов, а задача занимает \`1.5\` часа, остаток будет равен \`6.5\`.\r
\r
## Задание 7. Перевести часы в часы и минуты\r
\r
Пользователю удобно видеть не только дробное количество часов, но и примерное представление в часах и минутах.\r
\r
Сначала переведём часы в минуты:\r
\r
\`\`\`go\r
totalMinutes := int(math.Round(taskHours * 60))\r
\`\`\`\r
\r
Здесь используется несколько действий.\r
\r
Сначала \`taskHours * 60\` переводит часы в минуты. Например, \`1.5 * 60\` даёт \`90\`.\r
\r
Функция \`math.Round\` округляет результат. Она возвращает \`float64\`, поэтому затем используется явное преобразование:\r
\r
\`\`\`go\r
int(...)\r
\`\`\`\r
\r
После этого можно получить количество полных часов и остаток минут:\r
\r
\`\`\`go\r
hoursPart := totalMinutes / 60\r
minutesPart := totalMinutes % 60\r
\`\`\`\r
\r
Оператор \`/\` при работе с целыми числами возвращает целую часть деления. Если \`totalMinutes\` равен \`90\`, то \`90 / 60\` даст \`1\`.\r
\r
Оператор \`%\` возвращает остаток от деления. В примере \`90 % 60\` даст \`30\`.\r
\r
Именно поэтому из \`1.5\` часа можно получить вывод:\r
\r
\`\`\`text\r
1 ч. 30 мин.\r
\`\`\`\r
\r
Этот пример хорошо показывает практическое применение оператора \`%\`. Он часто используется, когда нужно отделить «целые части» от остатка: часы и минуты, рубли и копейки, страницы и оставшиеся элементы.\r
\r
## Задание 8. Выполнить сравнения\r
\r
Теперь добавим несколько проверок. Пока программа не будет выбирать разные действия в зависимости от результата. Она просто рассчитает логические значения и выведет их.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
validPriority := taskPriority >= 1 && taskPriority <= 3\r
isHighPriority := taskPriority == 1\r
isShortTask := taskHours <= 1\r
canFitInWorkDay := taskHours <= workDayHours\r
\`\`\`\r
\r
Переменная \`validPriority\` показывает, находится ли приоритет в допустимом диапазоне от \`1\` до \`3\`. Здесь используются операторы сравнения \`>=\` и \`<=\`, а также логический оператор \`&&\`.\r
\r
Переменная \`isHighPriority\` будет равна \`true\`, если приоритет равен \`1\`.\r
\r
Переменная \`isShortTask\` показывает, занимает ли задача один час или меньше.\r
\r
Переменная \`canFitInWorkDay\` показывает, помещается ли задача в условный рабочий день.\r
\r
Все эти переменные имеют тип \`bool\`, потому что результат любого сравнения — это логическое значение.\r
\r
## Задание 9. Составить логические выражения\r
\r
Теперь используем логические операторы для более сложных проверок.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
canStartToday := canFitInWorkDay && !taskCompleted\r
needsAttention := isHighPriority && !taskCompleted\r
goodQuickStart := isHighPriority && isShortTask && !taskCompleted\r
\`\`\`\r
\r
Выражение:\r
\r
\`\`\`go\r
canFitInWorkDay && !taskCompleted\r
\`\`\`\r
\r
означает: задачу можно начать сегодня, если она помещается в рабочий день и ещё не выполнена.\r
\r
Оператор \`!\` инвертирует логическое значение. Если \`taskCompleted\` равно \`false\`, то \`!taskCompleted\` будет равно \`true\`.\r
\r
Переменная \`needsAttention\` показывает, требует ли задача внимания. В этой версии программы задача требует внимания, если она имеет высокий приоритет и ещё не выполнена.\r
\r
Переменная \`goodQuickStart\` показывает, является ли задача хорошим кандидатом для быстрого старта: высокий приоритет, короткое время выполнения и статус «не выполнена».\r
\r
В следующей главе эти логические выражения можно будет использовать внутри \`if\`, чтобы программа не просто выводила \`true\` или \`false\`, а принимала решения.\r
\r
## Задание 10. Рассчитать условный балл важности\r
\r
Теперь добавим небольшой расчёт, в котором нужно явно преобразовать \`int\` в \`float64\`.\r
\r
Пусть приоритет \`1\` считается самым важным, а приоритет \`3\` — менее важным. Для расчёта можно использовать такую формулу:\r
\r
\`\`\`go\r
priorityWeight := 4 - taskPriority\r
taskScore := float64(priorityWeight)*10 + taskHours\r
\`\`\`\r
\r
Если \`taskPriority\` равен \`1\`, то \`priorityWeight\` будет равен \`3\`. Если приоритет равен \`3\`, то \`priorityWeight\` будет равен \`1\`.\r
\r
Затем значение \`priorityWeight\` преобразуется в \`float64\`, потому что \`taskHours\` уже имеет тип \`float64\`.\r
\r
Без преобразования такой код вызвал бы ошибку:\r
\r
\`\`\`go\r
taskScore := priorityWeight*10 + taskHours\r
\`\`\`\r
\r
Проблема в том, что \`priorityWeight*10\` имеет тип \`int\`, а \`taskHours\` имеет тип \`float64\`. Go не складывает эти типы автоматически. Поэтому нужно явно сказать компилятору, что целое число можно использовать как дробное:\r
\r
\`\`\`go\r
float64(priorityWeight)\r
\`\`\`\r
\r
Это не меняет исходную переменную \`priorityWeight\`. Преобразование создаёт новое значение типа \`float64\` только для конкретного выражения.\r
\r
## Задание 11. Преобразовать значения в строки\r
\r
В главе 2.4 рассматривается не только преобразование строк в числа, но и обратная задача: число или логическое значение можно представить как строку.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
idText := strconv.Itoa(defaultTaskID)\r
hoursText := strconv.FormatFloat(taskHours, 'f', 2, 64)\r
completedText := strconv.FormatBool(taskCompleted)\r
\`\`\`\r
\r
\`strconv.Itoa\` преобразует \`int\` в строку.\r
\r
\`strconv.FormatFloat\` преобразует \`float64\` в строку. Аргумент \`'f'\` означает обычную десятичную запись, \`2\` — два знака после точки, \`64\` — размер исходного числа.\r
\r
\`strconv.FormatBool\` преобразует \`bool\` в строку \`"true"\` или \`"false"\`.\r
\r
Теперь можно составить строку через конкатенацию:\r
\r
\`\`\`go\r
taskHeader := "Задача #" + idText + ": " + taskTitle\r
\`\`\`\r
\r
Оператор \`+\` объединяет строки. Но он не может автоматически объединить строку и число. Именно поэтому \`defaultTaskID\` сначала был преобразован в строку через \`strconv.Itoa\`.\r
\r
Для более сложной строки используйте \`fmt.Sprintf\`:\r
\r
\`\`\`go\r
taskLabel := fmt.Sprintf("[%s] %s — %s ч.", idText, taskTitle, hoursText)\r
\`\`\`\r
\r
\`fmt.Sprintf\` удобен, когда нужно собрать строку из нескольких значений и сохранить её в переменную.\r
\r
## Задание 12. Вывести результат\r
\r
Теперь выведите карточку задачи:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("===", appName, "===")\r
fmt.Println("Версия:", version)\r
fmt.Println()\r
\r
fmt.Println(taskHeader)\r
fmt.Println("Статус:", completedText)\r
fmt.Printf("Приоритет: %d\\n", taskPriority)\r
fmt.Printf("Плановое время: %.2f ч.\\n", taskHours)\r
fmt.Printf("Это примерно: %d ч. %d мин.\\n", hoursPart, minutesPart)\r
fmt.Printf("Остаток рабочего дня после задачи: %.2f ч.\\n", remainingHours)\r
fmt.Printf("Условный балл важности: %.2f\\n", taskScore)\r
fmt.Println("Метка:", taskLabel)\r
\`\`\`\r
\r
Здесь используются разные способы вывода. \`fmt.Println\` удобен для простых строк. \`fmt.Printf\` удобен там, где нужно управлять форматом числа, например вывести два знака после точки.\r
\r
Теперь выведите результаты проверок:\r
\r
\`\`\`go\r
fmt.Println()\r
fmt.Println("Проверки:")\r
fmt.Printf("Приоритет корректен: %t\\n", validPriority)\r
fmt.Printf("Высокий приоритет: %t\\n", isHighPriority)\r
fmt.Printf("Короткая задача: %t\\n", isShortTask)\r
fmt.Printf("Поместится в рабочий день: %t\\n", canFitInWorkDay)\r
fmt.Printf("Можно начать сегодня: %t\\n", canStartToday)\r
fmt.Printf("Требует внимания: %t\\n", needsAttention)\r
fmt.Printf("Хороший кандидат для быстрого старта: %t\\n", goodQuickStart)\r
\`\`\`\r
\r
Форматный глагол \`%t\` используется для вывода значений типа \`bool\`.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "math"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
func main() {\r
    const appName = "Go To-Do Manager"\r
    const version = 2\r
    const defaultTaskID = 1\r
    const workDayHours = 8.0\r
\r
    var taskTitle string\r
    var priorityInput string\r
    var hoursInput string\r
    var completedInput string\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    fmt.Print("Введите название задачи: ")\r
    taskTitle, _ = reader.ReadString('\\n')\r
    taskTitle = strings.TrimSpace(taskTitle)\r
\r
    fmt.Print("Введите приоритет задачи (1-3): ")\r
    priorityInput, _ = reader.ReadString('\\n')\r
    priorityInput = strings.TrimSpace(priorityInput)\r
\r
    fmt.Print("Введите примерное время выполнения в часах: ")\r
    hoursInput, _ = reader.ReadString('\\n')\r
    hoursInput = strings.TrimSpace(hoursInput)\r
\r
    fmt.Print("Задача выполнена? (true/false): ")\r
    completedInput, _ = reader.ReadString('\\n')\r
    completedInput = strings.TrimSpace(completedInput)\r
\r
    taskPriority, err := strconv.Atoi(priorityInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть целым числом")\r
        return\r
    }\r
\r
    taskHours, err := strconv.ParseFloat(hoursInput, 64)\r
    if err != nil {\r
        fmt.Println("Ошибка: время выполнения должно быть числом")\r
        return\r
    }\r
\r
    taskCompleted, err := strconv.ParseBool(completedInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: статус нужно ввести как true или false")\r
        return\r
    }\r
\r
    taskCount := 0\r
    totalPlannedHours := 0.0\r
\r
    taskCount++\r
    totalPlannedHours += taskHours\r
\r
    remainingHours := workDayHours - totalPlannedHours\r
\r
    totalMinutes := int(math.Round(taskHours * 60))\r
    hoursPart := totalMinutes / 60\r
    minutesPart := totalMinutes % 60\r
\r
    validPriority := taskPriority >= 1 && taskPriority <= 3\r
    isHighPriority := taskPriority == 1\r
    isShortTask := taskHours <= 1\r
    canFitInWorkDay := taskHours <= workDayHours\r
\r
    canStartToday := canFitInWorkDay && !taskCompleted\r
    needsAttention := isHighPriority && !taskCompleted\r
    goodQuickStart := isHighPriority && isShortTask && !taskCompleted\r
\r
    priorityWeight := 4 - taskPriority\r
    taskScore := float64(priorityWeight)*10 + taskHours\r
\r
    idText := strconv.Itoa(defaultTaskID)\r
    hoursText := strconv.FormatFloat(taskHours, 'f', 2, 64)\r
    completedText := strconv.FormatBool(taskCompleted)\r
\r
    taskHeader := "Задача #" + idText + ": " + taskTitle\r
    taskLabel := fmt.Sprintf("[%s] %s — %s ч.", idText, taskTitle, hoursText)\r
\r
    fmt.Println()\r
    fmt.Println("===", appName, "===")\r
    fmt.Println("Версия:", version)\r
    fmt.Println()\r
\r
    fmt.Println(taskHeader)\r
    fmt.Println("Статус:", completedText)\r
    fmt.Printf("Приоритет: %d\\n", taskPriority)\r
    fmt.Printf("Плановое время: %.2f ч.\\n", taskHours)\r
    fmt.Printf("Это примерно: %d ч. %d мин.\\n", hoursPart, minutesPart)\r
    fmt.Printf("Остаток рабочего дня после задачи: %.2f ч.\\n", remainingHours)\r
    fmt.Printf("Условный балл важности: %.2f\\n", taskScore)\r
    fmt.Println("Метка:", taskLabel)\r
\r
    fmt.Println()\r
    fmt.Println("Проверки:")\r
    fmt.Printf("Приоритет корректен: %t\\n", validPriority)\r
    fmt.Printf("Высокий приоритет: %t\\n", isHighPriority)\r
    fmt.Printf("Короткая задача: %t\\n", isShortTask)\r
    fmt.Printf("Поместится в рабочий день: %t\\n", canFitInWorkDay)\r
    fmt.Printf("Можно начать сегодня: %t\\n", canStartToday)\r
    fmt.Printf("Требует внимания: %t\\n", needsAttention)\r
    fmt.Printf("Хороший кандидат для быстрого старта: %t\\n", goodQuickStart)\r
\r
    fmt.Println()\r
    fmt.Println("Всего задач:", taskCount)\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
Введите название задачи: Подготовить практику по Go\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
Задача выполнена? (true/false): false\r
\r
=== Go To-Do Manager ===\r
Версия: 2\r
\r
Задача #1: Подготовить практику по Go\r
Статус: false\r
Приоритет: 1\r
Плановое время: 1.50 ч.\r
Это примерно: 1 ч. 30 мин.\r
Остаток рабочего дня после задачи: 6.50 ч.\r
Условный балл важности: 31.50\r
Метка: [1] Подготовить практику по Go — 1.50 ч.\r
\r
Проверки:\r
Приоритет корректен: true\r
Высокий приоритет: true\r
Короткая задача: false\r
Поместится в рабочий день: true\r
Можно начать сегодня: true\r
Требует внимания: true\r
Хороший кандидат для быстрого старта: false\r
\r
Всего задач: 1\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии To-Do приложения пользовательский ввод сначала сохраняется в строковых переменных. Это сделано намеренно: в реальных программах данные часто приходят именно как текст. После этого программа явно преобразует строки в нужные типы.\r
\r
Приоритет преобразуется через \`strconv.Atoi\`, потому что должен стать целым числом. Время выполнения преобразуется через \`strconv.ParseFloat\`, потому что может быть дробным. Статус выполнения преобразуется через \`strconv.ParseBool\`, потому что он должен стать логическим значением.\r
\r
После преобразования программа может выполнять операции. Она прибавляет время задачи к общему плановому времени, вычитает его из условного рабочего дня, переводит часы в минуты, а затем с помощью деления и остатка от деления получает часы и минуты отдельно.\r
\r
Сравнения позволяют получить логические характеристики задачи: корректен ли приоритет, высокий ли он, короткая ли задача, помещается ли она в рабочий день. Логические операторы позволяют собрать более сложные выводы: можно ли начать задачу сегодня, требует ли она внимания, подходит ли она для быстрого старта.\r
\r
Отдельно в программе показано, почему Go требует явных преобразований. При расчёте \`taskScore\` целочисленный вес приоритета преобразуется в \`float64\`, чтобы его можно было сложить с дробным временем задачи.\r
\r
В конце программа преобразует число и логическое значение обратно в строки. Это нужно для формирования текстовых фрагментов: заголовка задачи, статуса и метки.\r
\r
## Самостоятельное задание\r
\r
Измените программу так, чтобы она дополнительно запрашивала количество дней до дедлайна.\r
\r
Сначала считайте значение как строку:\r
\r
\`\`\`go\r
var deadlineInput string\r
\r
fmt.Print("Сколько дней осталось до дедлайна? ")\r
deadlineInput, _ = reader.ReadString('\\n')\r
deadlineInput = strings.TrimSpace(deadlineInput)\r
\`\`\`\r
\r
Затем преобразуйте его в \`int\`:\r
\r
\`\`\`go\r
daysLeft, err := strconv.Atoi(deadlineInput)\r
if err != nil {\r
    fmt.Println("Ошибка: количество дней должно быть целым числом")\r
    return\r
}\r
\`\`\`\r
\r
После этого создайте логические переменные:\r
\r
\`\`\`go\r
isUrgent := daysLeft <= 2\r
urgentAndImportant := isUrgent && isHighPriority && !taskCompleted\r
\`\`\`\r
\r
Выведите их в блоке проверок:\r
\r
\`\`\`go\r
fmt.Printf("Срочная задача: %t\\n", isUrgent)\r
fmt.Printf("Срочная и важная задача: %t\\n", urgentAndImportant)\r
\`\`\`\r
\r
Это задание подготавливает программу к следующей главе. Позже такие логические выражения можно будет использовать в \`if\`, чтобы программа сама выбирала, какое сообщение показать пользователю.\r
\r
## Вопросы для самопроверки\r
\r
1. Почему пользовательский ввод часто удобно сначала сохранять как строку?\r
2. Для чего используется \`strconv.Atoi\`?\r
3. Чем отличается \`strconv.Atoi\` от \`strconv.ParseFloat\`?\r
4. Почему функции парсинга возвращают два значения?\r
5. Что означает \`err != nil\`?\r
6. Почему Go не складывает \`int\` и \`float64\` автоматически?\r
7. Для чего в программе используется \`float64(priorityWeight)\`?\r
8. Что делает оператор \`++\`?\r
9. Чем отличается \`taskCount++\` от \`taskCount += 1\`?\r
10. Для чего используется оператор \`%\` при переводе минут в часы и минуты?\r
11. Почему результат сравнения имеет тип \`bool\`?\r
12. Что делает оператор \`&&\`?\r
13. Что делает оператор \`||\`?\r
14. Что делает оператор \`!\`?\r
15. Почему строку и число нельзя объединить через \`+\` без преобразования?\r
16. Чем отличается \`fmt.Sprintf\` от \`strconv.Itoa\`?\r
17. Для чего используется \`strconv.FormatFloat\`?\r
18. Почему \`fmt.Printf\` удобен для вывода времени с двумя знаками после точки?\r
`,h=`# Практическая работа 2.5. To-Do список: меню, условия и циклы\r
\r
## Тема\r
\r
Управление потоком выполнения в Go: \`if\`, \`else\`, \`else if\`, \`for\`, \`break\`, \`continue\`, \`range\` и \`switch\`.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В предыдущих версиях программа уже умела получать данные о задаче, преобразовывать строки в числа, выполнять вычисления и выводить результаты проверок.\r
\r
Теперь программа станет интерактивной. Пользователь сможет не просто один раз ввести данные и увидеть результат, а выбирать действия из меню. Для этого понадобится управление потоком выполнения: условия, циклы и множественный выбор.\r
\r
В этой версии приложение всё ещё будет работать только с одной задачей. Это сделано специально: списки задач удобнее реализовывать после изучения массивов и срезов. Сейчас важно сосредоточиться не на хранении множества данных, а на логике выполнения программы.\r
\r
После этой практики To-Do приложение будет уметь:\r
\r
- показывать меню действий;\r
- повторять работу программы до выбора выхода;\r
- выводить карточку задачи;\r
- отмечать задачу выполненной;\r
- изменять приоритет задачи;\r
- анализировать название задачи посимвольно;\r
- выбирать текстовые сообщения через \`if\` и \`switch\`.\r
\r
## Цель работы\r
\r
Цель практической работы — закрепить тему управления потоком выполнения на примере мини-проекта.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- использовать \`if\` для проверки условий;\r
- применять \`else\` и \`else if\` для нескольких вариантов поведения;\r
- создавать цикл \`for\`, который повторяет работу программы;\r
- использовать \`continue\` для пропуска текущей итерации;\r
- использовать \`break\` для досрочного завершения цикла;\r
- применять \`switch\` для выбора действия из меню;\r
- использовать \`switch\` без значения как замену длинной цепочке \`if/else if\`;\r
- перебирать символы строки через \`for ... range\`;\r
- контролировать область видимости переменных внутри условий и циклов.\r
\r
## Что должно получиться\r
\r
После запуска программа сначала попросит пользователя ввести задачу:\r
\r
\`\`\`text\r
Введите название задачи: Подготовить практику по Go\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
\`\`\`\r
\r
После этого появится меню:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
Версия: 3\r
\r
1 — Показать задачу\r
2 — Отметить задачу выполненной\r
3 — Изменить приоритет\r
4 — Проанализировать название задачи\r
5 — Показать рекомендацию\r
0 — Выйти\r
\r
Выберите действие:\r
\`\`\`\r
\r
Пользователь сможет выбирать действия до тех пор, пока не введёт \`0\`.\r
\r
## Подготовка проекта\r
\r
Продолжайте работать с проектом из практики 2.4 или создайте новую папку:\r
\r
\`\`\`bash\r
todo-flow\r
\`\`\`\r
\r
Если создаёте новый проект, выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-flow\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импортируемые пакеты\r
\r
В этой практической работе пользовательский ввод снова будет считываться как строки, а затем преобразовываться в нужные типы. Поэтому понадобятся уже знакомые пакеты:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
Пакет \`bufio\` используется для чтения строки целиком. Пакет \`os\` нужен для доступа к стандартному вводу. Пакет \`strings\` помогает убрать лишние пробелы и перенос строки. Пакет \`strconv\` нужен для преобразования строк в числа.\r
\r
## Задание 2. Добавить константы и переменные задачи\r
\r
В начале функции \`main\` добавьте константы приложения:\r
\r
\`\`\`go\r
const appName = "Go To-Do Manager"\r
const version = 3\r
const taskID = 1\r
\`\`\`\r
\r
Версия увеличивается до \`3\`, потому что это уже третья версия мини-проекта: первая была связана с переменными и вводом, вторая — с операциями и преобразованиями, а теперь появляется управление программой через меню.\r
\r
Теперь добавьте переменные задачи:\r
\r
\`\`\`go\r
var taskTitle string\r
var priorityInput string\r
var hoursInput string\r
\r
var taskPriority int\r
var taskHours float64\r
var taskCompleted bool\r
\`\`\`\r
\r
Здесь переменные разделены на две группы.\r
\r
Первая группа хранит исходный пользовательский ввод:\r
\r
\`\`\`go\r
var taskTitle string\r
var priorityInput string\r
var hoursInput string\r
\`\`\`\r
\r
Вторая группа хранит уже обработанные данные:\r
\r
\`\`\`go\r
var taskPriority int\r
var taskHours float64\r
var taskCompleted bool\r
\`\`\`\r
\r
Переменная \`taskCompleted\` не получает начального значения, поэтому Go автоматически присваивает ей \`false\`. Это подходит по смыслу: новая задача ещё не выполнена.\r
\r
## Задание 3. Считать начальные данные задачи\r
\r
Создайте \`reader\`:\r
\r
\`\`\`go\r
reader := bufio.NewReader(os.Stdin)\r
\`\`\`\r
\r
Теперь считайте название задачи:\r
\r
\`\`\`go\r
fmt.Print("Введите название задачи: ")\r
taskTitle, _ = reader.ReadString('\\n')\r
taskTitle = strings.TrimSpace(taskTitle)\r
\`\`\`\r
\r
Название читается через \`ReadString('\\n')\`, потому что оно может состоять из нескольких слов.\r
\r
Считайте приоритет:\r
\r
\`\`\`go\r
fmt.Print("Введите приоритет задачи (1-3): ")\r
priorityInput, _ = reader.ReadString('\\n')\r
priorityInput = strings.TrimSpace(priorityInput)\r
\`\`\`\r
\r
Считайте примерное время выполнения:\r
\r
\`\`\`go\r
fmt.Print("Введите примерное время выполнения в часах: ")\r
hoursInput, _ = reader.ReadString('\\n')\r
hoursInput = strings.TrimSpace(hoursInput)\r
\`\`\`\r
\r
Теперь преобразуйте строки в нужные типы:\r
\r
\`\`\`go\r
var err error\r
\r
taskPriority, err = strconv.Atoi(priorityInput)\r
if err != nil {\r
    fmt.Println("Ошибка: приоритет должен быть целым числом")\r
    return\r
}\r
\r
taskHours, err = strconv.ParseFloat(hoursInput, 64)\r
if err != nil {\r
    fmt.Println("Ошибка: время выполнения должно быть числом")\r
    return\r
}\r
\`\`\`\r
\r
Здесь используется \`if\` для проверки ошибки. Если преобразование прошло неудачно, программа выводит сообщение и завершается через \`return\`.\r
\r
Подробная обработка ошибок пока не является главной темой, но такой подход полезен: программа не должна продолжать работу с некорректными данными.\r
\r
## Задание 4. Проверить корректность приоритета через if/else if/else\r
\r
Теперь добавим проверку диапазона приоритета.\r
\r
Приоритет должен быть числом от \`1\` до \`3\`. Значение \`1\` будет означать высокий приоритет, \`2\` — средний, \`3\` — низкий.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
if taskPriority < 1 {\r
    fmt.Println("Ошибка: приоритет не может быть меньше 1")\r
    return\r
} else if taskPriority > 3 {\r
    fmt.Println("Ошибка: приоритет не может быть больше 3")\r
    return\r
} else {\r
    fmt.Println("Задача создана")\r
}\r
\`\`\`\r
\r
Этот фрагмент показывает цепочку \`if/else if/else\`.\r
\r
Сначала программа проверяет, не меньше ли приоритет \`1\`. Если это так, дальнейшие проверки уже не нужны: программа выводит ошибку и завершается.\r
\r
Если первое условие не подошло, проверяется второе: не больше ли приоритет \`3\`.\r
\r
Если оба условия ложны, значит значение находится в допустимом диапазоне. Тогда выполняется ветка \`else\`.\r
\r
Здесь хорошо видно важное свойство \`else if\`: условия проверяются сверху вниз, и выполняется только первая подходящая ветка.\r
\r
## Задание 5. Создать основной цикл программы\r
\r
Теперь нужно сделать так, чтобы программа не завершалась сразу после создания задачи. Она должна показывать меню снова и снова, пока пользователь не выберет выход.\r
\r
Для этого используется цикл \`for\`.\r
\r
Добавьте после проверки приоритета такой код:\r
\r
\`\`\`go\r
for {\r
    fmt.Println()\r
    fmt.Println("===", appName, "===")\r
    fmt.Println("Версия:", version)\r
    fmt.Println()\r
    fmt.Println("1 — Показать задачу")\r
    fmt.Println("2 — Отметить задачу выполненной")\r
    fmt.Println("3 — Изменить приоритет")\r
    fmt.Println("4 — Проанализировать название задачи")\r
    fmt.Println("5 — Показать рекомендацию")\r
    fmt.Println("0 — Выйти")\r
    fmt.Println()\r
\r
    fmt.Print("Выберите действие: ")\r
    command, _ := reader.ReadString('\\n')\r
    command = strings.TrimSpace(command)\r
\r
    if command == "" {\r
        fmt.Println("Команда не введена")\r
        continue\r
    }\r
}\r
\`\`\`\r
\r
Такой цикл называется бесконечным, потому что после \`for\` нет условия. Он будет повторяться до тех пор, пока внутри цикла не произойдёт выход.\r
\r
В этом фрагменте уже используется \`continue\`. Если пользователь просто нажал Enter и не ввёл команду, программа выводит сообщение и сразу переходит к следующей итерации цикла. Остальной код текущей итерации не выполняется.\r
\r
Пока в цикле нет выхода, поэтому после запуска программу придётся остановить вручную. На следующем шаге мы добавим \`switch\` и команду \`0\`.\r
\r
## Задание 6. Добавить switch для выбора действия\r
\r
Меню удобно обрабатывать через \`switch\`: каждое значение команды соответствует отдельному \`case\`.\r
\r
Внутри цикла после проверки пустой команды добавьте:\r
\r
\`\`\`go\r
switch command {\r
case "1":\r
    fmt.Println("Показать задачу")\r
case "2":\r
    fmt.Println("Отметить задачу выполненной")\r
case "3":\r
    fmt.Println("Изменить приоритет")\r
case "4":\r
    fmt.Println("Проанализировать название задачи")\r
case "5":\r
    fmt.Println("Показать рекомендацию")\r
case "0":\r
    fmt.Println("Выход из программы")\r
    return\r
default:\r
    fmt.Println("Неизвестная команда")\r
}\r
\`\`\`\r
\r
Теперь программа может реагировать на выбор пользователя.\r
\r
Обратите внимание: в Go не нужно писать \`break\` в конце каждого \`case\`. После выполнения подходящей ветки \`switch\` завершается автоматически.\r
\r
Для выхода из программы в пункте \`0\` используется \`return\`. Он завершает функцию \`main\`, а значит завершает всю программу.\r
\r
## Задание 7. Реализовать пункт 1 — показать задачу\r
\r
Теперь заменим временный текст в пункте \`1\` на настоящий вывод карточки задачи.\r
\r
Внутри \`case "1"\` добавьте:\r
\r
\`\`\`go\r
priorityText := ""\r
\r
switch taskPriority {\r
case 1:\r
    priorityText = "высокий"\r
case 2:\r
    priorityText = "средний"\r
case 3:\r
    priorityText = "низкий"\r
default:\r
    priorityText = "неизвестный"\r
}\r
\r
statusText := ""\r
if taskCompleted {\r
    statusText = "выполнена"\r
} else {\r
    statusText = "не выполнена"\r
}\r
\r
fmt.Println()\r
fmt.Printf("Задача #%d: %s\\n", taskID, taskTitle)\r
fmt.Println("Статус:", statusText)\r
fmt.Println("Приоритет:", priorityText)\r
fmt.Printf("Плановое время: %.2f ч.\\n", taskHours)\r
\`\`\`\r
\r
Здесь используется сразу два инструмента управления потоком.\r
\r
Первый — \`switch\`. Он преобразует числовой приоритет в понятный текст. Если \`taskPriority\` равен \`1\`, переменная \`priorityText\` получает значение \`"высокий"\`. Если равен \`2\` — \`"средний"\`, если \`3\` — \`"низкий"\`.\r
\r
Второй — \`if/else\`. Он выбирает текст статуса в зависимости от значения \`taskCompleted\`.\r
\r
Такой код делает вывод программы понятнее для пользователя. Вместо \`true\` и \`false\` он видит обычные слова: «выполнена» или «не выполнена».\r
\r
## Задание 8. Реализовать пункт 2 — отметить задачу выполненной\r
\r
Теперь добавим возможность изменить статус задачи.\r
\r
Замените код в \`case "2"\` на следующий:\r
\r
\`\`\`go\r
if taskCompleted {\r
    fmt.Println("Задача уже была отмечена как выполненная")\r
} else {\r
    taskCompleted = true\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\`\`\`\r
\r
Здесь условие защищает программу от бессмысленного повторного действия.\r
\r
Если задача уже выполнена, программа просто сообщает об этом. Если задача ещё не выполнена, значение переменной \`taskCompleted\` меняется с \`false\` на \`true\`.\r
\r
Это хороший пример того, как \`if\` используется в реальной логике приложения: программа выбирает действие в зависимости от текущего состояния.\r
\r
## Задание 9. Реализовать пункт 3 — изменить приоритет\r
\r
Теперь добавим изменение приоритета.\r
\r
Внутри \`case "3"\` добавьте:\r
\r
\`\`\`go\r
fmt.Print("Введите новый приоритет (1-3): ")\r
newPriorityInput, _ := reader.ReadString('\\n')\r
newPriorityInput = strings.TrimSpace(newPriorityInput)\r
\r
newPriority, err := strconv.Atoi(newPriorityInput)\r
if err != nil {\r
    fmt.Println("Ошибка: приоритет должен быть целым числом")\r
    continue\r
}\r
\r
if newPriority < 1 || newPriority > 3 {\r
    fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
    continue\r
}\r
\r
taskPriority = newPriority\r
fmt.Println("Приоритет обновлён")\r
\`\`\`\r
\r
Здесь пользовательский ввод снова считывается как строка и преобразуется в число через \`strconv.Atoi\`.\r
\r
Если преобразование не удалось, программа выводит ошибку и использует \`continue\`. Это значит, что текущая итерация основного цикла завершается, и пользователь снова увидит меню.\r
\r
Затем проверяется диапазон значения:\r
\r
\`\`\`go\r
if newPriority < 1 || newPriority > 3\r
\`\`\`\r
\r
Оператор \`||\` означает «или». Условие истинно, если приоритет меньше \`1\` или больше \`3\`.\r
\r
Если значение корректно, оно записывается в основную переменную:\r
\r
\`\`\`go\r
taskPriority = newPriority\r
\`\`\`\r
\r
## Задание 10. Реализовать пункт 4 — проанализировать название задачи\r
\r
Теперь используем цикл \`for ... range\`, \`continue\` и \`break\`.\r
\r
Внутри \`case "4"\` добавьте:\r
\r
\`\`\`go\r
fmt.Println("Первые пять непустых символов названия:")\r
\r
shownCharacters := 0\r
\r
for index, character := range taskTitle {\r
    if character == ' ' {\r
        continue\r
    }\r
\r
    fmt.Printf("Позиция %d: %s\\n", index, string(character))\r
    shownCharacters++\r
\r
    if shownCharacters == 5 {\r
        break\r
    }\r
}\r
\r
if shownCharacters == 0 {\r
    fmt.Println("В названии задачи нет символов для анализа")\r
}\r
\`\`\`\r
\r
Цикл \`range\` проходит по строке \`taskTitle\`. На каждой итерации он даёт два значения: индекс символа и сам символ.\r
\r
Если символ является пробелом, срабатывает \`continue\`. Это значит, что пробел не выводится, а программа сразу переходит к следующему символу.\r
\r
Переменная \`shownCharacters\` считает, сколько символов уже было показано. Когда их становится \`5\`, срабатывает \`break\`, и цикл завершается досрочно.\r
\r
Таким образом, в этом пункте меню используются оба инструмента: \`continue\` пропускает пробелы, а \`break\` останавливает цикл после пяти символов.\r
\r
## Задание 11. Реализовать пункт 5 — показать рекомендацию\r
\r
Теперь добавим рекомендацию по задаче. Для этого удобно использовать \`switch\` без значения.\r
\r
Внутри \`case "5"\` добавьте:\r
\r
\`\`\`go\r
switch {\r
case taskCompleted:\r
    fmt.Println("Задача уже выполнена. Можно переходить к следующей.")\r
case taskPriority == 1 && taskHours <= 1:\r
    fmt.Println("Это важная и короткая задача. Хороший кандидат для быстрого старта.")\r
case taskPriority == 1:\r
    fmt.Println("Это важная задача. Лучше запланировать её в первую очередь.")\r
case taskHours > 4:\r
    fmt.Println("Задача большая. Возможно, её стоит разделить на части.")\r
default:\r
    fmt.Println("Задача выглядит обычной. Выполните её по плану.")\r
}\r
\`\`\`\r
\r
Обычный \`switch\` сравнивает одно значение с разными вариантами. Но \`switch\` без значения работает иначе: каждый \`case\` содержит условие.\r
\r
Программа проверяет условия сверху вниз и выполняет первое подходящее.\r
\r
Например, если задача уже выполнена, сработает первый \`case\`, а остальные проверяться не будут. Если задача не выполнена, имеет высокий приоритет и занимает меньше часа, сработает второй \`case\`.\r
\r
Такой вариант часто читается легче, чем длинная цепочка \`if/else if\`.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
func main() {\r
    const appName = "Go To-Do Manager"\r
    const version = 3\r
    const taskID = 1\r
\r
    var taskTitle string\r
    var priorityInput string\r
    var hoursInput string\r
\r
    var taskPriority int\r
    var taskHours float64\r
    var taskCompleted bool\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    fmt.Print("Введите название задачи: ")\r
    taskTitle, _ = reader.ReadString('\\n')\r
    taskTitle = strings.TrimSpace(taskTitle)\r
\r
    fmt.Print("Введите приоритет задачи (1-3): ")\r
    priorityInput, _ = reader.ReadString('\\n')\r
    priorityInput = strings.TrimSpace(priorityInput)\r
\r
    fmt.Print("Введите примерное время выполнения в часах: ")\r
    hoursInput, _ = reader.ReadString('\\n')\r
    hoursInput = strings.TrimSpace(hoursInput)\r
\r
    var err error\r
\r
    taskPriority, err = strconv.Atoi(priorityInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть целым числом")\r
        return\r
    }\r
\r
    taskHours, err = strconv.ParseFloat(hoursInput, 64)\r
    if err != nil {\r
        fmt.Println("Ошибка: время выполнения должно быть числом")\r
        return\r
    }\r
\r
    if taskPriority < 1 {\r
        fmt.Println("Ошибка: приоритет не может быть меньше 1")\r
        return\r
    } else if taskPriority > 3 {\r
        fmt.Println("Ошибка: приоритет не может быть больше 3")\r
        return\r
    } else {\r
        fmt.Println("Задача создана")\r
    }\r
\r
    for {\r
        fmt.Println()\r
        fmt.Println("===", appName, "===")\r
        fmt.Println("Версия:", version)\r
        fmt.Println()\r
        fmt.Println("1 — Показать задачу")\r
        fmt.Println("2 — Отметить задачу выполненной")\r
        fmt.Println("3 — Изменить приоритет")\r
        fmt.Println("4 — Проанализировать название задачи")\r
        fmt.Println("5 — Показать рекомендацию")\r
        fmt.Println("0 — Выйти")\r
        fmt.Println()\r
\r
        fmt.Print("Выберите действие: ")\r
        command, _ := reader.ReadString('\\n')\r
        command = strings.TrimSpace(command)\r
\r
        if command == "" {\r
            fmt.Println("Команда не введена")\r
            continue\r
        }\r
\r
        switch command {\r
        case "1":\r
            priorityText := ""\r
\r
            switch taskPriority {\r
            case 1:\r
                priorityText = "высокий"\r
            case 2:\r
                priorityText = "средний"\r
            case 3:\r
                priorityText = "низкий"\r
            default:\r
                priorityText = "неизвестный"\r
            }\r
\r
            statusText := ""\r
            if taskCompleted {\r
                statusText = "выполнена"\r
            } else {\r
                statusText = "не выполнена"\r
            }\r
\r
            fmt.Println()\r
            fmt.Printf("Задача #%d: %s\\n", taskID, taskTitle)\r
            fmt.Println("Статус:", statusText)\r
            fmt.Println("Приоритет:", priorityText)\r
            fmt.Printf("Плановое время: %.2f ч.\\n", taskHours)\r
\r
        case "2":\r
            if taskCompleted {\r
                fmt.Println("Задача уже была отмечена как выполненная")\r
            } else {\r
                taskCompleted = true\r
                fmt.Println("Задача отмечена как выполненная")\r
            }\r
\r
        case "3":\r
            fmt.Print("Введите новый приоритет (1-3): ")\r
            newPriorityInput, _ := reader.ReadString('\\n')\r
            newPriorityInput = strings.TrimSpace(newPriorityInput)\r
\r
            newPriority, err := strconv.Atoi(newPriorityInput)\r
            if err != nil {\r
                fmt.Println("Ошибка: приоритет должен быть целым числом")\r
                continue\r
            }\r
\r
            if newPriority < 1 || newPriority > 3 {\r
                fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
                continue\r
            }\r
\r
            taskPriority = newPriority\r
            fmt.Println("Приоритет обновлён")\r
\r
        case "4":\r
            fmt.Println("Первые пять непустых символов названия:")\r
\r
            shownCharacters := 0\r
\r
            for index, character := range taskTitle {\r
                if character == ' ' {\r
                    continue\r
                }\r
\r
                fmt.Printf("Позиция %d: %s\\n", index, string(character))\r
                shownCharacters++\r
\r
                if shownCharacters == 5 {\r
                    break\r
                }\r
            }\r
\r
            if shownCharacters == 0 {\r
                fmt.Println("В названии задачи нет символов для анализа")\r
            }\r
\r
        case "5":\r
            switch {\r
            case taskCompleted:\r
                fmt.Println("Задача уже выполнена. Можно переходить к следующей.")\r
            case taskPriority == 1 && taskHours <= 1:\r
                fmt.Println("Это важная и короткая задача. Хороший кандидат для быстрого старта.")\r
            case taskPriority == 1:\r
                fmt.Println("Это важная задача. Лучше запланировать её в первую очередь.")\r
            case taskHours > 4:\r
                fmt.Println("Задача большая. Возможно, её стоит разделить на части.")\r
            default:\r
                fmt.Println("Задача выглядит обычной. Выполните её по плану.")\r
            }\r
\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
Введите название задачи: Подготовить практику по Go\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
Задача создана\r
\r
=== Go To-Do Manager ===\r
Версия: 3\r
\r
1 — Показать задачу\r
2 — Отметить задачу выполненной\r
3 — Изменить приоритет\r
4 — Проанализировать название задачи\r
5 — Показать рекомендацию\r
0 — Выйти\r
\r
Выберите действие: 1\r
\r
Задача #1: Подготовить практику по Go\r
Статус: не выполнена\r
Приоритет: высокий\r
Плановое время: 1.50 ч.\r
\r
=== Go To-Do Manager ===\r
Версия: 3\r
\r
1 — Показать задачу\r
2 — Отметить задачу выполненной\r
3 — Изменить приоритет\r
4 — Проанализировать название задачи\r
5 — Показать рекомендацию\r
0 — Выйти\r
\r
Выберите действие: 5\r
Это важная задача. Лучше запланировать её в первую очередь.\r
\r
=== Go To-Do Manager ===\r
Версия: 3\r
\r
1 — Показать задачу\r
2 — Отметить задачу выполненной\r
3 — Изменить приоритет\r
4 — Проанализировать название задачи\r
5 — Показать рекомендацию\r
0 — Выйти\r
\r
Выберите действие: 0\r
Выход из программы\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии мини-проекта программа впервые работает не как линейный сценарий, а как интерактивное приложение.\r
\r
Цикл \`for\` позволяет показывать меню снова и снова. Пользователь может выбрать одно действие, затем вернуться к меню и выбрать другое. Без цикла программа завершилась бы сразу после первого действия.\r
\r
Оператор \`switch\` используется для обработки пунктов меню. Это удобнее, чем писать длинную цепочку \`if/else if\`, потому что каждая команда находится в отдельном \`case\`.\r
\r
Оператор \`if\` используется там, где нужно проверить состояние программы. Например, перед отметкой задачи выполненной программа проверяет, не выполнена ли она уже. При изменении приоритета программа проверяет, является ли введённое значение числом и попадает ли оно в допустимый диапазон.\r
\r
Цикл \`for ... range\` используется для перебора символов названия задачи. Это пока не самая важная функция To-Do приложения, но она хорошо показывает, как \`range\` работает со строкой.\r
\r
Операторы \`continue\` и \`break\` управляют ходом цикла. \`continue\` пропускает текущую итерацию, если пользователь не ввёл команду или ввёл некорректный приоритет. \`break\` используется при анализе названия задачи, чтобы остановить перебор после пяти символов.\r
\r
Отдельно важно заметить форму \`switch\` без значения. В пункте с рекомендацией программа проверяет несколько условий по порядку и выбирает первое подходящее. Это делает код похожим на цепочку \`if/else if\`, но часто читается аккуратнее.\r
\r
## Самостоятельное задание\r
\r
Добавьте в меню новый пункт:\r
\r
\`\`\`text\r
6 — Изменить время выполнения\r
\`\`\`\r
\r
Внутри этого пункта нужно:\r
\r
1. Запросить новое время выполнения.\r
2. Считать его как строку.\r
3. Преобразовать в \`float64\` через \`strconv.ParseFloat\`.\r
4. Проверить, что время больше \`0\`.\r
5. Если значение корректно, записать его в \`taskHours\`.\r
6. Если значение некорректно, вывести сообщение об ошибке и вернуться к меню.\r
\r
Пример заготовки:\r
\r
\`\`\`go\r
case "6":\r
    fmt.Print("Введите новое время выполнения: ")\r
    newHoursInput, _ := reader.ReadString('\\n')\r
    newHoursInput = strings.TrimSpace(newHoursInput)\r
\r
    newHours, err := strconv.ParseFloat(newHoursInput, 64)\r
    if err != nil {\r
        fmt.Println("Ошибка: время должно быть числом")\r
        continue\r
    }\r
\r
    if newHours <= 0 {\r
        fmt.Println("Ошибка: время должно быть больше 0")\r
        continue\r
    }\r
\r
    taskHours = newHours\r
    fmt.Println("Время выполнения обновлено")\r
\`\`\`\r
\r
Не забудьте добавить новый пункт в текст меню.\r
\r
## Вопросы для самопроверки\r
\r
1. Что такое поток выполнения программы?\r
2. Почему программа без условий выполняется только сверху вниз?\r
3. Для чего используется оператор \`if\`?\r
4. Чем отличается \`else\` от \`else if\`?\r
5. Почему в цепочке \`else if\` выполняется только первая подходящая ветка?\r
6. Что произойдёт, если условие в \`if\` равно \`false\`?\r
7. Для чего нужен цикл \`for\`?\r
8. Почему цикл \`for { ... }\` называется бесконечным?\r
9. Что делает \`continue\`?\r
10. Что делает \`break\`?\r
11. Почему в пункте анализа названия задачи пробелы пропускаются через \`continue\`?\r
12. Почему перебор символов останавливается через \`break\`?\r
13. Чем удобен \`switch\` при обработке меню?\r
14. Почему в Go не нужно писать \`break\` после каждого \`case\` в \`switch\`?\r
15. Для чего используется \`default\` в \`switch\`?\r
16. Чем отличается обычный \`switch command\` от \`switch\` без значения?\r
17. Почему команда \`0\` завершает программу через \`return\`?\r
18. Почему в этой версии приложения пока хранится только одна задача?\r
`,S=`# Практическая работа 2.6. To-Do список: несколько задач, срезы и карты\r
\r
## Тема\r
\r
Массивы, срезы, карты и работа со строками в Go.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В предыдущей версии программа уже получила меню и могла выполнять разные действия с одной задачей. Это был важный шаг: приложение стало интерактивным, но у него всё ещё было серьёзное ограничение — оно хранило только одну задачу.\r
\r
Теперь это ограничение будет снято. Программа научится хранить несколько задач, добавлять новые задачи, показывать список, отмечать задачи выполненными и удалять их.\r
\r
Для этого понадобятся коллекции:\r
\r
- массив — для фиксированного набора названий приоритетов;\r
- срез — для хранения списка идентификаторов задач;\r
- карты — для хранения данных задач по идентификатору;\r
- \`range\` — для перебора задач и символов строки;\r
- \`[]rune\` — для корректной работы с названием задачи на русском языке.\r
\r
В этой главе ещё не используются структуры. Поэтому данные задачи будут храниться не в одном объекте, а в нескольких картах: отдельно название, приоритет, время выполнения и статус. Это не самый удобный вариант для большого проекта, но он хорошо подходит для отработки срезов и карт. В следующих главах этот код можно будет улучшить с помощью функций, структур и методов.\r
\r
## Цель работы\r
\r
Цель практической работы — научиться использовать коллекции Go на примере развития To-Do приложения.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- создавать массив с фиксированным количеством элементов;\r
- обращаться к элементам массива по индексу;\r
- создавать срез и добавлять в него элементы через \`append\`;\r
- узнавать длину среза через \`len\`;\r
- удалять элемент из среза через операцию среза \`[start:end]\`;\r
- создавать карты \`map\`;\r
- добавлять, читать и удалять значения из карты;\r
- проверять наличие ключа через \`value, ok\`;\r
- перебирать срез через \`for ... range\`;\r
- понимать, что порядок перебора карты не гарантирован;\r
- корректно считать символы в строке через \`[]rune\`.\r
\r
## Что должно получиться\r
\r
После запуска программа должна показать меню:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
Версия: 4\r
\r
1 — Добавить задачу\r
2 — Показать все задачи\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать важные невыполненные задачи\r
6 — Проанализировать название задачи\r
0 — Выйти\r
\r
Выберите действие:\r
\`\`\`\r
\r
Пользователь сможет добавить несколько задач:\r
\r
\`\`\`text\r
Введите название задачи: Изучить срезы\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
Задача добавлена с номером 1\r
\`\`\`\r
\r
Затем добавить ещё одну:\r
\r
\`\`\`text\r
Введите название задачи: Разобрать карты\r
Введите приоритет задачи (1-3): 2\r
Введите примерное время выполнения в часах: 2\r
Задача добавлена с номером 2\r
\`\`\`\r
\r
При выводе списка программа должна показать обе задачи:\r
\r
\`\`\`text\r
Список задач:\r
\r
#1 Изучить срезы\r
Статус: не выполнена\r
Приоритет: высокий\r
Время: 1.50 ч.\r
\r
#2 Разобрать карты\r
Статус: не выполнена\r
Приоритет: средний\r
Время: 2.00 ч.\r
\r
Всего задач: 2\r
\`\`\`\r
\r
## Подготовка проекта\r
\r
Продолжайте работать с проектом из практики 2.5 или создайте новую папку:\r
\r
\`\`\`bash\r
todo-collections\r
\`\`\`\r
\r
Если создаёте новый проект, выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-collections\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импортируемые пакеты\r
\r
В этой практической работе снова понадобится чтение пользовательского ввода и преобразование строк в числа.\r
\r
Добавьте импорт:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
Пакет \`bufio\` нужен для чтения строк с пробелами. Пакет \`os\` используется для доступа к стандартному вводу. Пакет \`strings\` помогает убрать лишние пробелы и перенос строки. Пакет \`strconv\` нужен для преобразования строк в числа.\r
\r
## Задание 2. Добавить константы и массив приоритетов\r
\r
В начале функции \`main\` добавьте константы приложения:\r
\r
\`\`\`go\r
const appName = "Go To-Do Manager"\r
const version = 4\r
\`\`\`\r
\r
Теперь добавьте массив с названиями приоритетов:\r
\r
\`\`\`go\r
priorityNames := [3]string{"высокий", "средний", "низкий"}\r
\`\`\`\r
\r
Здесь используется именно массив, потому что количество уровней приоритета фиксировано. В этой версии программы их ровно три:\r
\r
\`\`\`text\r
1 — высокий\r
2 — средний\r
3 — низкий\r
\`\`\`\r
\r
Массив \`[3]string\` хорошо подходит для такого случая: размер известен заранее и не должен изменяться во время работы программы.\r
\r
Чтобы получить текст приоритета, можно использовать индекс:\r
\r
\`\`\`go\r
priorityText := priorityNames[taskPriority-1]\r
\`\`\`\r
\r
Почему используется \`taskPriority-1\`? Потому что пользователь вводит приоритеты от \`1\` до \`3\`, а индексы массива начинаются с \`0\`:\r
\r
\`\`\`text\r
priorityNames[0] → высокий\r
priorityNames[1] → средний\r
priorityNames[2] → низкий\r
\`\`\`\r
\r
Значит, для приоритета \`1\` нужен индекс \`0\`, для приоритета \`2\` — индекс \`1\`, для приоритета \`3\` — индекс \`2\`.\r
\r
---\r
\r
## Задание 3. Создать срез идентификаторов задач\r
\r
Теперь нужно хранить несколько задач. Начнём со среза, который будет хранить номера задач:\r
\r
\`\`\`go\r
taskIDs := []int{}\r
\`\`\`\r
\r
Это пустой срез целых чисел. Пока задач нет, его длина равна нулю. Когда пользователь добавит задачу, её номер будет добавлен в этот срез через \`append\`.\r
\r
Также добавьте переменную для следующего номера задачи:\r
\r
\`\`\`go\r
nextID := 1\r
\`\`\`\r
\r
Когда первая задача будет создана, она получит номер \`1\`. После этого \`nextID\` увеличится до \`2\`. Вторая задача получит номер \`2\`, и так далее.\r
\r
Такой подход нужен для того, чтобы у каждой задачи был постоянный номер. Даже если одну задачу удалить, номера остальных задач не должны неожиданно изменяться.\r
\r
## Задание 4. Создать карты для данных задач\r
\r
Теперь создадим карты. Каждая карта будет хранить одну характеристику задачи.\r
\r
Добавьте код:\r
\r
\`\`\`go\r
taskTitles := map[int]string{}\r
taskPriorities := map[int]int{}\r
taskHours := map[int]float64{}\r
taskCompleted := map[int]bool{}\r
\`\`\`\r
\r
Разберём эти карты.\r
\r
Карта:\r
\r
\`\`\`go\r
taskTitles := map[int]string{}\r
\`\`\`\r
\r
хранит название задачи. Ключ — номер задачи \`int\`, значение — название \`string\`.\r
\r
Карта:\r
\r
\`\`\`go\r
taskPriorities := map[int]int{}\r
\`\`\`\r
\r
хранит приоритет задачи. Ключ — номер задачи, значение — число от \`1\` до \`3\`.\r
\r
Карта:\r
\r
\`\`\`go\r
taskHours := map[int]float64{}\r
\`\`\`\r
\r
хранит примерное время выполнения.\r
\r
Карта:\r
\r
\`\`\`go\r
taskCompleted := map[int]bool{}\r
\`\`\`\r
\r
хранит статус выполнения: \`true\` или \`false\`.\r
\r
Такой способ хранения называется раздельным хранением данных. Он не идеален, потому что данные одной задачи распределены по нескольким картам. Но для этой главы это полезно: так можно отдельно увидеть, как работают карты разных типов.\r
\r
Позже, когда появятся структуры, данные задачи можно будет объединить в один тип \`Task\`.\r
\r
## Задание 5. Создать основной цикл меню\r
\r
Как и в предыдущей практике, приложение должно работать до тех пор, пока пользователь не выберет выход.\r
\r
Добавьте \`reader\`:\r
\r
\`\`\`go\r
reader := bufio.NewReader(os.Stdin)\r
\`\`\`\r
\r
Теперь создайте основной цикл:\r
\r
\`\`\`go\r
for {\r
    fmt.Println()\r
    fmt.Println("===", appName, "===")\r
    fmt.Println("Версия:", version)\r
    fmt.Println()\r
    fmt.Println("1 — Добавить задачу")\r
    fmt.Println("2 — Показать все задачи")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Удалить задачу")\r
    fmt.Println("5 — Показать важные невыполненные задачи")\r
    fmt.Println("6 — Проанализировать название задачи")\r
    fmt.Println("0 — Выйти")\r
    fmt.Println()\r
\r
    fmt.Print("Выберите действие: ")\r
    command, _ := reader.ReadString('\\n')\r
    command = strings.TrimSpace(command)\r
\r
    if command == "" {\r
        fmt.Println("Команда не введена")\r
        continue\r
    }\r
\r
    switch command {\r
    case "0":\r
        fmt.Println("Выход из программы")\r
        return\r
    default:\r
        fmt.Println("Неизвестная команда")\r
    }\r
}\r
\`\`\`\r
\r
На этом этапе меню уже работает, но большинство пунктов ещё не реализованы. Если пользователь введёт \`0\`, программа завершится. Если введёт любую другую команду, появится сообщение «Неизвестная команда».\r
\r
Дальше мы будем постепенно добавлять новые \`case\`.\r
\r
## Задание 6. Реализовать добавление задачи\r
\r
Внутри \`switch command\` добавьте пункт \`case "1"\`:\r
\r
\`\`\`go\r
case "1":\r
    fmt.Print("Введите название задачи: ")\r
    title, _ := reader.ReadString('\\n')\r
    title = strings.TrimSpace(title)\r
\r
    if title == "" {\r
        fmt.Println("Ошибка: название задачи не может быть пустым")\r
        continue\r
    }\r
\r
    fmt.Print("Введите приоритет задачи (1-3): ")\r
    priorityInput, _ := reader.ReadString('\\n')\r
    priorityInput = strings.TrimSpace(priorityInput)\r
\r
    priority, err := strconv.Atoi(priorityInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть целым числом")\r
        continue\r
    }\r
\r
    if priority < 1 || priority > len(priorityNames) {\r
        fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
        continue\r
    }\r
\r
    fmt.Print("Введите примерное время выполнения в часах: ")\r
    hoursInput, _ := reader.ReadString('\\n')\r
    hoursInput = strings.TrimSpace(hoursInput)\r
\r
    hours, err := strconv.ParseFloat(hoursInput, 64)\r
    if err != nil {\r
        fmt.Println("Ошибка: время должно быть числом")\r
        continue\r
    }\r
\r
    if hours <= 0 {\r
        fmt.Println("Ошибка: время должно быть больше 0")\r
        continue\r
    }\r
\r
    id := nextID\r
    nextID++\r
\r
    taskIDs = append(taskIDs, id)\r
    taskTitles[id] = title\r
    taskPriorities[id] = priority\r
    taskHours[id] = hours\r
    taskCompleted[id] = false\r
\r
    fmt.Println("Задача добавлена с номером", id)\r
\`\`\`\r
\r
Этот фрагмент делает несколько важных действий.\r
\r
Сначала программа считывает название, приоритет и время выполнения. Название остаётся строкой, а приоритет и время преобразуются в числовые типы.\r
\r
Затем создаётся новый идентификатор:\r
\r
\`\`\`go\r
id := nextID\r
nextID++\r
\`\`\`\r
\r
Переменная \`id\` получает текущий номер задачи. После этого \`nextID\` увеличивается, чтобы следующая задача получила новый номер.\r
\r
Самое важное происходит здесь:\r
\r
\`\`\`go\r
taskIDs = append(taskIDs, id)\r
taskTitles[id] = title\r
taskPriorities[id] = priority\r
taskHours[id] = hours\r
taskCompleted[id] = false\r
\`\`\`\r
\r
Сначала номер задачи добавляется в срез \`taskIDs\`. Срез нужен для того, чтобы сохранить порядок задач.\r
\r
Затем данные записываются в карты. В каждой карте используется один и тот же ключ \`id\`. Благодаря этому по номеру задачи можно найти все её характеристики.\r
\r
Например, если \`id\` равен \`1\`, то:\r
\r
\`\`\`go\r
taskTitles[1]\r
\`\`\`\r
\r
даст название первой задачи, а:\r
\r
\`\`\`go\r
taskPriorities[1]\r
\`\`\`\r
\r
даст её приоритет.\r
\r
## Задание 7. Реализовать вывод всех задач\r
\r
Теперь добавим пункт \`case "2"\` для вывода списка задач.\r
\r
\`\`\`go\r
case "2":\r
    if len(taskIDs) == 0 {\r
        fmt.Println("Список задач пуст")\r
        continue\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for _, id := range taskIDs {\r
        title, ok := taskTitles[id]\r
        if !ok {\r
            continue\r
        }\r
\r
        priority := taskPriorities[id]\r
        priorityText := priorityNames[priority-1]\r
\r
        statusText := "не выполнена"\r
        if taskCompleted[id] {\r
            statusText = "выполнена"\r
        }\r
\r
        fmt.Println()\r
        fmt.Printf("#%d %s\\n", id, title)\r
        fmt.Println("Статус:", statusText)\r
        fmt.Println("Приоритет:", priorityText)\r
        fmt.Printf("Время: %.2f ч.\\n", taskHours[id])\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Всего задач:", len(taskIDs))\r
\`\`\`\r
\r
Сначала программа проверяет длину среза:\r
\r
\`\`\`go\r
if len(taskIDs) == 0\r
\`\`\`\r
\r
Если длина равна нулю, значит задач нет.\r
\r
Затем используется цикл \`for ... range\`:\r
\r
\`\`\`go\r
for _, id := range taskIDs\r
\`\`\`\r
\r
Здесь индекс не нужен, поэтому используется пустой идентификатор \`_\`. На каждой итерации переменная \`id\` получает номер очередной задачи.\r
\r
После этого программа читает название из карты:\r
\r
\`\`\`go\r
title, ok := taskTitles[id]\r
\`\`\`\r
\r
Это синтаксис \`value, ok\`. Если ключ найден, \`ok\` будет равен \`true\`. Если ключа нет, \`ok\` будет равен \`false\`.\r
\r
В нормальной ситуации ключ должен существовать. Но проверка делает программу безопаснее: если в срезе по ошибке остался номер удалённой задачи, программа не аварийно завершится, а просто пропустит такой элемент.\r
\r
## Задание 8. Реализовать отметку задачи как выполненной\r
\r
Добавьте пункт \`case "3"\`:\r
\r
\`\`\`go\r
case "3":\r
    fmt.Print("Введите номер задачи: ")\r
    idInput, _ := reader.ReadString('\\n')\r
    idInput = strings.TrimSpace(idInput)\r
\r
    id, err := strconv.Atoi(idInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
        continue\r
    }\r
\r
    title, ok := taskTitles[id]\r
    if !ok {\r
        fmt.Println("Задача с таким номером не найдена")\r
        continue\r
    }\r
\r
    if taskCompleted[id] {\r
        fmt.Println("Задача уже была выполнена:", title)\r
        continue\r
    }\r
\r
    taskCompleted[id] = true\r
    fmt.Println("Задача отмечена как выполненная:", title)\r
\`\`\`\r
\r
Здесь карта используется для поиска задачи по номеру. Пользователь вводит номер, программа преобразует его в \`int\`, а затем проверяет, есть ли такой ключ в карте \`taskTitles\`.\r
\r
Если ключ есть, можно менять статус:\r
\r
\`\`\`go\r
taskCompleted[id] = true\r
\`\`\`\r
\r
Это обновляет значение в карте \`taskCompleted\` для конкретной задачи.\r
\r
## Задание 9. Реализовать удаление задачи\r
\r
Удаление задачи требует двух действий.\r
\r
Во-первых, нужно удалить данные из карт через \`delete\`.\r
\r
Во-вторых, нужно удалить номер задачи из среза \`taskIDs\`, иначе при выводе списка программа всё ещё будет пытаться показать удалённый номер.\r
\r
Добавьте пункт \`case "4"\`:\r
\r
\`\`\`go\r
case "4":\r
    fmt.Print("Введите номер задачи для удаления: ")\r
    idInput, _ := reader.ReadString('\\n')\r
    idInput = strings.TrimSpace(idInput)\r
\r
    id, err := strconv.Atoi(idInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
        continue\r
    }\r
\r
    title, ok := taskTitles[id]\r
    if !ok {\r
        fmt.Println("Задача с таким номером не найдена")\r
        continue\r
    }\r
\r
    delete(taskTitles, id)\r
    delete(taskPriorities, id)\r
    delete(taskHours, id)\r
    delete(taskCompleted, id)\r
\r
    indexToDelete := -1\r
\r
    for index, taskID := range taskIDs {\r
        if taskID == id {\r
            indexToDelete = index\r
            break\r
        }\r
    }\r
\r
    if indexToDelete != -1 {\r
        taskIDs = append(taskIDs[:indexToDelete], taskIDs[indexToDelete+1:]...)\r
    }\r
\r
    fmt.Println("Задача удалена:", title)\r
\`\`\`\r
\r
Функция \`delete\` удаляет значение из карты:\r
\r
\`\`\`go\r
delete(taskTitles, id)\r
\`\`\`\r
\r
Если ключа нет, ошибки не будет. Go просто ничего не сделает.\r
\r
Более интересная часть — удаление из среза:\r
\r
\`\`\`go\r
taskIDs = append(taskIDs[:indexToDelete], taskIDs[indexToDelete+1:]...)\r
\`\`\`\r
\r
Эта строка соединяет две части среза:\r
\r
\`\`\`go\r
taskIDs[:indexToDelete]\r
\`\`\`\r
\r
это все элементы до удаляемого, а:\r
\r
\`\`\`go\r
taskIDs[indexToDelete+1:]\r
\`\`\`\r
\r
это все элементы после удаляемого.\r
\r
Функция \`append\` соединяет эти части в новый срез. Три точки \`...\` нужны потому, что вторая часть — это тоже срез, а \`append\` ожидает отдельные элементы.\r
\r
Например, если был срез:\r
\r
\`\`\`go\r
[]int{1, 2, 3, 4}\r
\`\`\`\r
\r
и нужно удалить элемент \`2\`, получится:\r
\r
\`\`\`go\r
[]int{1, 3, 4}\r
\`\`\`\r
\r
## Задание 10. Показать важные невыполненные задачи\r
\r
Теперь добавим пункт \`case "5"\`. Он покажет только задачи с высоким приоритетом, которые ещё не выполнены.\r
\r
\`\`\`go\r
case "5":\r
    found := false\r
\r
    fmt.Println()\r
    fmt.Println("Важные невыполненные задачи:")\r
\r
    for _, id := range taskIDs {\r
        title, ok := taskTitles[id]\r
        if !ok {\r
            continue\r
        }\r
\r
        if taskPriorities[id] == 1 && !taskCompleted[id] {\r
            fmt.Printf("#%d %s — %.2f ч.\\n", id, title, taskHours[id])\r
            found = true\r
        }\r
    }\r
\r
    if !found {\r
        fmt.Println("Таких задач нет")\r
    }\r
\`\`\`\r
\r
Здесь используется фильтрация. Программа перебирает все номера задач, но выводит только те, которые подходят под условие:\r
\r
\`\`\`go\r
taskPriorities[id] == 1 && !taskCompleted[id]\r
\`\`\`\r
\r
Это значит: приоритет равен \`1\` и задача ещё не выполнена.\r
\r
Переменная \`found\` нужна для того, чтобы понять, был ли найден хотя бы один подходящий элемент. Если после цикла она осталась \`false\`, программа выводит сообщение:\r
\r
\`\`\`text\r
Таких задач нет\r
\`\`\`\r
\r
## Задание 11. Проанализировать название задачи через руны\r
\r
В главе 2.6 отдельно разбиралась важная особенность строк: индексация строки работает по байтам, а не по символам. Для русских букв это важно, потому что кириллический символ занимает больше одного байта.\r
\r
Добавим пункт \`case "6"\`, который покажет количество байтов и количество символов в названии задачи.\r
\r
\`\`\`go\r
case "6":\r
    fmt.Print("Введите номер задачи: ")\r
    idInput, _ := reader.ReadString('\\n')\r
    idInput = strings.TrimSpace(idInput)\r
\r
    id, err := strconv.Atoi(idInput)\r
    if err != nil {\r
        fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
        continue\r
    }\r
\r
    title, ok := taskTitles[id]\r
    if !ok {\r
        fmt.Println("Задача с таким номером не найдена")\r
        continue\r
    }\r
\r
    runes := []rune(title)\r
\r
    fmt.Println("Название:", title)\r
    fmt.Println("Количество байтов:", len(title))\r
    fmt.Println("Количество символов:", len(runes))\r
\r
    fmt.Println("Первые пять символов:")\r
\r
    for index, character := range runes {\r
        if index == 5 {\r
            break\r
        }\r
\r
        fmt.Printf("%d: %s\\n", index, string(character))\r
    }\r
\`\`\`\r
\r
Здесь важно сравнить две строки:\r
\r
\`\`\`go\r
len(title)\r
\`\`\`\r
\r
и:\r
\r
\`\`\`go\r
len(runes)\r
\`\`\`\r
\r
\`len(title)\` возвращает количество байтов. Для строки с русскими буквами это число может быть больше, чем количество видимых символов.\r
\r
\`[]rune(title)\` преобразует строку в срез рун. Руна соответствует одному Unicode-символу. Поэтому \`len(runes)\` показывает количество символов более привычным для пользователя способом.\r
\r
Например, для строки:\r
\r
\`\`\`text\r
Go практика\r
\`\`\`\r
\r
количество байтов и символов может отличаться, потому что русские буквы занимают больше одного байта.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
func main() {\r
    const appName = "Go To-Do Manager"\r
    const version = 4\r
\r
    priorityNames := [3]string{"высокий", "средний", "низкий"}\r
\r
    taskIDs := []int{}\r
    nextID := 1\r
\r
    taskTitles := map[int]string{}\r
    taskPriorities := map[int]int{}\r
    taskHours := map[int]float64{}\r
    taskCompleted := map[int]bool{}\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    for {\r
        fmt.Println()\r
        fmt.Println("===", appName, "===")\r
        fmt.Println("Версия:", version)\r
        fmt.Println()\r
        fmt.Println("1 — Добавить задачу")\r
        fmt.Println("2 — Показать все задачи")\r
        fmt.Println("3 — Отметить задачу выполненной")\r
        fmt.Println("4 — Удалить задачу")\r
        fmt.Println("5 — Показать важные невыполненные задачи")\r
        fmt.Println("6 — Проанализировать название задачи")\r
        fmt.Println("0 — Выйти")\r
        fmt.Println()\r
\r
        fmt.Print("Выберите действие: ")\r
        command, _ := reader.ReadString('\\n')\r
        command = strings.TrimSpace(command)\r
\r
        if command == "" {\r
            fmt.Println("Команда не введена")\r
            continue\r
        }\r
\r
        switch command {\r
        case "1":\r
            fmt.Print("Введите название задачи: ")\r
            title, _ := reader.ReadString('\\n')\r
            title = strings.TrimSpace(title)\r
\r
            if title == "" {\r
                fmt.Println("Ошибка: название задачи не может быть пустым")\r
                continue\r
            }\r
\r
            fmt.Print("Введите приоритет задачи (1-3): ")\r
            priorityInput, _ := reader.ReadString('\\n')\r
            priorityInput = strings.TrimSpace(priorityInput)\r
\r
            priority, err := strconv.Atoi(priorityInput)\r
            if err != nil {\r
                fmt.Println("Ошибка: приоритет должен быть целым числом")\r
                continue\r
            }\r
\r
            if priority < 1 || priority > len(priorityNames) {\r
                fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
                continue\r
            }\r
\r
            fmt.Print("Введите примерное время выполнения в часах: ")\r
            hoursInput, _ := reader.ReadString('\\n')\r
            hoursInput = strings.TrimSpace(hoursInput)\r
\r
            hours, err := strconv.ParseFloat(hoursInput, 64)\r
            if err != nil {\r
                fmt.Println("Ошибка: время должно быть числом")\r
                continue\r
            }\r
\r
            if hours <= 0 {\r
                fmt.Println("Ошибка: время должно быть больше 0")\r
                continue\r
            }\r
\r
            id := nextID\r
            nextID++\r
\r
            taskIDs = append(taskIDs, id)\r
            taskTitles[id] = title\r
            taskPriorities[id] = priority\r
            taskHours[id] = hours\r
            taskCompleted[id] = false\r
\r
            fmt.Println("Задача добавлена с номером", id)\r
\r
        case "2":\r
            if len(taskIDs) == 0 {\r
                fmt.Println("Список задач пуст")\r
                continue\r
            }\r
\r
            fmt.Println()\r
            fmt.Println("Список задач:")\r
\r
            for _, id := range taskIDs {\r
                title, ok := taskTitles[id]\r
                if !ok {\r
                    continue\r
                }\r
\r
                priority := taskPriorities[id]\r
                priorityText := priorityNames[priority-1]\r
\r
                statusText := "не выполнена"\r
                if taskCompleted[id] {\r
                    statusText = "выполнена"\r
                }\r
\r
                fmt.Println()\r
                fmt.Printf("#%d %s\\n", id, title)\r
                fmt.Println("Статус:", statusText)\r
                fmt.Println("Приоритет:", priorityText)\r
                fmt.Printf("Время: %.2f ч.\\n", taskHours[id])\r
            }\r
\r
            fmt.Println()\r
            fmt.Println("Всего задач:", len(taskIDs))\r
\r
        case "3":\r
            fmt.Print("Введите номер задачи: ")\r
            idInput, _ := reader.ReadString('\\n')\r
            idInput = strings.TrimSpace(idInput)\r
\r
            id, err := strconv.Atoi(idInput)\r
            if err != nil {\r
                fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
                continue\r
            }\r
\r
            title, ok := taskTitles[id]\r
            if !ok {\r
                fmt.Println("Задача с таким номером не найдена")\r
                continue\r
            }\r
\r
            if taskCompleted[id] {\r
                fmt.Println("Задача уже была выполнена:", title)\r
                continue\r
            }\r
\r
            taskCompleted[id] = true\r
            fmt.Println("Задача отмечена как выполненная:", title)\r
\r
        case "4":\r
            fmt.Print("Введите номер задачи для удаления: ")\r
            idInput, _ := reader.ReadString('\\n')\r
            idInput = strings.TrimSpace(idInput)\r
\r
            id, err := strconv.Atoi(idInput)\r
            if err != nil {\r
                fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
                continue\r
            }\r
\r
            title, ok := taskTitles[id]\r
            if !ok {\r
                fmt.Println("Задача с таким номером не найдена")\r
                continue\r
            }\r
\r
            delete(taskTitles, id)\r
            delete(taskPriorities, id)\r
            delete(taskHours, id)\r
            delete(taskCompleted, id)\r
\r
            indexToDelete := -1\r
\r
            for index, taskID := range taskIDs {\r
                if taskID == id {\r
                    indexToDelete = index\r
                    break\r
                }\r
            }\r
\r
            if indexToDelete != -1 {\r
                taskIDs = append(taskIDs[:indexToDelete], taskIDs[indexToDelete+1:]...)\r
            }\r
\r
            fmt.Println("Задача удалена:", title)\r
\r
        case "5":\r
            found := false\r
\r
            fmt.Println()\r
            fmt.Println("Важные невыполненные задачи:")\r
\r
            for _, id := range taskIDs {\r
                title, ok := taskTitles[id]\r
                if !ok {\r
                    continue\r
                }\r
\r
                if taskPriorities[id] == 1 && !taskCompleted[id] {\r
                    fmt.Printf("#%d %s — %.2f ч.\\n", id, title, taskHours[id])\r
                    found = true\r
                }\r
            }\r
\r
            if !found {\r
                fmt.Println("Таких задач нет")\r
            }\r
\r
        case "6":\r
            fmt.Print("Введите номер задачи: ")\r
            idInput, _ := reader.ReadString('\\n')\r
            idInput = strings.TrimSpace(idInput)\r
\r
            id, err := strconv.Atoi(idInput)\r
            if err != nil {\r
                fmt.Println("Ошибка: номер задачи должен быть целым числом")\r
                continue\r
            }\r
\r
            title, ok := taskTitles[id]\r
            if !ok {\r
                fmt.Println("Задача с таким номером не найдена")\r
                continue\r
            }\r
\r
            runes := []rune(title)\r
\r
            fmt.Println("Название:", title)\r
            fmt.Println("Количество байтов:", len(title))\r
            fmt.Println("Количество символов:", len(runes))\r
\r
            fmt.Println("Первые пять символов:")\r
\r
            for index, character := range runes {\r
                if index == 5 {\r
                    break\r
                }\r
\r
                fmt.Printf("%d: %s\\n", index, string(character))\r
            }\r
\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
Версия: 4\r
\r
1 — Добавить задачу\r
2 — Показать все задачи\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать важные невыполненные задачи\r
6 — Проанализировать название задачи\r
0 — Выйти\r
\r
Выберите действие: 1\r
Введите название задачи: Изучить срезы\r
Введите приоритет задачи (1-3): 1\r
Введите примерное время выполнения в часах: 1.5\r
Задача добавлена с номером 1\r
\r
=== Go To-Do Manager ===\r
Версия: 4\r
\r
1 — Добавить задачу\r
2 — Показать все задачи\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать важные невыполненные задачи\r
6 — Проанализировать название задачи\r
0 — Выйти\r
\r
Выберите действие: 1\r
Введите название задачи: Разобрать карты\r
Введите приоритет задачи (1-3): 2\r
Введите примерное время выполнения в часах: 2\r
Задача добавлена с номером 2\r
\r
=== Go To-Do Manager ===\r
Версия: 4\r
\r
1 — Добавить задачу\r
2 — Показать все задачи\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать важные невыполненные задачи\r
6 — Проанализировать название задачи\r
0 — Выйти\r
\r
Выберите действие: 2\r
\r
Список задач:\r
\r
#1 Изучить срезы\r
Статус: не выполнена\r
Приоритет: высокий\r
Время: 1.50 ч.\r
\r
#2 Разобрать карты\r
Статус: не выполнена\r
Приоритет: средний\r
Время: 2.00 ч.\r
\r
Всего задач: 2\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии To-Do приложения впервые появляется хранение нескольких задач.\r
\r
Массив \`priorityNames\` используется для фиксированного набора текстовых названий приоритетов. Его длина равна трём, и программа не добавляет в него новые элементы.\r
\r
Срез \`taskIDs\` хранит номера задач в порядке добавления. Это важно, потому что карты в Go не гарантируют порядок перебора. Если бы программа перебирала напрямую карту \`taskTitles\`, задачи могли бы выводиться в разном порядке при разных запусках. Срез решает эту проблему: он сохраняет порядок, в котором задачи были добавлены.\r
\r
Карты \`taskTitles\`, \`taskPriorities\`, \`taskHours\` и \`taskCompleted\` хранят данные задач. Во всех картах используется один и тот же ключ — номер задачи. Благодаря этому программа может по одному \`id\` получить все данные конкретной задачи.\r
\r
Добавление задачи показывает работу \`append\`: новый номер добавляется в срез \`taskIDs\`, а остальные данные записываются в карты.\r
\r
Вывод списка показывает работу \`range\`: программа перебирает срез идентификаторов и по каждому номеру получает данные из карт.\r
\r
Удаление задачи показывает сразу две операции: \`delete\` удаляет значения из карт, а операция среза \`taskIDs[:indexToDelete]\` и \`taskIDs[indexToDelete+1:]\` помогает убрать номер задачи из среза.\r
\r
Анализ названия задачи показывает разницу между байтами и символами. Для русских строк \`len(title)\` может вернуть число больше, чем визуальное количество символов. Поэтому для посимвольной работы используется преобразование в \`[]rune\`.\r
\r
## Самостоятельное задание\r
\r
Добавьте в меню пункт:\r
\r
\`\`\`text\r
7 — Показать краткую статистику\r
\`\`\`\r
\r
В этом пункте нужно вывести:\r
\r
- общее количество задач;\r
- количество выполненных задач;\r
- количество невыполненных задач;\r
- суммарное плановое время всех задач.\r
\r
Пример заготовки:\r
\r
\`\`\`go\r
case "7":\r
    completedCount := 0\r
    totalHours := 0.0\r
\r
    for _, id := range taskIDs {\r
        if _, ok := taskTitles[id]; !ok {\r
            continue\r
        }\r
\r
        if taskCompleted[id] {\r
            completedCount++\r
        }\r
\r
        totalHours += taskHours[id]\r
    }\r
\r
    activeCount := len(taskIDs) - completedCount\r
\r
    fmt.Println("Всего задач:", len(taskIDs))\r
    fmt.Println("Выполнено:", completedCount)\r
    fmt.Println("Не выполнено:", activeCount)\r
    fmt.Printf("Общее время: %.2f ч.\\n", totalHours)\r
\`\`\`\r
\r
Не забудьте добавить новый пункт в меню.\r
\r
## Вопросы для самопроверки\r
\r
1. Чем массив отличается от среза?\r
2. Почему \`priorityNames\` удобно хранить в массиве?\r
3. Почему список номеров задач хранится в срезе, а не в массиве?\r
4. Что делает функция \`append\`?\r
5. Почему результат \`append\` нужно присваивать обратно переменной?\r
6. Для чего используется \`len(taskIDs)\`?\r
7. Что хранит карта \`taskTitles\`?\r
8. Что означает тип \`map[int]string\`?\r
9. Почему данные задачи в этой версии распределены по нескольким картам?\r
10. Что делает функция \`delete\`?\r
11. Что означает запись \`title, ok := taskTitles[id]\`?\r
12. Почему нельзя отличить отсутствующий ключ от нулевого значения без \`ok\`?\r
13. Почему порядок перебора карты в Go не гарантирован?\r
14. Зачем нужен срез \`taskIDs\`, если уже есть карты с задачами?\r
15. Как работает удаление элемента из среза через \`append(taskIDs[:i], taskIDs[i+1:]...)\`?\r
16. Почему индексация строки может быть опасной для русских символов?\r
17. Чем \`len(title)\` отличается от \`len([]rune(title))\`?\r
18. Почему \`range\` удобно использовать для перебора среза?\r
19. Почему в этой версии проекта ещё не используются структуры?\r
20. Как можно будет улучшить хранение задач после изучения структур?\r
`,I=`# Практическая работа 2.7. To-Do список: разбиение программы на функции\r
\r
## Тема\r
\r
Определение и использование функций в Go: простые функции, параметры, возвращаемые значения, несколько возвращаемых значений, вариативные параметры, указатели в параметрах и \`defer\`.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В предыдущей версии программа уже могла работать со списком задач с помощью коллекций. Но по мере роста кода появляется новая проблема: всё приложение начинает собираться в одной функции \`main\`.\r
\r
Такой код сложно читать и изменять. Если меню, добавление задачи, вывод списка, отметка выполнения и удаление находятся в одном большом блоке, программа быстро превращается в длинную последовательность команд. В этой практической работе нужно привести приложение в более аккуратный вид: вынести отдельные действия в функции.\r
\r
После выполнения работы программа будет по-прежнему работать как консольный To-Do список, но её код станет более понятным:\r
\r
\`\`\`text\r
showMenu()\r
showTasks(tasks, completed)\r
addTask(&tasks, completed, reader)\r
completeTask(completed, reader)\r
deleteTask(&tasks, completed, reader)\r
\`\`\`\r
\r
На этом этапе мы ещё не используем структуры. Каждая задача пока хранится как строка в срезе, а статус выполнения хранится отдельно в карте. Структуры появятся позже и сделают хранение задачи более естественным.\r
\r
## Цель работы\r
\r
Цель практической работы — научиться разбивать программу на функции и применять функции для организации кода мини-проекта.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- создавать собственные функции;\r
- вызывать функции из \`main\`;\r
- передавать данные в функцию через параметры;\r
- возвращать результат из функции;\r
- возвращать несколько значений;\r
- использовать пустой идентификатор \`_\`, если часть результата не нужна;\r
- применять вариативные параметры;\r
- передавать указатель в функцию, если нужно изменить исходные данные;\r
- использовать \`defer\` для вывода завершающего сообщения.\r
\r
## Что должно получиться\r
\r
После запуска программа должна показать меню:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие:\r
\`\`\`\r
\r
Программа должна позволять:\r
\r
- просматривать список задач;\r
- добавлять новую задачу;\r
- отмечать задачу выполненной;\r
- удалять задачу;\r
- выводить статистику;\r
- завершать работу через пункт \`0\`.\r
\r
Пример работы программы:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие: 1\r
\r
Список задач:\r
1. [ ] Изучить функции\r
2. [ ] Разбить программу на части\r
3. [x] Повторить срезы\r
\r
Выберите действие: 5\r
\r
Всего задач: 3\r
Выполнено: 1\r
Осталось: 2\r
\`\`\`\r
\r
## Подготовка проекта\r
\r
Создайте новую папку для практической работы:\r
\r
\`\`\`bash\r
todo-functions\r
\`\`\`\r
\r
Перейдите в неё и выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-functions\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импорты и начальные данные\r
\r
Для работы с консольным вводом понадобятся пакеты \`bufio\`, \`os\` и \`strings\`. Для преобразования строки в число понадобится \`strconv\`.\r
\r
Добавьте импорты:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
В функции \`main\` создайте начальный список задач:\r
\r
\`\`\`go\r
func main() {\r
    defer fmt.Println("Программа завершена")\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []string{\r
        "Изучить функции",\r
        "Разбить программу на части",\r
        "Повторить срезы",\r
    }\r
\r
    completed := map[int]bool{\r
        2: true,\r
    }\r
\r
    fmt.Println(tasks)\r
    fmt.Println(completed)\r
}\r
\`\`\`\r
\r
Срез \`tasks\` хранит названия задач. Карта \`completed\` хранит статусы выполнения. Ключом является индекс задачи в срезе.\r
\r
Например:\r
\r
\`\`\`go\r
completed := map[int]bool{\r
    2: true,\r
}\r
\`\`\`\r
\r
означает, что задача с индексом \`2\` уже выполнена. При выводе пользователю индекс будет увеличиваться на единицу, потому что пользователю удобнее видеть номера с \`1\`, а не с \`0\`.\r
\r
Строка:\r
\r
\`\`\`go\r
defer fmt.Println("Программа завершена")\r
\`\`\`\r
\r
показывает работу \`defer\`. Этот вывод появится не сразу, а только перед завершением функции \`main\`.\r
\r
## Задание 2. Создать функцию для вывода меню\r
\r
Меню приложения будет показываться много раз, поэтому его удобно вынести в отдельную функцию.\r
\r
Добавьте функцию выше \`main\`:\r
\r
\`\`\`go\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Удалить задачу")\r
    fmt.Println("5 — Показать статистику")\r
    fmt.Println("0 — Выйти")\r
}\r
\`\`\`\r
\r
Функция \`showMenu\` не принимает параметры и не возвращает результат. Её задача простая: вывести текст меню в консоль.\r
\r
Теперь в \`main\` можно вызвать её:\r
\r
\`\`\`go\r
showMenu()\r
\`\`\`\r
\r
Функция выполняется только в момент вызова. Само объявление функции не запускает код автоматически. Это важная мысль: функция — это не действие, а описание действия. Действием она становится только при вызове.\r
\r
## Задание 3. Создать функцию для чтения строки\r
\r
В программе несколько раз нужно будет читать строку из консоли и удалять символ переноса строки. Чтобы не повторять один и тот же код, создадим отдельную функцию.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\`\`\`\r
\r
Эта функция принимает параметр:\r
\r
\`\`\`go\r
reader *bufio.Reader\r
\`\`\`\r
\r
Тип \`*bufio.Reader\` означает указатель на объект чтения. В этой практике не нужно создавать новый \`reader\` в каждой функции. Достаточно создать его один раз в \`main\` и передавать в функции, которым нужен ввод.\r
\r
Функция возвращает строку:\r
\r
\`\`\`go\r
func readLine(reader *bufio.Reader) string\r
\`\`\`\r
\r
Внутри она читает текст до нажатия Enter, убирает лишние пробелы и возвращает готовое значение.\r
\r
Теперь вместо повторения такого кода:\r
\r
\`\`\`go\r
command, _ := reader.ReadString('\\n')\r
command = strings.TrimSpace(command)\r
\`\`\`\r
\r
можно писать короче:\r
\r
\`\`\`go\r
command := readLine(reader)\r
\`\`\`\r
\r
Это и есть одно из главных преимуществ функций: повторяющийся код появляется в одном месте.\r
\r
## Задание 4. Создать функцию для чтения номера задачи\r
\r
Во многих пунктах меню пользователю нужно будет ввести номер задачи. Внутри программы задачи хранятся по индексам с нуля, а пользователь вводит номера с единицы. Поэтому нужна функция, которая преобразует пользовательский номер в индекс.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func readTaskIndex(reader *bufio.Reader, tasks []string) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\`\`\`\r
\r
Эта функция возвращает два значения:\r
\r
\`\`\`go\r
(int, bool)\r
\`\`\`\r
\r
Первое значение — индекс задачи. Второе значение — признак успешности. Если пользователь ввёл корректный номер, функция возвращает индекс и \`true\`. Если ввод некорректный, функция возвращает \`0\` и \`false\`.\r
\r
Такой подход похож на паттерн \`result, err\`, который часто встречается в Go. Здесь вместо ошибки используется \`bool\`, потому что нам достаточно знать, получилось ли получить корректный индекс.\r
\r
Позже функция будет использоваться так:\r
\r
\`\`\`go\r
index, ok := readTaskIndex(reader, tasks)\r
if !ok {\r
    continue\r
}\r
\`\`\`\r
\r
Если \`ok\` равен \`false\`, действие не выполняется, и программа возвращается к меню.\r
\r
## Задание 5. Создать функцию для вывода задач\r
\r
Теперь вынесем вывод списка задач в отдельную функцию.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func showTasks(tasks []string, completed map[int]bool) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        mark := "[ ]"\r
\r
        if completed[index] {\r
            mark = "[x]"\r
        }\r
\r
        fmt.Printf("%d. %s %s\\n", index+1, mark, task)\r
    }\r
}\r
\`\`\`\r
\r
Функция принимает два параметра:\r
\r
\`\`\`go\r
tasks []string\r
completed map[int]bool\r
\`\`\`\r
\r
Срез \`tasks\` нужен для названий задач. Карта \`completed\` нужна для определения статуса.\r
\r
Если задач нет, функция выводит сообщение и завершает работу через \`return\`. Это пример раннего выхода из функции. Такой подход часто делает код проще: сначала обрабатывается особый случай, а затем идёт основная логика.\r
\r
В цикле \`range\` программа перебирает все задачи. Для каждой задачи создаётся переменная \`mark\`. По умолчанию она равна \`[ ]\`. Если в карте \`completed\` для текущего индекса хранится \`true\`, метка меняется на \`[x]\`.\r
\r
## Задание 6. Создать функцию для добавления задачи\r
\r
Теперь создадим функцию, которая будет добавлять новую задачу в срез.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func addTask(tasks *[]string, reader *bufio.Reader) {\r
    fmt.Print("Введите новую задачу: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    *tasks = append(*tasks, title)\r
    fmt.Println("Задача добавлена")\r
}\r
\`\`\`\r
\r
Обратите внимание на параметр:\r
\r
\`\`\`go\r
tasks *[]string\r
\`\`\`\r
\r
Это указатель на срез строк. Он нужен потому, что функция должна изменить исходный срез из \`main\`.\r
\r
Если бы параметр был таким:\r
\r
\`\`\`go\r
func addTask(tasks []string, reader *bufio.Reader)\r
\`\`\`\r
\r
то внутри функции можно было бы вызвать \`append\`, но изменение не обязательно сохранилось бы в переменной \`tasks\` из \`main\`. Поэтому для учебной практики мы явно передаём указатель на срез и изменяем исходное значение:\r
\r
\`\`\`go\r
*tasks = append(*tasks, title)\r
\`\`\`\r
\r
Символ \`*\` перед \`tasks\` означает: перейти по адресу и изменить тот срез, который находится в \`main\`.\r
\r
Вызов такой функции будет выглядеть так:\r
\r
\`\`\`go\r
addTask(&tasks, reader)\r
\`\`\`\r
\r
Символ \`&\` перед \`tasks\` означает: передать адрес переменной.\r
\r
## Задание 7. Создать функцию для отметки задачи выполненной\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func completeTask(tasks []string, completed map[int]bool, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if completed[index] {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    completed[index] = true\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\`\`\`\r
\r
Здесь функция вызывает другую функцию:\r
\r
\`\`\`go\r
index, ok := readTaskIndex(reader, tasks)\r
\`\`\`\r
\r
Это нормальная практика: функции могут вызывать другие функции. Благодаря этому код становится проще. \`completeTask\` не нужно знать все детали чтения и проверки номера задачи. Этим занимается \`readTaskIndex\`.\r
\r
Карта \`completed\` передаётся без указателя. В Go карты уже ведут себя как ссылочный тип: если функция меняет значение внутри карты, изменение будет видно и снаружи.\r
\r
## Задание 8. Создать функцию для удаления задачи\r
\r
Удаление задачи сложнее, чем добавление или отметка выполнения. Нужно удалить элемент из среза и обновить карту выполненных задач, потому что после удаления индексы задач изменятся.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func deleteTask(tasks *[]string, completed map[int]bool, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index]\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    newCompleted := map[int]bool{}\r
\r
    for oldIndex, done := range completed {\r
        if oldIndex < index {\r
            newCompleted[oldIndex] = done\r
        } else if oldIndex > index {\r
            newCompleted[oldIndex-1] = done\r
        }\r
    }\r
\r
    for key := range completed {\r
        delete(completed, key)\r
    }\r
\r
    for key, value := range newCompleted {\r
        completed[key] = value\r
    }\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\`\`\`\r
\r
Разберём важные части.\r
\r
Строка:\r
\r
\`\`\`go\r
*tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\`\`\`\r
\r
создаёт новый срез без удаляемого элемента. Первая часть \`(*tasks)[:index]\` берёт все элементы до удаляемого. Вторая часть \`(*tasks)[index+1:]\` берёт все элементы после него. Многоточие \`...\` распаковывает второй срез, чтобы \`append\` добавил его элементы по одному.\r
\r
После удаления индексы задач сдвигаются. Например, если удалить задачу с индексом \`1\`, задача с индексом \`2\` станет задачей с индексом \`1\`. Поэтому карту \`completed\` нужно пересобрать.\r
\r
Сначала создаётся новая карта:\r
\r
\`\`\`go\r
newCompleted := map[int]bool{}\r
\`\`\`\r
\r
Затем все старые статусы переносятся с учётом сдвига индексов. Статус удалённой задачи не переносится.\r
\r
После этого старая карта очищается через \`delete\`, а затем в неё записываются новые значения. Это нужно потому, что карта передана в функцию как ссылочный тип, и мы хотим изменить именно исходную карту.\r
\r
## Задание 9. Создать функцию для статистики\r
\r
Теперь добавим функцию, которая возвращает несколько значений.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func calculateStats(tasks []string, completed map[int]bool) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for index := range tasks {\r
        if completed[index] {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\`\`\`\r
\r
Функция возвращает три значения:\r
\r
\`\`\`go\r
(int, int, int)\r
\`\`\`\r
\r
Это общее количество задач, количество выполненных и количество оставшихся.\r
\r
Теперь создайте функцию для вывода статистики:\r
\r
\`\`\`go\r
func showStats(tasks []string, completed map[int]bool) {\r
    total, done, remaining := calculateStats(tasks, completed)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\`\`\`\r
\r
Здесь одна функция использует результат другой функции. Это обычный способ строить программу: одна функция считает данные, другая отвечает за вывод.\r
\r
## Задание 10. Создать функцию с вариативными параметрами\r
\r
Добавим небольшую служебную функцию для вывода сообщений.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func printInfo(messages ...string) {\r
    for _, message := range messages {\r
        fmt.Println(message)\r
    }\r
}\r
\`\`\`\r
\r
Параметр:\r
\r
\`\`\`go\r
messages ...string\r
\`\`\`\r
\r
означает, что функция может принять любое количество строк.\r
\r
Теперь её можно вызывать так:\r
\r
\`\`\`go\r
printInfo("Добро пожаловать в Go To-Do Manager")\r
\`\`\`\r
\r
или так:\r
\r
\`\`\`go\r
printInfo("Задача добавлена", "Вернитесь в меню для выбора следующего действия")\r
\`\`\`\r
\r
Внутри функции \`messages\` работает как обычный срез строк. Поэтому его можно перебирать через \`range\`.\r
\r
В итоговой программе мы используем эту функцию для приветствия. Она не является обязательной для логики To-Do, но хорошо показывает, как работают вариативные параметры.\r
\r
## Задание 11. Собрать программу в main\r
\r
Теперь функция \`main\` должна стать короче. В ней останутся начальные данные, основной цикл и вызовы функций.\r
\r
Добавьте основной цикл:\r
\r
\`\`\`go\r
for {\r
    showMenu()\r
\r
    fmt.Print("Выберите действие: ")\r
    command := readLine(reader)\r
\r
    switch command {\r
    case "1":\r
        showTasks(tasks, completed)\r
    case "2":\r
        addTask(&tasks, reader)\r
    case "3":\r
        completeTask(tasks, completed, reader)\r
    case "4":\r
        deleteTask(&tasks, completed, reader)\r
    case "5":\r
        showStats(tasks, completed)\r
    case "0":\r
        fmt.Println("Выход из программы")\r
        return\r
    default:\r
        fmt.Println("Неизвестная команда")\r
    }\r
}\r
\`\`\`\r
\r
Теперь \`main\` не содержит подробностей добавления, удаления или вывода задач. Она только управляет порядком действий.\r
\r
Это хороший признак: \`main\` становится центром управления программой, а конкретная работа распределяется по отдельным функциям.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Удалить задачу")\r
    fmt.Println("5 — Показать статистику")\r
    fmt.Println("0 — Выйти")\r
}\r
\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\r
func readTaskIndex(reader *bufio.Reader, tasks []string) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\r
func showTasks(tasks []string, completed map[int]bool) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        mark := "[ ]"\r
\r
        if completed[index] {\r
            mark = "[x]"\r
        }\r
\r
        fmt.Printf("%d. %s %s\\n", index+1, mark, task)\r
    }\r
}\r
\r
func addTask(tasks *[]string, reader *bufio.Reader) {\r
    fmt.Print("Введите новую задачу: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    *tasks = append(*tasks, title)\r
    fmt.Println("Задача добавлена")\r
}\r
\r
func completeTask(tasks []string, completed map[int]bool, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if completed[index] {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    completed[index] = true\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\r
func deleteTask(tasks *[]string, completed map[int]bool, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index]\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    newCompleted := map[int]bool{}\r
\r
    for oldIndex, done := range completed {\r
        if oldIndex < index {\r
            newCompleted[oldIndex] = done\r
        } else if oldIndex > index {\r
            newCompleted[oldIndex-1] = done\r
        }\r
    }\r
\r
    for key := range completed {\r
        delete(completed, key)\r
    }\r
\r
    for key, value := range newCompleted {\r
        completed[key] = value\r
    }\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\r
func calculateStats(tasks []string, completed map[int]bool) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for index := range tasks {\r
        if completed[index] {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\r
func showStats(tasks []string, completed map[int]bool) {\r
    total, done, remaining := calculateStats(tasks, completed)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\r
func printInfo(messages ...string) {\r
    for _, message := range messages {\r
        fmt.Println(message)\r
    }\r
}\r
\r
func main() {\r
    defer fmt.Println("Программа завершена")\r
\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []string{\r
        "Изучить функции",\r
        "Разбить программу на части",\r
        "Повторить срезы",\r
    }\r
\r
    completed := map[int]bool{\r
        2: true,\r
    }\r
\r
    printInfo("Добро пожаловать в Go To-Do Manager")\r
\r
    for {\r
        showMenu()\r
\r
        fmt.Print("Выберите действие: ")\r
        command := readLine(reader)\r
\r
        switch command {\r
        case "1":\r
            showTasks(tasks, completed)\r
        case "2":\r
            addTask(&tasks, reader)\r
        case "3":\r
            completeTask(tasks, completed, reader)\r
        case "4":\r
            deleteTask(&tasks, completed, reader)\r
        case "5":\r
            showStats(tasks, completed)\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
Добро пожаловать в Go To-Do Manager\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие: 1\r
\r
Список задач:\r
1. [ ] Изучить функции\r
2. [ ] Разбить программу на части\r
3. [x] Повторить срезы\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие: 2\r
Введите новую задачу: Добавить функции в проект\r
Задача добавлена\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие: 5\r
\r
Статистика:\r
Всего задач: 4\r
Выполнено: 1\r
Осталось: 3\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Удалить задачу\r
5 — Показать статистику\r
0 — Выйти\r
Выберите действие: 0\r
Выход из программы\r
Программа завершена\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии мини-проекта программа стала более организованной. Большая часть логики вынесена из \`main\` в отдельные функции.\r
\r
Функция \`showMenu\` отвечает только за вывод меню. Функция \`readLine\` отвечает только за чтение строки. Функция \`showTasks\` отвечает за отображение списка задач. Функция \`addTask\` добавляет новую задачу. Функция \`completeTask\` меняет статус задачи. Функция \`deleteTask\` удаляет задачу и обновляет карту выполненных задач. Функция \`calculateStats\` считает статистику, а \`showStats\` выводит её пользователю.\r
\r
Такое разделение делает программу понятнее. Если нужно изменить вывод меню, достаточно открыть \`showMenu\`. Если нужно изменить логику добавления задачи, нужно искать \`addTask\`. Код больше не выглядит как один длинный сценарий.\r
\r
В этой практике особенно важно заметить разницу между функциями, которые просто выполняют действие, и функциями, которые возвращают результат.\r
\r
Например, \`showMenu\` ничего не возвращает. Она просто печатает текст.\r
\r
Функция \`readLine\` возвращает строку. Её результат нужен вызывающему коду.\r
\r
Функция \`readTaskIndex\` возвращает два значения: индекс и признак успешности. Это удобно, потому что вместе с результатом функция сообщает, можно ли этим результатом пользоваться.\r
\r
Функция \`calculateStats\` возвращает сразу три значения. Это показывает одну из сильных сторон Go: несколько связанных результатов можно вернуть напрямую, без создания дополнительных объектов.\r
\r
Также в программе используется указатель на срез:\r
\r
\`\`\`go\r
func addTask(tasks *[]string, reader *bufio.Reader)\r
\`\`\`\r
\r
Он нужен, потому что функция должна изменить исходный срез из \`main\`. При вызове передаётся адрес:\r
\r
\`\`\`go\r
addTask(&tasks, reader)\r
\`\`\`\r
\r
А внутри функции используется разыменование:\r
\r
\`\`\`go\r
*tasks = append(*tasks, title)\r
\`\`\`\r
\r
Это связывает новую тему с главой об указателях.\r
\r
## Самостоятельное задание\r
\r
Добавьте функцию для очистки всех выполненных задач.\r
\r
Функция должна называться:\r
\r
\`\`\`go\r
func clearCompletedTasks(tasks *[]string, completed map[int]bool)\r
\`\`\`\r
\r
Она должна удалить из списка все задачи, у которых статус выполнения равен \`true\`.\r
\r
Подсказка: удобнее создать новый срез и новую карту, а затем перенести туда только невыполненные задачи.\r
\r
Пример общей идеи:\r
\r
\`\`\`go\r
newTasks := []string{}\r
newCompleted := map[int]bool{}\r
\r
for index, task := range *tasks {\r
    if completed[index] {\r
        continue\r
    }\r
\r
    newIndex := len(newTasks)\r
    newTasks = append(newTasks, task)\r
\r
    if completed[index] {\r
        newCompleted[newIndex] = true\r
    }\r
}\r
\r
*tasks = newTasks\r
\`\`\`\r
\r
После этого нужно очистить старую карту \`completed\` и перенести в неё значения из \`newCompleted\`.\r
\r
Добавьте новый пункт меню:\r
\r
\`\`\`text\r
6 — Удалить выполненные задачи\r
\`\`\`\r
\r
И вызовите функцию из \`switch\`.\r
\r
## Вопросы для самопроверки\r
\r
1. Зачем нужны функции?\r
2. Почему большой код внутри \`main\` становится неудобным?\r
3. Что означает ключевое слово \`func\`?\r
4. Чем объявление функции отличается от вызова функции?\r
5. Что такое параметр функции?\r
6. Что такое аргумент функции?\r
7. Почему порядок аргументов при вызове функции важен?\r
8. Какая функция в программе не принимает параметры и не возвращает значения?\r
9. Какая функция возвращает строку?\r
10. Какая функция возвращает два значения?\r
11. Зачем функция \`readTaskIndex\` возвращает \`bool\`?\r
12. Какая функция возвращает три значения?\r
13. Для чего используется пустой идентификатор \`_\`?\r
14. Что означает параметр \`messages ...string\`?\r
15. Почему \`addTask\` принимает \`*[]string\`, а не просто \`[]string\`?\r
16. Почему карту \`completed\` можно изменять без указателя?\r
17. Для чего используется \`defer\` в функции \`main\`?\r
18. Почему после разбиения на функции программа становится проще для сопровождения?\r
`,D=`# Практическая работа 2.8. To-Do список: переход к структурам\r
\r
## Тема\r
\r
Структуры в Go: объявление собственного типа, создание значений структуры, доступ к полям, срез структур, передача структур в функции и указатели на структуры.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В предыдущих версиях задачи хранились не очень удобно: отдельно был срез названий задач, а отдельно — карта статусов выполнения. Такой подход работает, но он плохо масштабируется.\r
\r
Например, если у задачи появится приоритет, дедлайн или описание, придётся создавать новые срезы или карты и следить, чтобы все они совпадали по индексам. Это неудобно и опасно: легко удалить задачу из одного среза, забыть обновить другую коллекцию и получить несогласованные данные.\r
\r
В этой версии проекта задача станет самостоятельной сущностью. Для этого будет создана структура \`Task\`, внутри которой будут храниться связанные данные:\r
\r
- название задачи;\r
- статус выполнения;\r
- приоритет.\r
\r
Теперь в программе будет не несколько несвязанных коллекций, а один срез задач:\r
\r
\`\`\`go\r
tasks := []Task{}\r
\`\`\`\r
\r
Каждый элемент этого среза — полноценная задача со всеми нужными полями.\r
\r
## Цель работы\r
\r
Цель практической работы — научиться использовать структуры для объединения связанных данных в мини-проекте.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- объявлять собственный тип структуры через \`type\` и \`struct\`;\r
- создавать значения структуры с заполнением полей;\r
- читать и изменять поля структуры через точку;\r
- использовать нулевые значения полей структуры;\r
- хранить несколько структур в срезе;\r
- передавать структуру в функцию;\r
- понимать, что структура копируется при передаче по значению;\r
- передавать указатель на структуру, если функция должна изменить оригинал;\r
- использовать структуры для упрощения кода приложения.\r
\r
## Что должно получиться\r
\r
После запуска программа должна показать меню:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
0 — Выйти\r
Выберите действие:\r
\`\`\`\r
\r
Программа должна позволять:\r
\r
- просматривать список задач;\r
- добавлять новую задачу с приоритетом;\r
- отмечать задачу выполненной;\r
- изменять приоритет задачи;\r
- удалять задачу;\r
- выводить статистику.\r
\r
Главное отличие от предыдущей версии: каждая задача теперь хранится не как отдельная строка, а как значение структуры \`Task\`.\r
\r
## Подготовка проекта\r
\r
Создайте новую папку для практической работы:\r
\r
\`\`\`bash\r
todo-structs\r
\`\`\`\r
\r
Перейдите в неё и выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-structs\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импорты\r
\r
В этой версии приложения снова понадобится работа с консольным вводом и преобразованием строк в числа.\r
\r
Добавьте импорты:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
Пакет \`bufio\` нужен для чтения пользовательского ввода. Пакет \`os\` используется для доступа к стандартному вводу. Пакет \`strings\` помогает убирать лишние пробелы и перенос строки. Пакет \`strconv\` нужен для преобразования введённого приоритета из строки в число.\r
\r
## Задание 2. Объявить структуру Task\r
\r
Выше функции \`main\` объявите структуру \`Task\`:\r
\r
\`\`\`go\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\`\`\`\r
\r
Это новый тип данных, созданный специально для нашего приложения.\r
\r
Поле \`Title\` хранит название задачи. Оно имеет тип \`string\`.\r
\r
Поле \`Done\` хранит статус выполнения. Оно имеет тип \`bool\`.\r
\r
Поле \`Priority\` хранит приоритет задачи. Оно имеет тип \`int\`.\r
\r
Имена полей начинаются с заглавной буквы. В небольшом приложении из одного файла это не обязательно, но в Go такой стиль часто используется для полей структур, которые могут быть доступны из других пакетов. Кроме того, такие имена легче визуально отличать от локальных переменных.\r
\r
Важно понимать: объявление структуры ещё не создаёт задачу. Это только описание формы данных. Настоящие задачи появятся позже, когда мы создадим значения типа \`Task\`.\r
\r
## Задание 3. Создать начальный срез задач\r
\r
В функции \`main\` создайте \`reader\` и срез задач:\r
\r
\`\`\`go\r
func main() {\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []Task{\r
        {\r
            Title:    "Изучить структуры",\r
            Done:     false,\r
            Priority: 1,\r
        },\r
        {\r
            Title:    "Переписать To-Do проект",\r
            Done:     false,\r
            Priority: 2,\r
        },\r
        {\r
            Title:    "Повторить функции",\r
            Done:     true,\r
            Priority: 3,\r
        },\r
    }\r
\r
    fmt.Println(tasks)\r
}\r
\`\`\`\r
\r
Здесь создаётся срез \`[]Task\`, то есть срез задач. Внутри него находятся три значения структуры.\r
\r
Каждая задача содержит сразу все связанные данные: название, статус и приоритет. Теперь не нужно отдельно хранить срез названий и карту статусов. Всё находится внутри одного объекта.\r
\r
Запустите программу. Вы увидите примерно такой вывод:\r
\r
\`\`\`text\r
[{Изучить структуры false 1} {Переписать To-Do проект false 2} {Повторить функции true 3}]\r
\`\`\`\r
\r
Такой вывод не очень красивый для пользователя, но он показывает, что данные действительно созданы. Позже мы сделаем отдельную функцию для аккуратного вывода задач.\r
\r
## Задание 4. Создать функцию showMenu\r
\r
Меню будет выводиться много раз, поэтому его удобно оставить отдельной функцией.\r
\r
Добавьте функцию выше \`main\`:\r
\r
\`\`\`go\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Изменить приоритет")\r
    fmt.Println("5 — Удалить задачу")\r
    fmt.Println("6 — Показать статистику")\r
    fmt.Println("0 — Выйти")\r
}\r
\`\`\`\r
\r
Эта функция не принимает параметры и не возвращает значение. Она просто выводит меню.\r
\r
## Задание 5. Создать функцию readLine\r
\r
Чтобы не повторять чтение строки в разных местах программы, создайте функцию:\r
\r
\`\`\`go\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\`\`\`\r
\r
Функция получает \`reader\`, читает строку до нажатия Enter, убирает лишние пробелы и возвращает готовую строку.\r
\r
Параметр имеет тип:\r
\r
\`\`\`go\r
reader *bufio.Reader\r
\`\`\`\r
\r
Это указатель на объект чтения. Мы создаём \`reader\` один раз в \`main\` и передаём его в функции, которым нужен ввод.\r
\r
## Задание 6. Создать функцию для текста приоритета\r
\r
Приоритет хранится как число, но пользователю удобнее видеть текст: высокий, средний или низкий.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func getPriorityText(priority int) string {\r
    switch priority {\r
    case 1:\r
        return "высокий"\r
    case 2:\r
        return "средний"\r
    case 3:\r
        return "низкий"\r
    default:\r
        return "неизвестный"\r
    }\r
}\r
\`\`\`\r
\r
Функция принимает число и возвращает строку.\r
\r
Это хороший пример функции с параметром и возвращаемым значением. Она ничего не выводит сама, а только преобразует данные. Такой подход делает функцию более универсальной: её результат можно вывести, сохранить или использовать в другой строке.\r
\r
## Задание 7. Создать функцию для вывода одной задачи\r
\r
Теперь создадим функцию, которая получает одну структуру \`Task\` и выводит её в понятном виде.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func printTask(index int, task Task) {\r
    status := "[ ]"\r
\r
    if task.Done {\r
        status = "[x]"\r
    }\r
\r
    fmt.Printf("%d. %s %s — приоритет: %s\\n",\r
        index+1,\r
        status,\r
        task.Title,\r
        getPriorityText(task.Priority),\r
    )\r
}\r
\`\`\`\r
\r
Функция принимает два параметра:\r
\r
\`\`\`go\r
index int\r
task Task\r
\`\`\`\r
\r
\`index\` нужен для вывода номера задачи. Внутри программы индексы начинаются с \`0\`, но пользователю удобнее видеть номера с \`1\`, поэтому при выводе используется \`index+1\`.\r
\r
\`task Task\` — это структура задачи. Через точку можно обратиться к её полям:\r
\r
\`\`\`go\r
task.Done\r
task.Title\r
task.Priority\r
\`\`\`\r
\r
Здесь важно помнить: при передаче в функцию структура копируется. Функция \`printTask\` только читает поля и ничего не меняет, поэтому передача по значению здесь подходит.\r
\r
## Задание 8. Создать функцию для вывода списка задач\r
\r
Теперь создайте функцию, которая выводит весь срез задач:\r
\r
\`\`\`go\r
func showTasks(tasks []Task) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        printTask(index, task)\r
    }\r
}\r
\`\`\`\r
\r
Функция принимает срез структур:\r
\r
\`\`\`go\r
tasks []Task\r
\`\`\`\r
\r
Если срез пустой, функция выводит сообщение и завершает работу через \`return\`.\r
\r
Если задачи есть, она перебирает их через \`for ... range\`. На каждой итерации переменная \`task\` содержит очередную задачу. Затем функция вызывает \`printTask\`, чтобы не дублировать код вывода одной задачи.\r
\r
Такой подход показывает важную идею: функции можно комбинировать. Одна функция отвечает за список, другая — за одну задачу.\r
\r
## Задание 9. Создать функцию для чтения приоритета\r
\r
При добавлении и изменении задачи нужно будет читать приоритет. Чтобы не повторять один и тот же код, создадим отдельную функцию.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func readPriority(reader *bufio.Reader) (int, bool) {\r
    fmt.Print("Введите приоритет (1-3): ")\r
    input := readLine(reader)\r
\r
    priority, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть числом")\r
        return 0, false\r
    }\r
\r
    if priority < 1 || priority > 3 {\r
        fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
        return 0, false\r
    }\r
\r
    return priority, true\r
}\r
\`\`\`\r
\r
Функция возвращает два значения:\r
\r
\`\`\`go\r
(int, bool)\r
\`\`\`\r
\r
Первое значение — приоритет. Второе — признак успешности. Если пользователь ввёл корректное число от \`1\` до \`3\`, функция возвращает приоритет и \`true\`. Если ввод неправильный, возвращается \`0\` и \`false\`.\r
\r
Это удобный способ не продолжать действие, если данные введены неверно.\r
\r
## Задание 10. Добавить новую задачу как структуру\r
\r
Теперь создадим функцию для добавления задачи.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func addTask(tasks *[]Task, reader *bufio.Reader) {\r
    fmt.Print("Введите название задачи: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    task := Task{\r
        Title:    title,\r
        Priority: priority,\r
    }\r
\r
    *tasks = append(*tasks, task)\r
    fmt.Println("Задача добавлена")\r
}\r
\`\`\`\r
\r
Здесь создаётся значение структуры \`Task\`.\r
\r
Обратите внимание, что поле \`Done\` не указано:\r
\r
\`\`\`go\r
task := Task{\r
    Title:    title,\r
    Priority: priority,\r
}\r
\`\`\`\r
\r
Это не ошибка. Если поле структуры не заполнено явно, оно получает нулевое значение своего типа. Для \`bool\` нулевое значение — \`false\`. Это подходит по смыслу: новая задача ещё не выполнена.\r
\r
Функция принимает параметр:\r
\r
\`\`\`go\r
tasks *[]Task\r
\`\`\`\r
\r
Это указатель на срез задач. Он нужен потому, что внутри функции выполняется \`append\`, и результат нужно сохранить в исходную переменную \`tasks\` из \`main\`.\r
\r
При вызове нужно будет передать адрес среза:\r
\r
\`\`\`go\r
addTask(&tasks, reader)\r
\`\`\`\r
\r
## Задание 11. Создать функцию для чтения номера задачи\r
\r
Для отметки выполнения, изменения приоритета и удаления нужно выбирать задачу по номеру.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func readTaskIndex(reader *bufio.Reader, tasks []Task) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\`\`\`\r
\r
Пользователь вводит номер с единицы, например \`1\`, \`2\`, \`3\`. Но внутри среза индексы начинаются с нуля, поэтому программа вычисляет:\r
\r
\`\`\`go\r
index := number - 1\r
\`\`\`\r
\r
Если индекс выходит за границы среза, функция возвращает \`false\`.\r
\r
## Задание 12. Отметить задачу выполненной через указатель на структуру\r
\r
Теперь создадим функцию, которая меняет поле \`Done\` у задачи.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func completeTask(task *Task) {\r
    task.Done = true\r
}\r
\`\`\`\r
\r
Параметр имеет тип:\r
\r
\`\`\`go\r
task *Task\r
\`\`\`\r
\r
Это указатель на структуру \`Task\`. Он нужен потому, что функция должна изменить оригинальную задачу, а не её копию.\r
\r
Внутри функции можно писать:\r
\r
\`\`\`go\r
task.Done = true\r
\`\`\`\r
\r
Хотя \`task\` — указатель, Go автоматически разыменовывает его при обращении к полю через точку. Поэтому не нужно писать так:\r
\r
\`\`\`go\r
(*task).Done = true\r
\`\`\`\r
\r
Теперь добавьте функцию, которая выбирает задачу из списка и передаёт её адрес в \`completeTask\`:\r
\r
\`\`\`go\r
func completeTaskByIndex(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if tasks[index].Done {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    completeTask(&tasks[index])\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\`\`\`\r
\r
Здесь особенно важна строка:\r
\r
\`\`\`go\r
completeTask(&tasks[index])\r
\`\`\`\r
\r
Она передаёт в функцию адрес конкретной задачи из среза. Благодаря этому меняется именно элемент среза, а не временная копия.\r
\r
## Задание 13. Изменить приоритет задачи\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func changeTaskPriority(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    tasks[index].Priority = priority\r
    fmt.Println("Приоритет обновлён")\r
}\r
\`\`\`\r
\r
Срез передаётся без указателя, но изменение поля элемента сохраняется. В этой практике достаточно понимать это так: мы не заменяем сам срез целиком, а меняем поле конкретной структуры внутри существующего среза.\r
\r
Если бы функция должна была добавить или удалить элементы, тогда понадобился бы указатель на срез. Но для изменения поля существующего элемента можно работать с \`tasks[index]\`.\r
\r
## Задание 14. Удалить задачу\r
\r
Добавьте функцию удаления:\r
\r
\`\`\`go\r
func deleteTask(tasks *[]Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index].Title\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\`\`\`\r
\r
Здесь снова используется указатель на срез:\r
\r
\`\`\`go\r
tasks *[]Task\r
\`\`\`\r
\r
Удаление меняет сам срез: его длина становится меньше. Поэтому результат нужно записать обратно в исходную переменную.\r
\r
Строка удаления:\r
\r
\`\`\`go\r
*tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\`\`\`\r
\r
соединяет элементы до удаляемой задачи и элементы после неё.\r
\r
В отличие от предыдущей версии проекта, здесь не нужно пересобирать карту выполненных задач. Статус выполнения теперь хранится внутри самой структуры \`Task\`, поэтому при удалении элемента из среза все данные задачи удаляются вместе.\r
\r
Это одно из главных преимуществ структур в этом проекте.\r
\r
## Задание 15. Посчитать статистику по срезу структур\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func calculateStats(tasks []Task) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for _, task := range tasks {\r
        if task.Done {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\`\`\`\r
\r
Эта функция получает срез задач и возвращает три значения:\r
\r
- всего задач;\r
- выполнено;\r
- осталось.\r
\r
Теперь добавьте функцию вывода статистики:\r
\r
\`\`\`go\r
func showStats(tasks []Task) {\r
    total, done, remaining := calculateStats(tasks)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\`\`\`\r
\r
Теперь статистика считается проще, чем раньше. Не нужно отдельно сверять срез задач и карту статусов. Достаточно пройти по срезу \`[]Task\` и проверить поле \`Done\` у каждой задачи.\r
\r
## Задание 16. Собрать программу в main\r
\r
Теперь соберите основную функцию \`main\`.\r
\r
Она должна создать начальные данные, показать меню и вызывать нужные функции:\r
\r
\`\`\`go\r
func main() {\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []Task{\r
        {\r
            Title:    "Изучить структуры",\r
            Done:     false,\r
            Priority: 1,\r
        },\r
        {\r
            Title:    "Переписать To-Do проект",\r
            Done:     false,\r
            Priority: 2,\r
        },\r
        {\r
            Title:    "Повторить функции",\r
            Done:     true,\r
            Priority: 3,\r
        },\r
    }\r
\r
    for {\r
        showMenu()\r
\r
        fmt.Print("Выберите действие: ")\r
        command := readLine(reader)\r
\r
        switch command {\r
        case "1":\r
            showTasks(tasks)\r
        case "2":\r
            addTask(&tasks, reader)\r
        case "3":\r
            completeTaskByIndex(tasks, reader)\r
        case "4":\r
            changeTaskPriority(tasks, reader)\r
        case "5":\r
            deleteTask(&tasks, reader)\r
        case "6":\r
            showStats(tasks)\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
Теперь \`main\` снова остаётся достаточно короткой. Она управляет меню, но подробности работы с задачами находятся в отдельных функциях.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Изменить приоритет")\r
    fmt.Println("5 — Удалить задачу")\r
    fmt.Println("6 — Показать статистику")\r
    fmt.Println("0 — Выйти")\r
}\r
\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\r
func getPriorityText(priority int) string {\r
    switch priority {\r
    case 1:\r
        return "высокий"\r
    case 2:\r
        return "средний"\r
    case 3:\r
        return "низкий"\r
    default:\r
        return "неизвестный"\r
    }\r
}\r
\r
func printTask(index int, task Task) {\r
    status := "[ ]"\r
\r
    if task.Done {\r
        status = "[x]"\r
    }\r
\r
    fmt.Printf("%d. %s %s — приоритет: %s\\n",\r
        index+1,\r
        status,\r
        task.Title,\r
        getPriorityText(task.Priority),\r
    )\r
}\r
\r
func showTasks(tasks []Task) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        printTask(index, task)\r
    }\r
}\r
\r
func readPriority(reader *bufio.Reader) (int, bool) {\r
    fmt.Print("Введите приоритет (1-3): ")\r
    input := readLine(reader)\r
\r
    priority, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть числом")\r
        return 0, false\r
    }\r
\r
    if priority < 1 || priority > 3 {\r
        fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
        return 0, false\r
    }\r
\r
    return priority, true\r
}\r
\r
func addTask(tasks *[]Task, reader *bufio.Reader) {\r
    fmt.Print("Введите название задачи: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    task := Task{\r
        Title:    title,\r
        Priority: priority,\r
    }\r
\r
    *tasks = append(*tasks, task)\r
    fmt.Println("Задача добавлена")\r
}\r
\r
func readTaskIndex(reader *bufio.Reader, tasks []Task) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\r
func completeTask(task *Task) {\r
    task.Done = true\r
}\r
\r
func completeTaskByIndex(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if tasks[index].Done {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    completeTask(&tasks[index])\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\r
func changeTaskPriority(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    tasks[index].Priority = priority\r
    fmt.Println("Приоритет обновлён")\r
}\r
\r
func deleteTask(tasks *[]Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index].Title\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\r
func calculateStats(tasks []Task) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for _, task := range tasks {\r
        if task.Done {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\r
func showStats(tasks []Task) {\r
    total, done, remaining := calculateStats(tasks)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\r
func main() {\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []Task{\r
        {\r
            Title:    "Изучить структуры",\r
            Done:     false,\r
            Priority: 1,\r
        },\r
        {\r
            Title:    "Переписать To-Do проект",\r
            Done:     false,\r
            Priority: 2,\r
        },\r
        {\r
            Title:    "Повторить функции",\r
            Done:     true,\r
            Priority: 3,\r
        },\r
    }\r
\r
    for {\r
        showMenu()\r
\r
        fmt.Print("Выберите действие: ")\r
        command := readLine(reader)\r
\r
        switch command {\r
        case "1":\r
            showTasks(tasks)\r
        case "2":\r
            addTask(&tasks, reader)\r
        case "3":\r
            completeTaskByIndex(tasks, reader)\r
        case "4":\r
            changeTaskPriority(tasks, reader)\r
        case "5":\r
            deleteTask(&tasks, reader)\r
        case "6":\r
            showStats(tasks)\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
0 — Выйти\r
Выберите действие: 1\r
\r
Список задач:\r
1. [ ] Изучить структуры — приоритет: высокий\r
2. [ ] Переписать To-Do проект — приоритет: средний\r
3. [x] Повторить функции — приоритет: низкий\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
0 — Выйти\r
Выберите действие: 2\r
Введите название задачи: Добавить структуры в проект\r
Введите приоритет (1-3): 1\r
Задача добавлена\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
0 — Выйти\r
Выберите действие: 6\r
\r
Статистика:\r
Всего задач: 4\r
Выполнено: 1\r
Осталось: 3\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии To-Do приложения данные стали организованы намного лучше. Вместо среза строк и отдельной карты статусов используется один срез структур:\r
\r
\`\`\`go\r
tasks := []Task{}\r
\`\`\`\r
\r
Каждая задача теперь хранит все свои данные внутри себя:\r
\r
\`\`\`go\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\`\`\`\r
\r
Это делает программу проще. При выводе задачи не нужно искать её статус в отдельной карте. При удалении задачи не нужно пересобирать карту статусов. При добавлении новой задачи не нужно синхронизировать несколько коллекций. Достаточно создать одно значение \`Task\` и добавить его в срез.\r
\r
Функция \`printTask\` показывает, как структура передаётся в функцию по значению. Она получает копию задачи и просто читает её поля. Это безопасно, потому что функция не должна менять оригинал.\r
\r
Функция \`completeTask\` показывает другой подход: она принимает указатель на структуру. Это нужно, потому что функция меняет поле \`Done\` у исходной задачи.\r
\r
\`\`\`go\r
func completeTask(task *Task) {\r
    task.Done = true\r
}\r
\`\`\`\r
\r
При вызове используется адрес конкретного элемента среза:\r
\r
\`\`\`go\r
completeTask(&tasks[index])\r
\`\`\`\r
\r
Так изменение сохраняется в основном списке задач.\r
\r
Функция \`addTask\` показывает ещё одну ситуацию, где нужен указатель: она принимает \`*[]Task\`, потому что добавление через \`append\` может заменить срез новым значением. Чтобы это изменение сохранилось в \`main\`, функция получает адрес среза.\r
\r
Таким образом, в программе используются оба варианта:\r
\r
- структура по значению, если нужно только читать данные;\r
- указатель на структуру, если нужно изменить саму задачу;\r
- указатель на срез, если нужно изменить длину списка задач.\r
\r
## Самостоятельное задание\r
\r
Добавьте в структуру \`Task\` новое поле:\r
\r
\`\`\`go\r
Deadline string\r
\`\`\`\r
\r
После этого измените функцию \`addTask\`, чтобы пользователь мог вводить срок выполнения задачи:\r
\r
\`\`\`go\r
fmt.Print("Введите срок выполнения: ")\r
deadline := readLine(reader)\r
\`\`\`\r
\r
При создании задачи заполните новое поле:\r
\r
\`\`\`go\r
task := Task{\r
    Title:    title,\r
    Priority: priority,\r
    Deadline: deadline,\r
}\r
\`\`\`\r
\r
Измените функцию \`printTask\`, чтобы она выводила срок:\r
\r
\`\`\`go\r
fmt.Printf("%d. %s %s — приоритет: %s, срок: %s\\n",\r
    index+1,\r
    status,\r
    task.Title,\r
    getPriorityText(task.Priority),\r
    task.Deadline,\r
)\r
\`\`\`\r
\r
Если срок не введён, можно выводить текст \`"без срока"\`.\r
\r
Это задание показывает, почему структуры удобнее отдельных срезов. Чтобы добавить новое свойство задачи, достаточно расширить структуру и обновить функции, которые с ней работают.\r
\r
## Вопросы для самопроверки\r
\r
1. Зачем нужны структуры?\r
2. Чем структура отличается от отдельного набора переменных?\r
3. Что означает объявление \`type Task struct\`?\r
4. Что такое поле структуры?\r
5. Какие поля есть у структуры \`Task\` в этой практике?\r
6. Как создать значение структуры с заполнением полей?\r
7. Что произойдёт с полем структуры, если не указать его значение при создании?\r
8. Как обратиться к полю структуры?\r
9. Как изменить поле структуры?\r
10. Что означает тип \`[]Task\`?\r
11. Почему срез структур удобнее, чем несколько отдельных коллекций?\r
12. Что происходит со структурой при передаче в функцию по значению?\r
13. Почему \`printTask\` может принимать \`task Task\`, а не \`*Task\`?\r
14. Почему \`completeTask\` принимает \`*Task\`?\r
15. Почему в вызове используется \`completeTask(&tasks[index])\`?\r
16. Почему для \`addTask\` используется параметр \`*[]Task\`?\r
17. Почему после перехода к структурам больше не нужна отдельная карта \`completed\`?\r
18. Почему удалить задачу стало проще, чем в версии с отдельной картой статусов?\r
`,v=`# Практическая работа 2.9. To-Do список: методы и интерфейсы\r
\r
## Тема\r
\r
Методы, получатели по значению и по указателю, интерфейсы в Go.\r
\r
## Мини-проект\r
\r
В этой практической работе продолжается разработка консольного приложения **To-Do List**. В предыдущей версии проекта появилась структура \`Task\`, которая объединила связанные данные задачи: название, статус выполнения и приоритет.\r
\r
Теперь сделаем следующий шаг. Задача будет хранить не только данные, но и часть поведения. Например, задача сможет:\r
\r
- возвращать текстовое описание своего приоритета;\r
- выводить себя в консоль;\r
- отмечать себя выполненной;\r
- изменять свой приоритет;\r
- формировать краткое описание.\r
\r
Для этого используются методы. Метод похож на обычную функцию, но он привязан к конкретному типу. В нашем случае методы будут привязаны к типу \`Task\`.\r
\r
В конце практической работы также будет добавлен простой интерфейс \`Summary\`, который описывает объект, способный возвращать краткое описание. Это позволит показать, что интерфейс в Go описывает не конкретную структуру, а поведение.\r
\r
## Цель работы\r
\r
Цель практической работы — научиться добавлять поведение к собственным типам данных с помощью методов и познакомиться с базовым использованием интерфейсов.\r
\r
В ходе выполнения работы необходимо научиться:\r
\r
- создавать методы для структуры;\r
- использовать получатель метода;\r
- выбирать получатель по значению, если метод только читает данные;\r
- выбирать получатель по указателю, если метод изменяет объект;\r
- вызывать методы через точку;\r
- переносить часть логики из обычных функций в методы;\r
- создавать простой интерфейс;\r
- использовать интерфейс как параметр функции.\r
\r
## Что должно получиться\r
\r
После запуска программа должна показать меню:\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
7 — Показать краткое описание задачи\r
0 — Выйти\r
Выберите действие:\r
\`\`\`\r
\r
Программа должна позволять работать со списком задач, как и в предыдущей версии. Главное отличие будет не столько во внешнем поведении, сколько во внутренней организации кода.\r
\r
Раньше логика задачи находилась во внешних функциях:\r
\r
\`\`\`go\r
printTask(index, task)\r
completeTask(&tasks[index])\r
getPriorityText(task.Priority)\r
\`\`\`\r
\r
Теперь часть поведения будет вызываться прямо у задачи:\r
\r
\`\`\`go\r
task.Print(index)\r
tasks[index].Complete()\r
task.PriorityText()\r
\`\`\`\r
\r
Такой код читается естественнее: действие вызывается у того объекта, к которому оно относится.\r
\r
## Подготовка проекта\r
\r
Создайте новую папку для практической работы:\r
\r
\`\`\`bash\r
todo-methods\r
\`\`\`\r
\r
Перейдите в неё и выполните команду:\r
\r
\`\`\`bash\r
go mod init todo-methods\r
\`\`\`\r
\r
Создайте файл:\r
\r
\`\`\`text\r
main.go\r
\`\`\`\r
\r
Добавьте стартовый код:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Go To-Do Manager")\r
}\r
\`\`\`\r
\r
Запустите программу:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
Если программа запускается, можно переходить к заданиям.\r
\r
## Задание 1. Подготовить импорты\r
\r
В этой версии приложения снова понадобятся пакеты для чтения строк, работы с вводом и преобразования строк в числа.\r
\r
Добавьте импорты:\r
\r
\`\`\`go\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\`\`\`\r
\r
Эти пакеты уже использовались в предыдущих практических работах. В этой версии они нужны для меню, добавления задач и чтения номера задачи.\r
\r
## Задание 2. Объявить структуру Task\r
\r
Выше функции \`main\` объявите структуру \`Task\`:\r
\r
\`\`\`go\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\`\`\`\r
\r
Эта структура описывает одну задачу.\r
\r
Поле \`Title\` хранит название задачи.  \r
Поле \`Done\` показывает, выполнена задача или нет.  \r
Поле \`Priority\` хранит приоритет задачи: \`1\`, \`2\` или \`3\`.\r
\r
Пока это только данные. В следующих заданиях к этому типу будут добавлены методы.\r
\r
## Задание 3. Добавить метод PriorityText\r
\r
Начнём с метода, который только читает данные задачи и ничего не изменяет.\r
\r
Добавьте метод:\r
\r
\`\`\`go\r
func (task Task) PriorityText() string {\r
    switch task.Priority {\r
    case 1:\r
        return "высокий"\r
    case 2:\r
        return "средний"\r
    case 3:\r
        return "низкий"\r
    default:\r
        return "неизвестный"\r
    }\r
}\r
\`\`\`\r
\r
Этот метод привязан к типу \`Task\`.\r
\r
Часть:\r
\r
\`\`\`go\r
(task Task)\r
\`\`\`\r
\r
называется получателем метода. Она означает: метод \`PriorityText\` принадлежит типу \`Task\`, а внутри метода конкретная задача доступна через имя \`task\`.\r
\r
Метод возвращает строку, поэтому после скобок указан тип результата:\r
\r
\`\`\`go\r
func (task Task) PriorityText() string\r
\`\`\`\r
\r
Получатель здесь передаётся по значению. Это подходит, потому что метод только читает поле \`Priority\` и не изменяет задачу.\r
\r
Теперь вместо внешней функции \`getPriorityText(task.Priority)\` можно будет писать:\r
\r
\`\`\`go\r
task.PriorityText()\r
\`\`\`\r
\r
Такой вызов читается естественнее: «у задачи получить текст приоритета».\r
\r
## Задание 4. Добавить метод StatusMark\r
\r
Добавьте метод, который возвращает метку выполнения:\r
\r
\`\`\`go\r
func (task Task) StatusMark() string {\r
    if task.Done {\r
        return "[x]"\r
    }\r
\r
    return "[ ]"\r
}\r
\`\`\`\r
\r
Этот метод тоже использует получатель по значению:\r
\r
\`\`\`go\r
(task Task)\r
\`\`\`\r
\r
Он только читает поле \`Done\`, поэтому не должен менять исходную задачу.\r
\r
Если задача выполнена, метод возвращает \`[x]\`. Если не выполнена — \`[ ]\`.\r
\r
Теперь вывод задачи можно сделать проще:\r
\r
\`\`\`go\r
fmt.Println(task.StatusMark())\r
\`\`\`\r
\r
Вместо того чтобы каждый раз писать проверку \`if task.Done\`, программа может спросить у самой задачи, какую метку ей нужно показать.\r
\r
## Задание 5. Добавить метод Print\r
\r
Теперь добавим метод, который выводит задачу в консоль.\r
\r
\`\`\`go\r
func (task Task) Print(index int) {\r
    fmt.Printf("%d. %s %s — приоритет: %s\\n",\r
        index+1,\r
        task.StatusMark(),\r
        task.Title,\r
        task.PriorityText(),\r
    )\r
}\r
\`\`\`\r
\r
Метод \`Print\` принимает обычный параметр:\r
\r
\`\`\`go\r
index int\r
\`\`\`\r
\r
Получатель \`task Task\` не считается обычным параметром. Это объект, у которого вызывается метод.\r
\r
Внутри \`Print\` используются другие методы той же структуры:\r
\r
\`\`\`go\r
task.StatusMark()\r
task.PriorityText()\r
\`\`\`\r
\r
Это нормальная практика. Один метод может вызывать другие методы того же типа.\r
\r
Метод \`Print\` не меняет задачу, поэтому получатель снова передаётся по значению.\r
\r
## Задание 6. Добавить метод Complete с получателем по указателю\r
\r
Теперь создадим метод, который меняет задачу.\r
\r
Добавьте метод:\r
\r
\`\`\`go\r
func (task *Task) Complete() {\r
    task.Done = true\r
}\r
\`\`\`\r
\r
Здесь получатель имеет тип:\r
\r
\`\`\`go\r
(task *Task)\r
\`\`\`\r
\r
Звёздочка означает, что метод получает указатель на задачу. Это нужно потому, что метод должен изменить поле \`Done\` у исходной задачи, а не у копии.\r
\r
Если бы метод был объявлен так:\r
\r
\`\`\`go\r
func (task Task) Complete() {\r
    task.Done = true\r
}\r
\`\`\`\r
\r
он изменил бы только копию задачи, и после выхода из метода исходная задача осталась бы невыполненной.\r
\r
В Go при работе с полями через точку не нужно писать \`(*task).Done\`. Можно писать проще:\r
\r
\`\`\`go\r
task.Done = true\r
\`\`\`\r
\r
Go сам понимает, что \`task\` — указатель, и автоматически разыменовывает его.\r
\r
## Задание 7. Добавить метод ChangePriority\r
\r
Добавьте метод для изменения приоритета:\r
\r
\`\`\`go\r
func (task *Task) ChangePriority(priority int) {\r
    task.Priority = priority\r
}\r
\`\`\`\r
\r
Этот метод тоже использует получатель по указателю, потому что изменяет исходную задачу.\r
\r
Метод принимает обычный параметр:\r
\r
\`\`\`go\r
priority int\r
\`\`\`\r
\r
Он задаёт новое значение приоритета.\r
\r
Позже метод будет вызываться так:\r
\r
\`\`\`go\r
tasks[index].ChangePriority(priority)\r
\`\`\`\r
\r
Хотя метод требует указатель \`*Task\`, Go позволит вызвать его у элемента среза через точку. Компилятор автоматически возьмёт адрес элемента, если это возможно.\r
\r
## Задание 8. Добавить метод Summary\r
\r
Теперь добавим метод, который будет возвращать краткое описание задачи.\r
\r
\`\`\`go\r
func (task Task) Summary() string {\r
    status := "не выполнена"\r
\r
    if task.Done {\r
        status = "выполнена"\r
    }\r
\r
    return fmt.Sprintf("%s — %s, приоритет: %s",\r
        task.Title,\r
        status,\r
        task.PriorityText(),\r
    )\r
}\r
\`\`\`\r
\r
Метод \`Summary\` возвращает строку. Он не печатает её сам, а именно возвращает результат.\r
\r
Это удобно, потому что строку можно использовать по-разному:\r
\r
\`\`\`go\r
fmt.Println(task.Summary())\r
\`\`\`\r
\r
или передать в другую функцию.\r
\r
Метод использует получатель по значению, потому что только читает поля задачи.\r
\r
## Задание 9. Объявить интерфейс Summary\r
\r
Теперь добавим простой интерфейс.\r
\r
Выше функций объявите интерфейс:\r
\r
\`\`\`go\r
type Summary interface {\r
    Summary() string\r
}\r
\`\`\`\r
\r
Интерфейс \`Summary\` описывает требование: любой тип, который хочет считаться \`Summary\`, должен иметь метод:\r
\r
\`\`\`go\r
Summary() string\r
\`\`\`\r
\r
Структура \`Task\` уже имеет такой метод:\r
\r
\`\`\`go\r
func (task Task) Summary() string\r
\`\`\`\r
\r
Значит, \`Task\` автоматически реализует интерфейс \`Summary\`.\r
\r
В Go не нужно писать отдельное объявление вроде \`implements Summary\`. Если у типа есть все методы интерфейса, он подходит под этот интерфейс автоматически.\r
\r
## Задание 10. Создать функцию, принимающую интерфейс\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func printSummary(item Summary) {\r
    fmt.Println(item.Summary())\r
}\r
\`\`\`\r
\r
Эта функция принимает не конкретно \`Task\`, а любое значение, у которого есть метод \`Summary() string\`.\r
\r
Пока в нашей программе таким значением будет задача. Но сама функция не знает, что ей передали именно задачу. Она знает только одно: у переданного объекта можно вызвать метод \`Summary\`.\r
\r
Это и есть смысл интерфейса: работать не с конкретной структурой, а с поведением.\r
\r
## Задание 11. Добавить вспомогательные функции меню и ввода\r
\r
Теперь добавьте уже знакомые функции для меню и чтения строки:\r
\r
\`\`\`go\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Изменить приоритет")\r
    fmt.Println("5 — Удалить задачу")\r
    fmt.Println("6 — Показать статистику")\r
    fmt.Println("7 — Показать краткое описание задачи")\r
    fmt.Println("0 — Выйти")\r
}\r
\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\`\`\`\r
\r
Эти функции остаются обычными функциями, а не методами, потому что они не относятся к конкретной задаче. Меню относится ко всему приложению, а чтение строки — это служебная операция.\r
\r
Не всё нужно превращать в методы. Метод стоит создавать тогда, когда действие логически принадлежит конкретному типу.\r
\r
## Задание 12. Создать функцию readPriority\r
\r
Добавьте функцию для чтения приоритета:\r
\r
\`\`\`go\r
func readPriority(reader *bufio.Reader) (int, bool) {\r
    fmt.Print("Введите приоритет (1-3): ")\r
    input := readLine(reader)\r
\r
    priority, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть числом")\r
        return 0, false\r
    }\r
\r
    if priority < 1 || priority > 3 {\r
        fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
        return 0, false\r
    }\r
\r
    return priority, true\r
}\r
\`\`\`\r
\r
Эта функция возвращает два значения: сам приоритет и признак успешности. Она остаётся обычной функцией, потому что чтение приоритета не принадлежит одной конкретной задаче. Это часть пользовательского ввода.\r
\r
## Задание 13. Создать функцию readTaskIndex\r
\r
Добавьте функцию для выбора задачи по номеру:\r
\r
\`\`\`go\r
func readTaskIndex(reader *bufio.Reader, tasks []Task) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\`\`\`\r
\r
Пользователь вводит номер с \`1\`, а внутри среза индексы начинаются с \`0\`. Поэтому программа вычисляет:\r
\r
\`\`\`go\r
index := number - 1\r
\`\`\`\r
\r
## Задание 14. Обновить функции работы со списком задач\r
\r
Теперь функции, которые работают со списком, могут использовать методы \`Task\`.\r
\r
Добавьте функцию вывода задач:\r
\r
\`\`\`go\r
func showTasks(tasks []Task) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        task.Print(index)\r
    }\r
}\r
\`\`\`\r
\r
Обратите внимание на строку:\r
\r
\`\`\`go\r
task.Print(index)\r
\`\`\`\r
\r
Теперь вывод одной задачи спрятан внутри метода самой задачи.\r
\r
Добавьте функцию добавления задачи:\r
\r
\`\`\`go\r
func addTask(tasks *[]Task, reader *bufio.Reader) {\r
    fmt.Print("Введите название задачи: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    task := Task{\r
        Title:    title,\r
        Priority: priority,\r
    }\r
\r
    *tasks = append(*tasks, task)\r
    fmt.Println("Задача добавлена")\r
}\r
\`\`\`\r
\r
Новая задача создаётся как структура \`Task\`. Поле \`Done\` не заполняется явно, поэтому получает нулевое значение \`false\`.\r
\r
## Задание 15. Отметить задачу выполненной через метод\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func completeTaskByIndex(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if tasks[index].Done {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    tasks[index].Complete()\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\`\`\`\r
\r
Главная строка здесь:\r
\r
\`\`\`go\r
tasks[index].Complete()\r
\`\`\`\r
\r
Метод \`Complete\` имеет получатель по указателю:\r
\r
\`\`\`go\r
func (task *Task) Complete()\r
\`\`\`\r
\r
Но при вызове мы всё равно пишем просто \`tasks[index].Complete()\`. Go автоматически понимает, что нужно взять адрес элемента среза.\r
\r
Это удобнее, чем вызывать обычную функцию:\r
\r
\`\`\`go\r
completeTask(&tasks[index])\r
\`\`\`\r
\r
Метод делает код ближе к смыслу: «у этой задачи выполнить действие Complete».\r
\r
## Задание 16. Изменить приоритет через метод\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func changeTaskPriority(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    tasks[index].ChangePriority(priority)\r
    fmt.Println("Приоритет обновлён")\r
}\r
\`\`\`\r
\r
Здесь используется метод:\r
\r
\`\`\`go\r
tasks[index].ChangePriority(priority)\r
\`\`\`\r
\r
Он изменяет поле \`Priority\` у выбранной задачи.\r
\r
## Задание 17. Удалить задачу\r
\r
Удаление задачи остаётся обычной функцией, потому что оно относится не к одной задаче, а ко всему срезу задач.\r
\r
Добавьте функцию:\r
\r
\`\`\`go\r
func deleteTask(tasks *[]Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index].Title\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\`\`\`\r
\r
Здесь нужен указатель на срез \`*[]Task\`, потому что функция изменяет длину списка.\r
\r
## Задание 18. Посчитать статистику\r
\r
Добавьте функции статистики:\r
\r
\`\`\`go\r
func calculateStats(tasks []Task) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for _, task := range tasks {\r
        if task.Done {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\r
func showStats(tasks []Task) {\r
    total, done, remaining := calculateStats(tasks)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\`\`\`\r
\r
Эти функции можно было бы позже тоже перенести в отдельный тип списка задач, но в этой практике достаточно методов у самой структуры \`Task\`.\r
\r
## Задание 19. Добавить пункт меню для интерфейса\r
\r
Теперь добавим функцию, которая показывает краткое описание выбранной задачи через интерфейс \`Summary\`.\r
\r
\`\`\`go\r
func showTaskSummary(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    printSummary(tasks[index])\r
}\r
\`\`\`\r
\r
Здесь функция \`printSummary\` принимает значение интерфейсного типа:\r
\r
\`\`\`go\r
func printSummary(item Summary)\r
\`\`\`\r
\r
А передаём мы туда обычную задачу:\r
\r
\`\`\`go\r
printSummary(tasks[index])\r
\`\`\`\r
\r
Это работает, потому что у \`Task\` есть метод \`Summary() string\`.\r
\r
Такой пример показывает базовый смысл интерфейса: функция зависит не от конкретного типа \`Task\`, а от того, что переданное значение умеет возвращать краткое описание.\r
\r
## Итоговый код программы\r
\r
\`\`\`go\r
package main\r
\r
import (\r
    "bufio"\r
    "fmt"\r
    "os"\r
    "strconv"\r
    "strings"\r
)\r
\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\r
type Summary interface {\r
    Summary() string\r
}\r
\r
func (task Task) PriorityText() string {\r
    switch task.Priority {\r
    case 1:\r
        return "высокий"\r
    case 2:\r
        return "средний"\r
    case 3:\r
        return "низкий"\r
    default:\r
        return "неизвестный"\r
    }\r
}\r
\r
func (task Task) StatusMark() string {\r
    if task.Done {\r
        return "[x]"\r
    }\r
\r
    return "[ ]"\r
}\r
\r
func (task Task) Print(index int) {\r
    fmt.Printf("%d. %s %s — приоритет: %s\\n",\r
        index+1,\r
        task.StatusMark(),\r
        task.Title,\r
        task.PriorityText(),\r
    )\r
}\r
\r
func (task *Task) Complete() {\r
    task.Done = true\r
}\r
\r
func (task *Task) ChangePriority(priority int) {\r
    task.Priority = priority\r
}\r
\r
func (task Task) Summary() string {\r
    status := "не выполнена"\r
\r
    if task.Done {\r
        status = "выполнена"\r
    }\r
\r
    return fmt.Sprintf("%s — %s, приоритет: %s",\r
        task.Title,\r
        status,\r
        task.PriorityText(),\r
    )\r
}\r
\r
func printSummary(item Summary) {\r
    fmt.Println(item.Summary())\r
}\r
\r
func showMenu() {\r
    fmt.Println()\r
    fmt.Println("=== Go To-Do Manager ===")\r
    fmt.Println("1 — Показать задачи")\r
    fmt.Println("2 — Добавить задачу")\r
    fmt.Println("3 — Отметить задачу выполненной")\r
    fmt.Println("4 — Изменить приоритет")\r
    fmt.Println("5 — Удалить задачу")\r
    fmt.Println("6 — Показать статистику")\r
    fmt.Println("7 — Показать краткое описание задачи")\r
    fmt.Println("0 — Выйти")\r
}\r
\r
func readLine(reader *bufio.Reader) string {\r
    text, _ := reader.ReadString('\\n')\r
    return strings.TrimSpace(text)\r
}\r
\r
func readPriority(reader *bufio.Reader) (int, bool) {\r
    fmt.Print("Введите приоритет (1-3): ")\r
    input := readLine(reader)\r
\r
    priority, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: приоритет должен быть числом")\r
        return 0, false\r
    }\r
\r
    if priority < 1 || priority > 3 {\r
        fmt.Println("Ошибка: приоритет должен быть от 1 до 3")\r
        return 0, false\r
    }\r
\r
    return priority, true\r
}\r
\r
func readTaskIndex(reader *bufio.Reader, tasks []Task) (int, bool) {\r
    fmt.Print("Введите номер задачи: ")\r
    input := readLine(reader)\r
\r
    number, err := strconv.Atoi(input)\r
    if err != nil {\r
        fmt.Println("Ошибка: нужно ввести число")\r
        return 0, false\r
    }\r
\r
    index := number - 1\r
\r
    if index < 0 || index >= len(tasks) {\r
        fmt.Println("Ошибка: задачи с таким номером нет")\r
        return 0, false\r
    }\r
\r
    return index, true\r
}\r
\r
func showTasks(tasks []Task) {\r
    if len(tasks) == 0 {\r
        fmt.Println("Список задач пуст")\r
        return\r
    }\r
\r
    fmt.Println()\r
    fmt.Println("Список задач:")\r
\r
    for index, task := range tasks {\r
        task.Print(index)\r
    }\r
}\r
\r
func addTask(tasks *[]Task, reader *bufio.Reader) {\r
    fmt.Print("Введите название задачи: ")\r
    title := readLine(reader)\r
\r
    if title == "" {\r
        fmt.Println("Название задачи не может быть пустым")\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    task := Task{\r
        Title:    title,\r
        Priority: priority,\r
    }\r
\r
    *tasks = append(*tasks, task)\r
    fmt.Println("Задача добавлена")\r
}\r
\r
func completeTaskByIndex(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    if tasks[index].Done {\r
        fmt.Println("Задача уже выполнена")\r
        return\r
    }\r
\r
    tasks[index].Complete()\r
    fmt.Println("Задача отмечена как выполненная")\r
}\r
\r
func changeTaskPriority(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    priority, ok := readPriority(reader)\r
    if !ok {\r
        return\r
    }\r
\r
    tasks[index].ChangePriority(priority)\r
    fmt.Println("Приоритет обновлён")\r
}\r
\r
func deleteTask(tasks *[]Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, *tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    title := (*tasks)[index].Title\r
\r
    *tasks = append((*tasks)[:index], (*tasks)[index+1:]...)\r
\r
    fmt.Println("Удалена задача:", title)\r
}\r
\r
func calculateStats(tasks []Task) (int, int, int) {\r
    total := len(tasks)\r
    done := 0\r
\r
    for _, task := range tasks {\r
        if task.Done {\r
            done++\r
        }\r
    }\r
\r
    remaining := total - done\r
\r
    return total, done, remaining\r
}\r
\r
func showStats(tasks []Task) {\r
    total, done, remaining := calculateStats(tasks)\r
\r
    fmt.Println()\r
    fmt.Println("Статистика:")\r
    fmt.Println("Всего задач:", total)\r
    fmt.Println("Выполнено:", done)\r
    fmt.Println("Осталось:", remaining)\r
}\r
\r
func showTaskSummary(tasks []Task, reader *bufio.Reader) {\r
    index, ok := readTaskIndex(reader, tasks)\r
    if !ok {\r
        return\r
    }\r
\r
    printSummary(tasks[index])\r
}\r
\r
func main() {\r
    reader := bufio.NewReader(os.Stdin)\r
\r
    tasks := []Task{\r
        {\r
            Title:    "Изучить методы",\r
            Done:     false,\r
            Priority: 1,\r
        },\r
        {\r
            Title:    "Добавить методы в To-Do проект",\r
            Done:     false,\r
            Priority: 2,\r
        },\r
        {\r
            Title:    "Повторить структуры",\r
            Done:     true,\r
            Priority: 3,\r
        },\r
    }\r
\r
    for {\r
        showMenu()\r
\r
        fmt.Print("Выберите действие: ")\r
        command := readLine(reader)\r
\r
        switch command {\r
        case "1":\r
            showTasks(tasks)\r
        case "2":\r
            addTask(&tasks, reader)\r
        case "3":\r
            completeTaskByIndex(tasks, reader)\r
        case "4":\r
            changeTaskPriority(tasks, reader)\r
        case "5":\r
            deleteTask(&tasks, reader)\r
        case "6":\r
            showStats(tasks)\r
        case "7":\r
            showTaskSummary(tasks, reader)\r
        case "0":\r
            fmt.Println("Выход из программы")\r
            return\r
        default:\r
            fmt.Println("Неизвестная команда")\r
        }\r
    }\r
}\r
\`\`\`\r
\r
## Пример работы программы\r
\r
\`\`\`text\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
7 — Показать краткое описание задачи\r
0 — Выйти\r
Выберите действие: 1\r
\r
Список задач:\r
1. [ ] Изучить методы — приоритет: высокий\r
2. [ ] Добавить методы в To-Do проект — приоритет: средний\r
3. [x] Повторить структуры — приоритет: низкий\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
7 — Показать краткое описание задачи\r
0 — Выйти\r
Выберите действие: 7\r
Введите номер задачи: 1\r
Изучить методы — не выполнена, приоритет: высокий\r
\r
=== Go To-Do Manager ===\r
1 — Показать задачи\r
2 — Добавить задачу\r
3 — Отметить задачу выполненной\r
4 — Изменить приоритет\r
5 — Удалить задачу\r
6 — Показать статистику\r
7 — Показать краткое описание задачи\r
0 — Выйти\r
Выберите действие: 3\r
Введите номер задачи: 1\r
Задача отмечена как выполненная\r
\`\`\`\r
\r
## Разбор итоговой программы\r
\r
В этой версии To-Do проекта структура \`Task\` получила методы. Это значит, что часть поведения теперь находится рядом с данными.\r
\r
Методы \`PriorityText\`, \`StatusMark\`, \`Print\` и \`Summary\` используют получатель по значению:\r
\r
\`\`\`go\r
func (task Task) PriorityText() string\r
\`\`\`\r
\r
Они только читают поля задачи и не изменяют её. Поэтому безопасно работать с копией.\r
\r
Методы \`Complete\` и \`ChangePriority\` используют получатель по указателю:\r
\r
\`\`\`go\r
func (task *Task) Complete()\r
\`\`\`\r
\r
Они изменяют поля структуры. Если бы получатель был по значению, метод изменил бы только копию, и исходная задача в срезе осталась бы прежней.\r
\r
Особенно важно сравнить два варианта.\r
\r
Обычная функция из предыдущей практики:\r
\r
\`\`\`go\r
completeTask(&tasks[index])\r
\`\`\`\r
\r
Метод в этой версии:\r
\r
\`\`\`go\r
tasks[index].Complete()\r
\`\`\`\r
\r
Второй вариант читается проще. Видно, что действие выполняется именно у выбранной задачи.\r
\r
Интерфейс \`Summary\` показывает, что в Go можно описывать не конкретный тип, а поведение:\r
\r
\`\`\`go\r
type Summary interface {\r
    Summary() string\r
}\r
\`\`\`\r
\r
Функция \`printSummary\` принимает любой объект, который умеет возвращать краткое описание:\r
\r
\`\`\`go\r
func printSummary(item Summary) {\r
    fmt.Println(item.Summary())\r
}\r
\`\`\`\r
\r
В этой программе таким объектом является \`Task\`, потому что у него есть метод \`Summary() string\`.\r
\r
## Самостоятельное задание\r
\r
Добавьте в структуру \`Task\` новое поле:\r
\r
\`\`\`go\r
Deadline string\r
\`\`\`\r
\r
Измените функцию \`addTask\`, чтобы пользователь мог вводить срок выполнения задачи:\r
\r
\`\`\`go\r
fmt.Print("Введите срок выполнения: ")\r
deadline := readLine(reader)\r
\`\`\`\r
\r
При создании задачи заполните поле:\r
\r
\`\`\`go\r
task := Task{\r
    Title:    title,\r
    Priority: priority,\r
    Deadline: deadline,\r
}\r
\`\`\`\r
\r
Затем измените метод \`Summary\`, чтобы он включал срок выполнения:\r
\r
\`\`\`go\r
return fmt.Sprintf("%s — %s, приоритет: %s, срок: %s",\r
    task.Title,\r
    status,\r
    task.PriorityText(),\r
    task.Deadline,\r
)\r
\`\`\`\r
\r
Если срок пустой, можно выводить текст \`"без срока"\`.\r
\r
## Вопросы для самопроверки\r
\r
1. Что такое метод?\r
2. Чем метод отличается от обычной функции?\r
3. Что такое получатель метода?\r
4. Где в объявлении метода записывается получатель?\r
5. Почему метод \`PriorityText\` использует получатель по значению?\r
6. Почему метод \`Complete\` использует получатель по указателю?\r
7. Что произойдёт, если метод, изменяющий задачу, сделать с получателем по значению?\r
8. Почему метод можно вызывать через точку?\r
9. Почему \`tasks[index].Complete()\` изменяет задачу в срезе?\r
10. Какие методы в программе только читают данные задачи?\r
11. Какие методы изменяют задачу?\r
12. Почему не все функции нужно превращать в методы?\r
13. Что такое интерфейс?\r
14. Что описывает интерфейс: поля структуры или методы?\r
15. Почему \`Task\` реализует интерфейс \`Summary\`?\r
16. Нужно ли в Go явно писать, что тип реализует интерфейс?\r
17. Почему функция \`printSummary\` может принимать \`Task\`?\r
18. В чём преимущество интерфейса как параметра функции?\r
`,w=Object.assign({"../content/practice/practice-2.2.md":x,"../content/practice/practice-2.3.md":y,"../content/practice/practice-2.4.md":b,"../content/practice/practice-2.5.md":h,"../content/practice/practice-2.6.md":S,"../content/practice/practice-2.7.md":I,"../content/practice/practice-2.8.md":D,"../content/practice/practice-2.9.md":v});function f(t){return typeof t=="string"?t:Array.isArray(t)?t.map(f).join(""):t?.props?.children?f(t.props.children):""}function C({children:t,title:e="Код"}){const[i,a]=c.useState(!1),s=f(t).replace(/\n$/,""),o=()=>{navigator.clipboard.writeText(s),a(!0),setTimeout(()=>{a(!1)},1500)},n=e==="Вывод";return r.jsxs("div",{className:"my-5 overflow-hidden rounded-lg border border-base-300 bg-base-100",children:[r.jsxs("div",{className:"flex items-center justify-between bg-base-200 px-4 py-2 text-sm",children:[r.jsx("span",{className:"text-base-content/60",children:e}),!n&&r.jsx("button",{type:"button",onClick:o,className:"text-xs text-base-content/70 hover:text-base-content",children:i?"Скопировано":"Скопировать код"})]}),r.jsx("pre",{className:"m-0 overflow-x-auto bg-base-100 p-4 text-sm",children:t})]})}function R({children:t}){return r.jsx("div",{className:"my-5 border-l-4 border-[#5B7FFF] bg-[#5B7FFF]/5 p-4",children:r.jsx("div",{className:"text-sm text-slate-700 [&>p]:m-0",children:t})})}const F=k(function(){const{slug:e}=u(),i=e?.replace("lesson-","practice-"),a=i?`../content/practice/${i}.md`:"",s=w[a],o=e?T(e,"practice"):null;return s?r.jsxs("div",{children:[r.jsx("div",{className:"navbar bg-base-100 border-b border-base-300",children:r.jsx("div",{className:"w-full max-w-4xl mx-auto px-4",children:r.jsx(d,{to:"/",className:"text-sm text-base-content/70 hover:text-base-content",children:"← Назад"})})}),r.jsxs("main",{className:"max-w-4xl mx-auto px-4 py-6",children:[r.jsx("article",{className:"text-base leading-7",children:r.jsx(p,{remarkPlugins:[g],rehypePlugins:[P],components:{h1({children:n}){return r.jsx("h1",{className:"mb-6 text-3xl font-bold leading-tight",children:n})},h2({children:n}){return r.jsx("h2",{className:"mt-10 mb-4 text-2xl font-semibold",children:n})},h3({children:n}){return r.jsx("h3",{className:"mt-7 mb-3 text-xl font-semibold",children:n})},p({children:n}){return r.jsx("p",{className:"my-4 text-base-content/85",children:n})},ul({children:n}){return r.jsx("ul",{className:"my-4 list-disc pl-6 space-y-1",children:n})},li({children:n}){return r.jsx("li",{className:"text-base-content/85",children:n})},hr(){return r.jsx("hr",{className:"my-8 border-base-300"})},pre({children:n}){const l=(n?.props?.className||"").includes("language-output");return r.jsx(C,{title:l?"Вывод":"Код",children:n})},code({className:n,children:m,...l}){return n?.includes("language-")?r.jsx("code",{className:n,...l,children:m}):r.jsx("code",{className:"rounded bg-base-200 px-1.5 py-0.5 text-sm",...l,children:m})},blockquote({children:n}){return r.jsx(R,{children:n})},table({children:n}){return r.jsx("div",{className:"my-6 overflow-x-auto",children:r.jsx("table",{className:"table table-zebra border border-base-300",children:n})})}},children:s})}),o&&r.jsx("div",{className:"mt-10 flex justify-end",children:r.jsx(d,{to:o.path,className:"btn border-[#5B7FFF] bg-[#5B7FFF]/10 text-[#3556D8] hover:border-[#5B7FFF] hover:bg-[#5B7FFF]/15",children:"Далее"})})]})]}):r.jsx("div",{className:"p-6",children:"Практика не найдена"})});export{F as default};
