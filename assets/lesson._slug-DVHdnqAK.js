import{w as f,v as m,p as n,t as u,a as p}from"./chunk-EPOLDU6W-BGD713rL.js";import{M as d,r as g,a as P}from"./stackoverflow-light-B9T71yH0.js";import{g as k}from"./courseNavigation-D6c0ZrpE.js";import"./courseData-BnFNstkl.js";const x="# Глоссарий по языку программирования Go\r\n\r\nГлоссарий содержит основные термины, используемые при изучении языка программирования Go. Материал предназначен для быстрого повторения ключевых понятий курса.\r\n\r\n| Термин                         | Определение                                                                                                                                                                                        |\r\n| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\r\n| Go                             | Компилируемый язык программирования, разработанный в компании Google. Отличается простой синтаксической структурой, строгой типизацией и встроенными средствами для создания эффективных программ. |\r\n| Программа                      | Набор инструкций, записанных на языке программирования и выполняемых компьютером для решения определённой задачи.                                                                                  |\r\n| Исходный код                   | Текст программы, написанный разработчиком на языке программирования. В Go файлы исходного кода обычно имеют расширение `.go`.                                                                      |\r\n| Пакет                          | Способ организации кода в Go. Каждый файл программы начинается с объявления пакета с помощью ключевого слова `package`.                                                                            |\r\n| `package main`                 | Объявление основного пакета программы. Используется в исполняемых программах, которые могут быть запущены пользователем.                                                                           |\r\n| Импорт                         | Подключение внешнего или стандартного пакета к программе с помощью ключевого слова `import`.                                                                                                       |\r\n| Стандартная библиотека         | Набор встроенных пакетов Go, которые можно использовать без дополнительной установки. Например, `fmt`, `math`, `strings`, `time`.                                                                  |\r\n| `fmt`                          | Пакет стандартной библиотеки Go, предназначенный для форматированного ввода и вывода данных.                                                                                                       |\r\n| `fmt.Println`                  | Функция пакета `fmt`, которая выводит данные в консоль и добавляет переход на новую строку.                                                                                                        |\r\n| Функция                        | Именованный блок кода, который выполняет определённое действие. В Go функция объявляется с помощью ключевого слова `func`.                                                                         |\r\n| `func main()`                  | Главная функция программы. Именно с неё начинается выполнение исполняемой Go-программы.                                                                                                            |\r\n| Переменная                     | Именованная область памяти, предназначенная для хранения значения, которое может изменяться во время выполнения программы.                                                                         |\r\n| Константа                      | Именованное значение, которое задаётся один раз и не может быть изменено в процессе выполнения программы.                                                                                          |\r\n| Тип данных                     | Характеристика значения, определяющая, какие данные могут храниться в переменной и какие операции с ними допустимы.                                                                                |\r\n| Строгая типизация              | Особенность языка, при которой переменная имеет определённый тип, а операции между несовместимыми типами требуют явного преобразования.                                                            |\r\n| `int`                          | Целочисленный тип данных, предназначенный для хранения целых чисел.                                                                                                                                |\r\n| `float64`                      | Числовой тип данных, предназначенный для хранения вещественных чисел с плавающей точкой.                                                                                                           |\r\n| `string`                       | Тип данных, предназначенный для хранения строковой информации.                                                                                                                                     |\r\n| `bool`                         | Логический тип данных, принимающий одно из двух значений: `true` или `false`.                                                                                                                      |\r\n| Оператор присваивания          | Оператор `=`, который используется для записи значения в переменную.                                                                                                                               |\r\n| Краткое объявление переменной  | Способ объявления и инициализации переменной с помощью оператора `:=`.                                                                                                                             |\r\n| Преобразование типа            | Явное изменение типа значения. В Go преобразование выполняется с помощью записи вида `тип(значение)`.                                                                                              |\r\n| Условная конструкция           | Конструкция, позволяющая выполнять разные участки кода в зависимости от выполнения условия. В Go используется `if`, `else if` и `else`.                                                            |\r\n| `if`                           | Ключевое слово, с помощью которого задаётся проверяемое условие. Если условие истинно, выполняется соответствующий блок кода.                                                                      |\r\n| `else if`                      | Дополнительная проверка условия, которая выполняется, если предыдущее условие оказалось ложным.                                                                                                    |\r\n| `else`                         | Блок кода, который выполняется в том случае, если все предыдущие условия оказались ложными.                                                                                                        |\r\n| `switch`                       | Конструкция выбора, позволяющая сравнивать значение с несколькими вариантами и выполнять соответствующий блок кода.                                                                                |\r\n| Цикл                           | Конструкция, позволяющая многократно выполнять один и тот же блок кода. В Go для организации циклов используется ключевое слово `for`.                                                             |\r\n| `for`                          | Единственная циклическая конструкция в Go. Может использоваться как обычный цикл со счётчиком, как цикл с условием или как бесконечный цикл.                                                       |\r\n| Массив                         | Коллекция элементов одного типа с фиксированной длиной. Размер массива является частью его типа.                                                                                                   |\r\n| Срез                           | Гибкая структура данных, представляющая последовательность элементов одного типа. В отличие от массива, длина среза может изменяться.                                                              |\r\n| Карта                          | Структура данных, предназначенная для хранения пар «ключ — значение». В Go карты объявляются с помощью типа `map`.                                                                                 |\r\n| Ключ                           | Значение, по которому осуществляется доступ к элементу карты.                                                                                                                                      |\r\n| Значение                       | Данные, связанные с определённым ключом в карте или хранящиеся в переменной.                                                                                                                       |\r\n| Указатель                      | Переменная, которая хранит адрес другой переменной в памяти.                                                                                                                                       |\r\n| Адрес переменной               | Место расположения переменной в памяти компьютера. В Go адрес можно получить с помощью оператора `&`.                                                                                              |\r\n| Разыменование                  | Получение значения, находящегося по адресу, который хранится в указателе. В Go выполняется с помощью оператора `*`.                                                                                |\r\n| Параметр функции               | Переменная, указанная в объявлении функции и принимающая значение при её вызове.                                                                                                                   |\r\n| Аргумент функции               | Конкретное значение, передаваемое в функцию при её вызове.                                                                                                                                         |\r\n| Возвращаемое значение          | Результат работы функции, который передаётся обратно в место её вызова с помощью ключевого слова `return`.                                                                                         |\r\n| Множественный возврат          | Возможность функции Go возвращать несколько значений одновременно. Часто используется для возврата результата и ошибки.                                                                            |\r\n| `return`                       | Ключевое слово, завершающее выполнение функции и возвращающее результат.                                                                                                                           |\r\n| `defer`                        | Ключевое слово, откладывающее выполнение функции до завершения текущей функции. Часто используется для освобождения ресурсов.                                                                      |\r\n| Структура                      | Пользовательский тип данных, объединяющий несколько полей под одним именем. Объявляется с помощью ключевого слова `struct`.                                                                        |\r\n| Поле структуры                 | Отдельная именованная часть структуры, предназначенная для хранения определённого значения.                                                                                                        |\r\n| Метод                          | Функция, связанная с определённым типом данных. В Go метод объявляется как функция с получателем.                                                                                                  |\r\n| Получатель метода              | Специальный параметр, который указывает, с каким типом связан метод.                                                                                                                               |\r\n| Интерфейс                      | Тип, задающий набор методов. Значение считается реализующим интерфейс, если его тип содержит все необходимые методы.                                                                               |\r\n| Модуль                         | Способ организации проекта и управления зависимостями в Go. Модуль создаётся с помощью команды `go mod init`.                                                                                      |\r\n| `go.mod`                       | Файл, содержащий имя модуля и информацию о зависимостях проекта.                                                                                                                                   |\r\n| Зависимость                    | Внешний модуль или пакет, который используется в проекте.                                                                                                                                          |\r\n| `go run`                       | Команда, которая компилирует и сразу запускает Go-программу.                                                                                                                                       |\r\n| `go build`                     | Команда, которая компилирует программу и создаёт исполняемый файл.                                                                                                                                 |\r\n| `go fmt`                       | Команда, автоматически приводящая исходный код Go к единому стилю форматирования.                                                                                                                  |\r\n| `go test`                      | Команда для запуска тестов в Go-проекте.                                                                                                                                                           |\r\n| Компиляция                     | Процесс преобразования исходного кода в исполняемую программу.                                                                                                                                     |\r\n| Ошибка компиляции              | Ошибка, обнаруженная до запуска программы, например из-за неправильного синтаксиса или несовместимости типов.                                                                                      |\r\n| Ошибка выполнения              | Ошибка, возникающая во время работы программы.                                                                                                                                                     |\r\n| Комментарий                    | Пояснительный текст в исходном коде, который не выполняется программой. В Go используются однострочные комментарии `//` и многострочные комментарии `/* ... */`.                                   |\r\n| Идентификатор                  | Имя переменной, функции, типа, пакета или другого элемента программы.                                                                                                                              |\r\n| Область видимости              | Часть программы, в которой доступен определённый идентификатор.                                                                                                                                    |\r\n| Экспортируемый идентификатор   | Идентификатор, начинающийся с заглавной буквы. Такой элемент доступен из других пакетов.                                                                                                           |\r\n| Неэкспортируемый идентификатор | Идентификатор, начинающийся со строчной буквы. Такой элемент доступен только внутри своего пакета.                                                                                                 |\r\n| Нулевое значение               | Значение, которое переменная получает по умолчанию, если она объявлена без явной инициализации. Например, для `int` это `0`, для `bool` — `false`, для `string` — пустая строка.                   |\r\n",G=`# 1.1. Роль и место языка Go в современной разработке\r
\r
Язык программирования Go (распространенное название — Golang) был разработан в компании Google и представлен сообществу в 2009 году. Его создателями выступили известные инженеры Роберт Гризмер, Роб Пайк и Кен Томпсон. Проектирование языка велось как ответ на растущую сложность программного обеспечения и инфраструктуры внутри Google. Основной целью было создание инструмента, который сочетал бы в себе производительность компилируемых языков (таких как C++) и простоту разработки и чтения кода, характерную для динамических языков (таких как Python).\r
\r
## Технические особенности и сфера применения\r
\r
Go — это статически типизированный компилируемый язык с открытым исходным кодом. Синтаксически он близок к языку C, однако в нем реализованы современные механизмы, повышающие надежность и скорость разработки:\r
\r
- **Безопасная работа с памятью:** использование безопасных указателей и автоматическое управление памятью (сборка мусора).\r
\r
- **Развитая стандартная библиотека:** Go обладает одной из наиболее полных стандартных библиотек, включающей мощные инструменты для работы с сетевыми протоколами (HTTP/HTTPS), базами данных (SQL), криптографией и обработкой данных.\r
\r
- **Встроенная поддержка параллелизма:** использование механизмов горутин (goroutines) и каналов (channels) позволяет эффективно использовать ресурсы многоядерных процессоров.\r
\r
Наиболее эффективно Go проявляет себя в системном программировании и разработке серверных решений. На сегодняшний день язык стал стандартом де-факто для создания облачных сервисов, микросетевой архитектуры и инструментов контейнеризации (например, Docker и Kubernetes полностью написаны на Go). Благодаря кроссплатформенности, приложения на Go могут разрабатываться в одной операционной системе и развертываться в другой (например, сборка в Windows для запуска в Linux-контейнере) без изменения исходного кода.\r
\r
## Философия и идиоматичность языка\r
\r
Важной характеристикой Go является его «идиоматичность» или наличие строгого мнения разработчиков о том, как должен выглядеть код. В отличие от многих других современных языков, Go сознательно ограничивает набор доступных конструкций, чтобы сделать код предсказуемым и легко читаемым.\r
\r
Для разработчика это выражается в строгой дисциплине:\r
\r
- **Единообразие стиля:** компилятор и встроенные инструменты форматирования (go fmt) пресекают использование нетипичных конструкций, что упрощает командную разработку.\r
\r
- **Обработка ошибок:** в Go отсутствует механизм исключений (try-catch), вместо этого используется явный возврат ошибок как значений функций. Это делает логику приложения более прозрачной, хотя и требует написания дополнительного кода.\r
\r
Несмотря на свою универсальность, Go имеет узкую специализацию. Например, при наличии библиотек для разработки графических интерфейсов (GUI), он значительно уступает в этой области профильным языкам. Go ориентирован прежде всего на высокую производительность серверной логики и сетевого взаимодействия.\r
\r
В рамках данного курса изучение Go позволит освоить принципы создания надежных, масштабируемых систем с использованием современных подходов к написанию кода и автоматизированному тестированию.\r
`,h=`# 1.2. Установка Go и настройка рабочего окружения\r
\r
После рассмотрения ключевых особенностей языка Go перейдём к настройке рабочего окружения, необходимого для начала разработки.\r
\r
Процесс подготовки к разработке на Go включает три основных этапа: установку инструментария (Toolchain), настройку переменных окружения и подготовку интегрированной среды разработки (IDE). Данные этапы формируют базовое рабочее окружение разработчика и обеспечивают корректное функционирование компилятора Go, системных утилит и инструментов разработки. В совокупности это позволяет выполнять сборку, запуск и отладку программ без дополнительной ручной настройки в процессе работы.\r
\r
## Шаг 1. Установка средств разработки\r
\r
Инструментарий Go включает в себя компилятор, стандартную библиотеку и набор утилит для управления проектами. Дистрибутивы для всех операционных систем доступны на официальном сайте go.dev/dl.\r
\r
### 1. Установка в Windows\r
\r
Процесс установки заключается в загрузке установочного файла формата .msi и выполнении стандартных шагов мастера установки. По умолчанию Go устанавливается в каталог C:\\Go.\r
\r
Особенности установки:\r
\r
- установщик автоматически добавляет путь к исполняемым файлам Go в системную переменную PATH;\r
- после завершения установки требуется перезапустить терминал (cmd или PowerShell) для применения изменений.\r
\r
### 2. Установка в macOS\r
\r
Для установки используется пакет формата .pkg, который размещает дистрибутив в каталоге /usr/local/go.\r
\r
Особенности установки:\r
\r
- автоматически добавляется путь /usr/local/go/bin в переменную PATH;\r
- конфигурация оболочки обновляется автоматически.\r
\r
### 3. Установка в Linux\r
\r
В Linux-системах рекомендуется ручная установка для контроля версии:\r
\r
- удалить предыдущие версии (при наличии):\r
\r
\`\`\`bash\r
sudo rm -rf /usr/local/go\r
\`\`\`\r
\r
- распаковать архив в /usr/local:\r
\r
\`\`\`bash\r
sudo tar -C /usr/local -xzf go1.XX.X.linux-amd64.tar.gz\r
\`\`\`\r
\r
- добавить путь в переменные окружения (~/.profile или ~/.bashrc):\r
\r
\`\`\`bash\r
export PATH=$PATH:/usr/local/go/bin\r
\`\`\`\r
\r
### Проверка переменных окружения\r
\r
Для корректной работы Go важно учитывать ключевые переменные окружения:\r
\r
- **PATH** — должен содержать путь к бинарным файлам Go (/usr/local/go/bin или C:\\Go\\bin), что позволяет запускать команду go из любого каталога.\r
\r
- **GOPATH** — рабочая директория для сторонних библиотек и инструментов. По умолчанию:\r
  - Linux/macOS: ~/go\r
  - Windows: %USERPROFILE%\\go\r
\r
Проверка установки выполняется командой:\r
\r
\`\`\`bash\r
go version\r
\`\`\`\r
\r
Если система возвращает версию (например, go version go1.22.0), установка выполнена корректно.\r
\r
## Шаг 2. Выбор среды разработки (IDE)\r
\r
Хотя код на Go можно писать в любом текстовом редакторе, для эффективной разработки рекомендуется использовать специализированные решения, обеспечивающие подсветку синтаксиса, автодополнение (IntelliSense) и инструменты отладки.\r
\r
Наиболее популярными решениями являются:\r
\r
- **Visual Studio Code (VS Code):** Бесплатный редактор с открытым исходным кодом. Для полноценной поддержки Go необходимо установить расширение «Go» (разработчик — Google). Оно обеспечивает интеграцию с инструментами анализа кода и отладки.\r
\r
- **JetBrains GoLand:** Профессиональная специализированная среда разработки.\r
\r
- **Терминал:** Инструментарий Go спроектирован так, что все основные действия (сборка, запуск, тестирование) могут выполняться через командную строку, что важно для автоматизации и CI/CD процессов.\r
\r
## Настройка среды разработки (VS Code)\r
\r
На практике одной из наиболее распространённых сред разработки для Go является Visual Studio Code. Это обусловлено его кроссплатформенностью, низкими системными требованиями и возможностью расширения функциональности через плагины.\r
\r
Ключевым преимуществом VS Code является поддержка Go через официальное расширение, которое интегрирует инструменты анализа кода, форматирования и отладки. Благодаря этому редактор обеспечивает полноценную среду разработки, сопоставимую с профессиональными IDE.\r
\r
### Шаги по настройке:\r
\r
- **Установка VS Code:** загрузите редактор с официального сайта [code.visualstudio.com](https://code.visualstudio.com/) и установите его, следуя стандартному мастеру установки операционной системы.\r
\r
- **Установка расширения Go**\r
\r
Для полноценной работы с языком требуется установить официальное расширение «Go», разработанное компанией Google. Оно обеспечивает интеграцию с инструментами форматирования, анализа кода и отладки.\r
\r
Процесс установки:\r
\r
- перейдите в раздел Extensions (или нажмите сочетание клавиш Ctrl+Shift+X);\r
- в строке поиска введите Go;\r
- выберите расширение «Go» (Go Team at Google);\r
- нажмите Install.\r
\r
![alt](/images/1.2-1.png)\r
![alt](/images/1.2-2.png)\r
\r
После установки расширения VS Code может автоматически предложить установить дополнительные компоненты (tools), необходимые для корректной работы языка.\r
\r
К таким компонентам относятся:\r
\r
- gopls — языковой сервер, обеспечивающий интеллектуальные функции редактора (подсказки, навигацию по коду);\r
- gofmt — инструмент автоматического форматирования кода;\r
- goimports — управление импортируемыми пакетами;\r
- дополнительные инструменты для тестирования и отладки.\r
\r
Их установка является обязательной для полноценного использования среды разработки.\r
\r
- **Первичная настройка расширения**\r
\r
После установки расширения рекомендуется открыть любой файл с расширением .go. При первом запуске VS Code автоматически выполнит проверку окружения и предложит установить недостающие инструменты Go.\r
\r
Если появится уведомление с предложением установить дополнительные зависимости, необходимо подтвердить установку. Это обеспечит корректную работу автодополнения, форматирования и анализа кода.\r
\r
- **Проверка интеграции Go с VS Code**\r
\r
После завершения установки расширений и инструментов важно убедиться, что среда разработки корректно распознаёт установленный Go Toolchain.\r
\r
Для этого можно:\r
\r
открыть встроенный терминал VS Code;\r
\r
выполнить команду проверки версии:\r
\r
\`\`\`bash\r
go version\r
\`\`\`\r
\r
![alt](/images/1.2-3.png)\r
\r
Если команда возвращает установленную версию Go, значит интеграция выполнена корректно.\r
\r
### 4. Открытие рабочего проекта\r
\r
Для начала разработки необходимо создать рабочую папку проекта и открыть её в VS Code через пункт меню **«Open Folder».**\r
\r
Работа именно с папкой, а не с отдельными файлами, важна, поскольку Go использует модульную структуру проектов (Go Modules), и большинство инструментов корректно работают только в контексте проекта.\r
\r
После открытия папки можно создать первый файл с расширением **.go.**\r
\r
![alt](/images/1.2-4.png)\r
\r
Для проверки корректной настройки среды создаётся простой тестовый файл, например example.go, содержащий минимальную программу:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func main() {\r
    fmt.Println("Hello, World!")\r
}\r
\`\`\`\r
\r
![alt](/images/1.2-5.png)\r
\r
После запуска программы через терминал или встроенные инструменты VS Code можно убедиться, что среда разработки настроена корректно и готова к дальнейшей работе. В качестве ожидаемого результата выполнения программы в терминале должно быть отображено сообщение **Hello, World!,** что подтверждает корректную установку Go, работоспособность компилятора и правильную конфигурацию рабочего окружения.\r
`,v=`# 2.1 Инструменты разработчика и запуск первой программы\r
\r
После настройки рабочего окружения и успешного запуска первой программы на языке Go рассмотрим основные инструменты разработчика, используемые в процессе работы.\r
\r
## 1. Использование команды go\r
\r
Всё взаимодействие с языком Go происходит через одну мощную команду в терминале (командной строке) — команду go. Это своего рода швейцарский нож разработчика.\r
В зависимости от того, какое слово мы напишем после go, инструмент выполнит разные задачи. В этом уроке мы познакомимся с самыми важными:\r
\r
Таблица 2-1 Используемые аргументы в команде go\r
\r
| Аргумент | Описание                                                                                                                                                                                                        |\r
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\r
| build    | Команда go build компилирует исходный код в текущем каталоге и создает исполняемый файл, как описано в разделе «Компиляция и запуск исходного кода».                                                            |\r
| clean    | Команда go clean удаляет выходные данные, созданные командой go build, включая исполняемый файл и любые временные файлы, созданные во время сборки, как описано в разделе «Компиляция и запуск исходного кода». |\r
| doc      | Команда go doc генерирует документацию из исходного кода. Смотрите простой пример в разделе «Линтинг кода Go».                                                                                                  |\r
| fmt      | Команда go fmt обеспечивает согласованный отступ и выравнивание в файлах исходного кода, как описано в разделе «Форматирование кода Go».                                                                        |\r
| get      | Команда go get загружает и устанавливает внешние пакеты, как описано в главе 12.                                                                                                                                |\r
| install  | Команда go install загружает пакеты и обычно используется для установки пакетов инструментов, как показано в разделе «Отладка кода Go».                                                                         |\r
| help     | Команда go help отображает справочную информацию по другим функциям Go. Например, команда go help build отображает информацию об аргументе build.                                                               |\r
| mod      | Команда go mod используется для создания модуля Go и управления им, как показано в разделе «Определение модуля» и более подробно описано в главе 12.                                                            |\r
| run      | Команда go run создает и выполняет исходный код в указанной папке без создания исполняемого вывода, как описано в разделе «Использование команды go run».                                                       |\r
| test     | Команда go test выполняет модульные тесты, как описано в главе 31.                                                                                                                                              |\r
| version  | Команда go version выводит номер версии Go.                                                                                                                                                                     |\r
| vet      | Команда go vet обнаруживает распространенные проблемы в коде Go, как описано в разделе «Устранение распространенных проблем в коде Go».                                                                         |\r
\r
## 2. Определение модуля (go mod init)\r
\r
Любой современный проект на Go начинается с создания **модуля**. Модуль — это то, что объединяет ваши файлы с кодом и позволяет в будущем подключать сторонние библиотеки.\r
\r
Откройте терминал, создайте пустую папку для нашего проекта (например, hello-go), перейдите в неё и выполните команду:\r
\r
\`\`\`bash\r
go mod init hello-go\r
\`\`\`\r
\r
Эта команда создаст в папке специальный файлик go.mod. Можете считать его "паспортом" вашего проекта. Пока нам не нужно знать о нём больше — просто привыкните всегда начинать новый проект с этой команды.\r
\r
## 3. Создание проекта Go\r
\r
Теперь напишем саму программу. Создайте в этой же папке файл с именем main.go и добавьте в него следующий код:\r
\r
![alt](/images/2.1-1.png)\r
\r
Мы будем подробно изучать язык Go в последующих уроках, но для начала давайте разберем ключевые элементы этого базового файла.\r
\r
### Объявление пакета (package)\r
\r
Каждый файл с кодом на Go обязан начинаться с объявления пакета — это первое, что видит компилятор. Пакеты служат для группировки связанных функций и типов в единый модуль.\r
\r
Синтаксис: ключевое слово package, за которым следует имя пакета.\r
\r
![alt](/images/2.1-2.png)\r
\r
В нашем случае пакет называется main. Это зарезервированное имя: именно пакет main компилятор Go считает точкой входа программы. Только в пакете main можно объявить функцию main(), с которой начнётся выполнение. Все остальные пакеты — это библиотеки, которые можно подключать, но нельзя запустить напрямую.\r
\r
### Подключение зависимостей (import)\r
\r
Следующий оператор — это import. Оператор import сообщает компилятору, какие внешние пакеты нужны этому файлу. Без объявления импорта использовать чужой код нельзя.\r
\r
![alt](/images/2.1-3.png)\r
\r
Пакет fmt (сокращение от format) — стандартная библиотека Go для форматированного ввода и вывода. Она содержит функции для печати текста в консоль, форматирования строк и чтения пользовательского ввода. Полный список встроенных пакетов доступен на https://pkg.go.dev/std\r
\r
Если нужно подключить несколько пакетов, их можно сгруппировать в один блок со скобками — это стандартный стиль в Go:\r
\r
![alt](/images/2.1-4.png)\r
\r
В языке Go имя пакета при импорте определяется последним сегментом пути, указанного в import.\r
\r
Если путь состоит из одного сегмента, например "fmt", то имя пакета совпадает с этим сегментом — fmt.\r
\r
Если путь содержит несколько сегментов, например "math/rand", то именем пакета считается только последний сегмент — rand. Именно это имя используется при обращении к функциям пакета, например: rand.Int().\r
\r
### Определение функции (func main)\r
\r
Остальная часть файла — это определение функции main. Разберём структуру по элементам:\r
\r
![alt](/images/2.1-5.png)\r
\r
- **func** — ключевое слово, которое объявляет новую функцию.\r
\r
- **main** — имя функции. Оно выбрано не случайно — подробнее об этом в следующем подразделе.\r
\r
- **()** — пустые круглые скобки означают, что функция не принимает параметров (входных данных).\r
\r
- Отсутствие типа после **()** означает, что функция ничего не возвращает. В Go возвращаемый тип указывается явно — если его нет, функция завершается без результата.\r
\r
- **{ }** — фигурные скобки образуют тело функции. Все операторы внутри них выполняются при вызове функции.\r
\r
### Почему именно main?\r
\r
Когда Go-компилятор собирает исполняемую программу, ему нужно знать, с какого места начать выполнение. Для этого используется соглашение: точкой входа считается функция с именем main, объявленная в пакете с именем main. Операционная система запускает именно её — всё остальное вызывается уже из неё.\r
\r
Два обязательных условия: (1) файл должен принадлежать пакету main, и (2) в этом пакете должна быть функция main. Если выполнено только одно из них — компилятор не создаст исполняемый файл.\r
\r
\`\`\`go\r
// Компилятор ищет именно это сочетание:\r
package main   // ← пакет называется main\r
func main() {  // ← функция называется main\r
    // выполнение начинается здесь\r
}\r
\`\`\`\r
\r
Если вы пишете не программу, а библиотеку (пакет для переиспользования другими), то пакет называется не main, а произвольным именем, и функции main в нём нет. Такой пакет нельзя запустить напрямую — он только подключается другими программами.\r
\r
## 4. Выполнение команды (оператор кода)\r
\r
Внутри функции main у нас находится всего одна команда:\r
\r
![alt](/images/2.1-6.png)\r
\r
После импорта пакета вы получаете доступ ко всем его экспортируемым функциям. Доступ к ним осуществляется через имя пакета и точку.\r
\r
fmt.Println — функция, которая выводит переданную строку в стандартный вывод (консоль) и добавляет символ перевода строки в конце. Строка "Hello, Go" — это аргумент, который передаётся функции.\r
\r
### Важное отступление: Точки с запятой в коде Go\r
\r
В Go точки с запятой (;) технически необходимы для завершения каждого оператора — так устроена грамматика языка. Однако писать их вручную не нужно: компилятор Go автоматически вставляет точку с запятой в конце строки во время лексического анализа (до компиляции), если строка заканчивается определёнными токенами — именем, числом, закрывающей скобкой и т.д.\r
\r
Именно из-за этой автоматики в Go есть жёсткое правило: открывающая фигурная скобка { должна стоять на той же строке, что и объявление функции или блока. Если перенести её на новую строку:\r
\r
\`\`\`go\r
func main()\r
{\r
    fmt.Println("Hello, Go")\r
}\r
\`\`\`\r
\r
Компилятор вставит точку с запятой после main() и получит синтаксически неверный код:\r
\r
\`\`\`go\r
func main();  // ← точка с запятой вставлена автоматически\r
{\r
    fmt.Println("Hello, Go")\r
}\r
\`\`\`\r
\r
Это вызовет ошибку: unexpected semicolon or newline, expecting { after function declaration. Сообщение сразу понятно, если знать о механизме автоматической вставки.\r
\r
## 4. Форматирование кода Go (go fmt)\r
\r
Разобравшись с тем, как Go читает код, самое время поговорить о том, как он хочет, чтобы этот код выглядел. В большинстве языков стиль оформления — дело вкуса: кто-то ставит пробел перед скобкой, кто-то нет, и команды часами спорят об отступах. В Go этой проблемы нет вообще.\r
\r
В языке есть официальный стандарт форматирования, и инструмент go fmt автоматически приводит любой код к этому стандарту. Неровные отступы, лишние пробелы, случайные точки с запятой — всё это исчезает одной командой.\r
\r
Представьте, что вы написали вот такой код — второпях, не думая об оформлении:\r
\r
\`\`\`go\r
func main() {\r
fmt.Println("Привет, Go!")\r
  fmt.Println("Вторая строка")\r
}\r
\`\`\`\r
\r
Вместо того чтобы выравнивать отступы вручную, просто запустите в терминале:\r
\r
\`\`\`bash\r
go fmt .\r
\`\`\`\r
\r
Точка в конце означает «применить ко всем файлам в текущей папке». go fmt пробежится по каждому файлу и молча приведёт код в порядок. Результат:\r
\r
\`\`\`go\r
func main() {\r
	fmt.Println("Привет, Go!")\r
	fmt.Println("Вторая строка")\r
}\r
\`\`\`\r
\r
> Совет: большинство современных редакторов — VS Code, GoLand, Vim с плагинами — запускают go fmt автоматически при каждом сохранении файла. Настройте это один раз и забудьте об отступах навсегда.\r
\r
## 5. Быстрый запуск кода: команда go run\r
\r
Код написан, отформатирован — теперь хочется его запустить. Самый быстрый способ сделать это во время разработки — команда go run. Она компилирует программу прямо на лету, запускает и тут же удаляет временные файлы. Никаких лишних шагов.\r
\r
Находясь в папке с файлом main.go, введите:\r
\r
\`\`\`bash\r
go run main.go\r
\`\`\`\r
\r
Или, если хотите запустить сразу все файлы в текущей папке:\r
\r
\`\`\`bash\r
go run .\r
\`\`\`\r
\r
В обоих случаях вы увидите в консоли:\r
\r
\`\`\`\r
Hello, Go\r
\`\`\`\r
\r
go run — ваш главный инструмент в процессе учёбы. Написали строчку — запустили — увидели результат. Именно так и работает цикл разработки на Go.\r
\r
> Что происходит под капотом: Go компилирует исходный код во временную папку, запускает получившийся бинарный файл и удаляет его после завершения. Для вас это выглядит как мгновенный запуск, но технически это полноценная компиляция — просто скрытая.\r
\r
## 6. Компиляция: создание готовой программы (go build)\r
\r
go run удобен пока вы разрабатываете, но однажды вы захотите поделиться программой с кем-то, у кого Go не установлен. Или просто запустить её без лишних команд — двойным кликом. Для этого код нужно скомпилировать в самостоятельный исполняемый файл.\r
\r
Введите в терминале:\r
\r
\`\`\`bash\r
go build\r
\`\`\`\r
\r
Go скомпилирует всё в текущей папке и создаст исполняемый файл. Имя файла совпадает с именем папки проекта: если ваша папка называется tools, на Windows появится tools.exe, на macOS и Linux — просто tools.\r
\r
Запустите его напрямую из терминала:\r
\r
\`\`\`bash\r
./tools      # macOS / Linux\r
tools.exe    # Windows\r
\`\`\`\r
\r
Результат тот же:\r
\r
\`\`\`\r
Hello, Go\r
\`\`\`\r
\r
> Разница между go run и go build: go run — для разработки: быстро, без лишних файлов. go build — для распространения: создаёт файл, который можно передать другому человеку или задеплоить на сервер без установки Go.\r
`,b='# 2.2. Основные типы, константы и переменные\r\n\r\nВ этой главе начинается настоящее знакомство с языком Go. Мы разберём базовые типы данных — те кирпичики, из которых строится любая программа: числа, строки, логические значения. Затем посмотрим, как хранить данные в константах и переменных, и наконец разберёмся с указателями — темой, которая пугает многих, кто пришёл из Java или C#, но на самом деле в Go устроена просто и безопасно.\r\n\r\n## Код примера для этой главы\r\n\r\nВ примерах этой главы мы будем работать со следующим стартовым файлом main.go. Создайте папку basicFeatures, инициализируйте модуль командой `go mod init basicfeatures` и добавьте файл:\r\n\r\n```go\r\npackage main\r\n\r\nimport (\r\n	"fmt"\r\n	"math/rand"\r\n)\r\n\r\nfunc main() {\r\n	fmt.Println(rand.Int())\r\n}\r\n```\r\n\r\nЗапустите командой `go run .` — вы увидите большое случайное число. Это нормально: именно так работает `math/rand` без явной установки зерна генератора.\r\n\r\n> Заметьте: вывод всегда будет одинаковым при каждом запуске — это особенность Go начиная с версии 1.20.\r\n\r\n## 1. Основные типы данных\r\n\r\nGo — строго типизированный язык. Это значит, что каждая переменная имеет конкретный тип, и компилятор не позволит случайно смешать несовместимые значения. Вот основные типы, которые вы будете использовать постоянно:\r\n\r\n| Имя                       | Описание                                                                                                                                                                                                                                                                                         |\r\n| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\r\n| `int`                     | Представляет целое число (положительное или отрицательное). Размер зависит от платформы (32 или 64 бита). Существуют также целые типы, которые имеют определённый размер, например `int8`, `int16`, `int32` и `int64`, но следует использовать тип `int`, если вам не нужен определённый размер. |\r\n| `uint`                    | Беззнаковое положительное целое число. Также имеет размер 32 или 64 бита. Существуют также целочисленные типы без знака, которые имеют определённый размер, например `uint8`, `uint16`, `uint32` и `uint64`, но следует использовать тип `uint`, если вам не нужен определённый размер.          |\r\n| `byte`                    | Псевдоним для `uint8`, обычно используется для представления байта данных.                                                                                                                                                                                                                       |\r\n| `float32`, `float64`      | Числа с плавающей точкой (дробные числа). Выделяют 32 или 64 бита памяти.                                                                                                                                                                                                                        |\r\n| `complex64`, `complex128` | Числа с действительной и мнимой частями (используются редко, в основном в математике и инженерии).                                                                                                                                                                                               |\r\n| `bool`                    | Логический тип. Принимает только значения `true` (истина) или `false` (ложь).                                                                                                                                                                                                                    |\r\n| `string`                  | Последовательность текстовых символов (строка).                                                                                                                                                                                                                                                  |\r\n| `rune`                    | Представляет один символ Unicode. Является псевдонимом для `int32`.                                                                                                                                                                                                                              |\r\n\r\n## 2. Литеральные значения\r\n\r\nЛитерал — это значение, написанное прямо в коде: число, строка, `true` или `false`. Компилятор сам определяет тип на основе того, как выглядит значение — указывать тип явно не нужно.\r\n\r\n![alt](/images/2.2-1.png)\r\n\r\n> **Подсказка:** если импортировать пакет и не использовать его, компилятор выдаст ошибку. Неиспользуемый импорт можно временно закомментировать: `// "math/rand"`\r\n\r\n## 3. Константы\r\n\r\nПрограмма — это не просто набор команд, это работа с данными. Данные нужно где-то хранить и как-то к ним обращаться. Самый простой способ — дать значению имя. Тогда вместо того, чтобы писать одно и то же число в десяти местах, вы пишете его один раз и даёте ему понятное название.\r\n\r\nНо зачем вообще давать значению имя, если можно просто написать число прямо в коде? Во-первых, имя объясняет смысл: `0.2` ничего не говорит, а `TAX_RATE` сразу понятно. Во-вторых, если ставка изменится — достаточно поменять одну строку в начале файла, а не искать число `0.2` по всей программе и гадать, то ли это налог, то ли скидка, то ли что-то ещё.\r\n\r\nПредставьте, что в начале года вы записали ставку налога на бумажке и приклеили её на стену. Весь год вы смотрите на неё и используете в расчётах. Менять её нельзя — это фиксированное правило. Вот что такое константа: имя, намертво привязанное к значению.\r\n\r\n### Типизированные константы\r\n\r\nКонстанта объявляется ключевым словом `const`, за которым следует имя, тип и значение. После объявления изменить значение константы невозможно — компилятор выдаст ошибку при любой попытке присваивания.\r\n\r\n![alt](/images/2.2-2.png)\r\n\r\n### Нетипизированные константы\r\n\r\nGo строго следит за типами и не смешивает их автоматически. Большинство языков программирования автоматически преобразовали бы типы в выражении — Go этого не делает. Попытка использовать `int` и `float32` в одном выражении вызовет ошибку:\r\n\r\n```go\r\nconst price float32 = 275.00\r\nconst tax float32 = 27.50\r\nconst quantity int = 2\r\nfmt.Println("Total:", quantity * (price + tax))\r\n// Ошибка: invalid operation: mismatched types int and float32\r\n```\r\n\r\nРешение — объявить `quantity` без типа. Нетипизированная константа не привязана к конкретному типу: компилятор гибко адаптирует её под тот тип, который нужен в конкретном выражении. Если опустить тип при объявлении, компилятор сам выведет его из значения:\r\n\r\n```go\r\nconst quantity = 2  // нет типа — нетипизированная константа\r\nfmt.Println("Total:", quantity * (price + tax))\r\n```\r\n\r\n```output\r\nTotal: 605\r\n```\r\n\r\n> **Важно:** нетипизированная константа будет преобразована только если значение вписывается в целевой тип. Целое число легко становится `float32`. Но между несовместимыми типами — например `string` и `int` — автоматического преобразования не будет.\r\n\r\n### Несколько констант в одном объявлении\r\n\r\nНесколько констант можно объявить одним оператором, перечислив имена и значения через запятую. Если тип указан — все константы получат этот тип. Если тип опущен — каждая будет нетипизированной:\r\n\r\n```go\r\nconst price, tax float32 = 275, 27.50\r\nconst quantity, inStock = 2, true\r\nfmt.Println("Total:", quantity * (price + tax))\r\nfmt.Println("In stock:", inStock)\r\n```\r\n\r\n```output\r\nTotal: 605\r\nIn stock: true\r\n```\r\n\r\n## 4. Переменные\r\n\r\nКогда программа работает, она постоянно что-то вычисляет и получает новые данные — результаты расчётов, ввод пользователя, ответы от сервера. Чтобы не потерять эти данные и использовать их позже, нужно где-то их хранить. Для этого и существуют переменные.\r\n\r\nРазница между константой и переменной — в том, можно ли поменять значение после того, как оно было задано. Константа — это замок на коробке: значение записано раз и навсегда. Переменная — коробка без замка: значение можно достать и положить другое.\r\n\r\nПредставьте коробку с наклейкой. На наклейке написано «цена». Внутри коробки лежит листок с числом 275. Когда вам нужна цена — вы берёте коробку с наклейкой «цена», а не ищете нужное число среди сотни других. Именно так работает переменная: имя — это наклейка, значение — это то, что лежит внутри. И в отличие от константы, содержимое коробки можно заменить в любой момент.\r\n\r\n![alt](/images/2.2-3.png)\r\n\r\n### Объявление переменных\r\n\r\nПеременные объявляются ключевым словом `var`. Синтаксис: `var имя тип = значение`. В отличие от константы, значение переменной можно изменить в любой момент с помощью оператора присваивания `=`.\r\n\r\n![alt](/images/2.2-4.png)\r\n\r\n### Вывод типа\r\n\r\nПисать тип каждый раз необязательно — если начальное значение задано, компилятор выведет тип сам. Значение переменной может быть задано через литерал или через другую переменную:\r\n\r\n```go\r\nvar price = 275.00   // Go выведет float64\r\nvar price2 = price   // тоже float64\r\n```\r\n\r\nЗдесь важна одна тонкость: буквальное число с точкой Go всегда считает `float64`, а не `float32`. Это может привести к неожиданной ошибке, если смешать такую переменную с явно объявленным `float32`:\r\n\r\n```go\r\nvar price = 275.00       // float64 — компилятор вывел из литерала\r\nvar tax float32 = 27.50  // float32 — указан явно\r\nfmt.Println(price + tax)\r\n// Ошибка: invalid operation: mismatched types float64 and float32\r\n```\r\n\r\nВывод типа не делает Go менее строгим — он лишь избавляет от лишнего написания. Правила совместимости типов остаются в силе. Чтобы смешивать такие переменные, нужно явно привести тип — об этом подробнее в следующей главе.\r\n\r\n### Нулевые значения (Zero Values)\r\n\r\nПеременную можно объявить без начального значения — тогда тип указать обязательно. Go автоматически присвоит ей нулевое значение для этого типа. Это удобно: в Go не бывает «мусорных» значений в памяти, как в C или C++.\r\n\r\n```go\r\nvar price float32\r\nfmt.Println(price)  // 0\r\nprice = 275.00\r\nfmt.Println(price)  // 275\r\n```\r\n\r\n```output\r\n0\r\n275\r\n```\r\n\r\n| Тип                           | Нулевое значение     |\r\n| ----------------------------- | -------------------- |\r\n| `int`, `uint`, `byte`, `rune` | `0`                  |\r\n| `float32`, `float64`          | `0`                  |\r\n| `bool`                        | `false`              |\r\n| `string`                      | `""` (пустая строка) |\r\n\r\n### Краткий синтаксис :=\r\n\r\nПисать `var` и тип каждый раз утомительно, особенно когда компилятор всё равно выведет тип сам. Внутри функций есть более короткий способ — оператор `:=`. Он объявляет переменную и сразу присваивает значение, без ключевого слова `var` и без явного типа:\r\n\r\n```go\r\nprice := 275.00\r\nfmt.Println(price)  // 275\r\n```\r\n\r\nС помощью `:=` можно объявить несколько переменных разных типов в одну строку — компилятор выведет тип каждой из её значения:\r\n\r\n```go\r\nprice, tax, inStock := 275.00, 27.50, true\r\nfmt.Println("Total:", price + tax)\r\nfmt.Println("In stock:", inStock)\r\n```\r\n\r\n```output\r\nTotal: 302.5\r\nIn stock: true\r\n```\r\n\r\n> **Важно:** `:=` можно использовать только внутри функций — например, внутри `func main()`. На уровне пакета (вне функций) используйте `var`.\r\n\r\n### Переопределение переменных через :=\r\n\r\nGo обычно запрещает объявлять переменную с тем же именем, которое уже существует в той же области видимости. Попытка сделать это через `var` вызовет ошибку:\r\n\r\n```go\r\nprice, tax, inStock := 275.00, 27.50, true\r\nvar price2, tax = 200.00, 25.00\r\n// Ошибка: tax redeclared in this block\r\n```\r\n\r\nОднако `:=` делает исключение: если хотя бы одна переменная в списке новая — переопределение разрешено, при условии что тип переменной не меняется. Это удобно, когда нужно добавить новую переменную рядом с уже существующей:\r\n\r\n```go\r\nprice, tax, inStock := 275.00, 27.50, true\r\nfmt.Println("Total:", price + tax)\r\n\r\nprice2, tax := 200.00, 25.00  // price2 — новая, tax — переопределяется\r\nfmt.Println("Total 2:", price2 + tax)\r\n```\r\n\r\n```output\r\nTotal: 302.5\r\nIn stock: true\r\nTotal 2: 225\r\n```\r\n\r\n### Пустой идентификатор \\_\r\n\r\nВ Go нельзя объявить переменную и не использовать её — компилятор выдаст ошибку. Это сделано намеренно: неиспользуемые переменные часто признак ошибки или забытого кода. Посмотрим на пример:\r\n\r\n```go\r\nprice, tax, inStock, discount := 275.00, 27.50, true, true\r\nvar salesPerson = "Alice"\r\nfmt.Println("Total:", price + tax)\r\nfmt.Println("In stock:", inStock)\r\n// Ошибка: discount declared but not used\r\n// Ошибка: salesPerson declared but not used\r\n```\r\n\r\n`discount` и `salesPerson` объявлены, но нигде не используются — компилятор это не простит. Иногда удалить переменную нельзя: например, функция возвращает два значения, а вам нужно только одно. Для таких случаев Go предоставляет пустой идентификатор — символ подчёркивания `_`. Он говорит компилятору: «я знаю об этом значении, но оно мне не нужно»:\r\n\r\n```go\r\nprice, tax, inStock, _ := 275.00, 27.50, true, true\r\nvar _ = "Alice"\r\nfmt.Println("Total:", price + tax)\r\nfmt.Println("In stock:", inStock)\r\n```\r\n\r\n```output\r\nTotal: 302.5\r\nIn stock: true\r\n```\r\n\r\n## 5. Ввод и вывод\r\n\r\nПрограмма без ввода и вывода — закрытая коробка: что-то внутри происходит, но снаружи ничего не видно. Чтобы программа общалась с пользователем, нужны две вещи: показать результат на экране и получить данные с клавиатуры. В Go для этого используется стандартный пакет `fmt` — мы уже встречали его в примерах, теперь разберёмся подробнее.\r\n\r\n### Вывод: fmt.Println и fmt.Print\r\n\r\nСамые простые функции вывода — `fmt.Println` и `fmt.Print`. Обе принимают любое количество аргументов любых типов и выводят их в консоль. Разница только в одном: `Println` добавляет перенос строки в конце, `Print` — нет.\r\n\r\n```go\r\nname := "Алиса"\r\nage  := 25\r\nfmt.Println("Имя:", name, "Возраст:", age)\r\nfmt.Print("Имя: ", name, " Возраст: ", age, "\\n")\r\n```\r\n\r\n```output\r\nИмя: Алиса Возраст: 25\r\nИмя: Алиса Возраст: 25\r\n```\r\n\r\n`Println` сам расставляет пробелы между аргументами. `Print` — нет, поэтому пробелы и перенос строки нужно писать вручную, как видно во втором примере. На практике `Println` используется намного чаще: она удобнее и почти всегда делает то, что нужно. В пакете `fmt` есть и более мощная функция — `fmt.Printf` для форматированного вывода — её мы разберём ниже, когда понадобится точный контроль над форматом.\r\n\r\n### Ввод: fmt.Scan и fmt.Scanln\r\n\r\nДля чтения данных с клавиатуры используется `fmt.Scan` или `fmt.Scanln`. В отличие от вывода, здесь есть одна особенность: функции ввода не возвращают значение, а записывают его в уже объявленную переменную. Поэтому им нужно передать не саму переменную, а её адрес — то есть указатель, через оператор `&`:\r\n\r\n```go\r\nvar name string\r\nvar age  int\r\nfmt.Print("Введите имя и возраст: ")\r\nfmt.Scan(&name, &age)\r\nfmt.Println("Привет,", name, "тебе", age, "лет")\r\n```\r\n\r\n```output\r\nВведите имя и возраст: Алиса 25\r\nПривет, Алиса тебе 25 лет\r\n```\r\n\r\n`Scan` разделяет введённые значения по пробельным символам — пробелам, табуляции и переносам строк. То есть значения можно вводить через пробел в одной строке или каждое с новой — для `Scan` это одинаково. Тип каждой переменной известен компилятору, поэтому `Scan` сам понимает, что `"25"` нужно превратить в число.\r\n\r\n> **Подсказка:** `Scanln` отличается от `Scan` тем, что считывает только одну строку — перенос строки служит для неё границей. Если пользователь нажмёт Enter раньше, чем введёт все значения, `Scanln` вернёт ошибку. Удобно, когда вы хотите гарантировать, что все данные попадут в одну строку.\r\n\r\n### Ввод строки с пробелами\r\n\r\nУ `fmt.Scan` есть ограничение: он считает пробел разделителем. Если пользователь введёт «Иван Петров», в переменную попадёт только «Иван» — всё, что после пробела, окажется в следующей переменной или будет проигнорировано. Чтобы прочитать целую строку с пробелами, используется пакет `bufio`:\r\n\r\n```go\r\nimport (\r\n    "bufio"\r\n    "fmt"\r\n    "os"\r\n    "strings"\r\n)\r\n\r\nreader := bufio.NewReader(os.Stdin)\r\nfmt.Print("Введите полное имя: ")\r\nline, _ := reader.ReadString(\'\\n\')\r\nline = strings.TrimSpace(line)\r\nfmt.Println("Привет,", line)\r\n```\r\n\r\n```output\r\nВведите полное имя: Иван Петров\r\nПривет, Иван Петров\r\n```\r\n\r\n`bufio.NewReader` создаёт читатель, обёрнутый вокруг стандартного ввода `os.Stdin`. Метод `ReadString(\'\\n\')` читает всё до символа переноса строки включительно — то есть всю строку целиком, со всеми пробелами. Этот перенос остаётся в конце строки, поэтому его убирают через `strings.TrimSpace`, который заодно срезает и лишние пробелы по краям.\r\n\r\n> **Важно:** все функции ввода возвращают ошибку вторым значением — на случай, если пользователь введёт что-то невалидное (например, букву там, где ожидалось число). В простых примерах её можно игнорировать через `_`, как в коде выше, но в реальных программах ошибки обязательно нужно проверять. Подробнее о работе с ошибками — в следующей главе.\r\n\r\n### Форматированный вывод: fmt.Printf и fmt.Sprintf\r\n\r\n`Println` хорош для быстрого вывода, но у него нет точного контроля над форматом: числа печатаются как есть, пробелы расставлены автоматически. Когда нужно сказать «вот строка-шаблон, подставь сюда значение с двумя знаками после точки, а сюда — целое число», в дело вступает `Printf`. Если вы знакомы с Python, `fmt.Printf` — это аналог f-строк, только синтаксис другой.\r\n\r\n| Функция                          | Что делает                                                      |\r\n| -------------------------------- | --------------------------------------------------------------- |\r\n| `fmt.Println(a, b, ...)`         | Выводит значения через пробел, добавляет перенос строки в конце |\r\n| `fmt.Print(a, b, ...)`           | То же, но без переноса строки в конце                           |\r\n| `fmt.Printf(format, a, b, ...)`  | Форматированный вывод — подставляет значения в шаблон           |\r\n| `fmt.Sprintf(format, a, b, ...)` | То же что `Printf`, но возвращает строку вместо вывода          |\r\n| `fmt.Fprintf(w, format, ...)`    | Форматированный вывод в произвольный поток (файл, сеть)         |\r\n\r\n`fmt.Printf` принимает строку-шаблон с форматными глаголами — специальными местозаполнителями, которые начинаются с `%`. Каждый глагол заменяется соответствующим аргументом:\r\n\r\n```go\r\nprice := 275.00\r\nname  := "каяк"\r\nfmt.Printf("Товар: %s, цена: %.2f руб.\\n", name, price)\r\n```\r\n\r\n```output\r\nТовар: каяк, цена: 275.00 руб.\r\n```\r\n\r\nОбратите внимание на `\\n` в конце — `fmt.Printf` не добавляет перенос строки автоматически, в отличие от `fmt.Println`. Нужно написать его явно.\r\n\r\nОсновные форматные глаголы:\r\n\r\n| Глагол | Тип     | Описание                         | Пример                                    |\r\n| ------ | ------- | -------------------------------- | ----------------------------------------- |\r\n| `%v`   | любой   | Значение в формате по умолчанию  | `fmt.Printf("%v", 42)` → `42`             |\r\n| `%T`   | любой   | Тип значения                     | `fmt.Printf("%T", 42)` → `int`            |\r\n| `%d`   | int     | Целое число в десятичной системе | `fmt.Printf("%d", 42)` → `42`             |\r\n| `%05d` | int     | С ведущими нулями до 5 символов  | `fmt.Printf("%05d", 42)` → `00042`        |\r\n| `%f`   | float   | Число с плавающей точкой         | `fmt.Printf("%f", 3.14)` → `3.140000`     |\r\n| `%.2f` | float   | Два знака после точки            | `fmt.Printf("%.2f", 3.14159)` → `3.14`    |\r\n| `%e`   | float   | Научная нотация                  | `fmt.Printf("%e", 3.14)` → `3.140000e+00` |\r\n| `%s`   | string  | Строка                           | `fmt.Printf("%s", "Go")` → `Go`           |\r\n| `%q`   | string  | Строка в кавычках                | `fmt.Printf("%q", "Go")` → `"Go"`         |\r\n| `%t`   | bool    | Логическое значение              | `fmt.Printf("%t", true)` → `true`         |\r\n| `%p`   | pointer | Адрес указателя                  | `fmt.Printf("%p", &x)` → `0xc000...`      |\r\n\r\n```go\r\nage    := 17\r\nheight := 1.756\r\nname   := "Алиса"\r\nactive := true\r\n\r\nfmt.Printf("Имя: %s\\n", name)\r\nfmt.Printf("Возраст: %d лет\\n", age)\r\nfmt.Printf("Рост: %.2f м\\n", height)\r\nfmt.Printf("Активен: %t\\n", active)\r\nfmt.Printf("Тип age: %T\\n", age)\r\n```\r\n\r\n```output\r\nИмя: Алиса\r\nВозраст: 17 лет\r\nРост: 1.76 м\r\nАктивен: true\r\nТип age: int\r\n```\r\n\r\n### fmt.Sprintf — строка вместо вывода\r\n\r\nИногда нужно не напечатать строку, а получить её как значение — чтобы сохранить в переменную, передать в функцию или объединить с другой строкой. Для этого используется `fmt.Sprintf` — он работает точно так же как `Printf`, но возвращает готовую строку:\r\n\r\n```go\r\nprice := 275.00\r\nname  := "каяк"\r\nlabel := fmt.Sprintf("Товар: %s, цена: %.2f руб.", name, price)\r\nfmt.Println(label)\r\n```\r\n\r\n```output\r\nТовар: каяк, цена: 275.00 руб.\r\n```\r\n\r\n> **Сравнение с Python:** `f"Товар: {name}, цена: {price:.2f} руб."` ≈ `fmt.Sprintf("Товар: %s, цена: %.2f руб.", name, price)`. Синтаксис другой, но идея та же: шаблон + значения = готовая строка.\r\n',y=`# 2.3. Указатели\r
\r
Указатели — одна из тех тем, которые вызывают панику у новичков, особенно если вы пришли из Java или C#, где указатели используются за кулисами, но тщательно скрыты от разработчика. На самом деле в Go указатели — это просто переменные, которые хранят не само значение, а адрес в памяти, где это значение находится. Чтобы понять зачем они нужны, начнём с того, как Go работает без них.\r
\r
## 1. Как Go работает без указателей: копирование значений\r
\r
Когда вы присваиваете переменной значение другой переменной, Go копирует это значение в новую ячейку памяти. После этого переменные полностью независимы:\r
\r
\`\`\`go\r
first := 100\r
second := first\r
first++\r
fmt.Println("First:", first)   // 101\r
fmt.Println("Second:", second) // 100\r
\`\`\`\r
\r
_First: 101_\r
_Second: 100_\r
\r
Go скопировал значение из first в second в момент присваивания. Теперь каждая переменная ссылается на свою отдельную ячейку памяти. Когда мы увеличиваем first с помощью ++, Go считывает значение в её ячейке памяти, прибавляет единицу и сохраняет обратно — ячейка памяти second при этом не затрагивается.\r
\r
Представьте два листка бумаги. На первом написано «100». Когда вы создаёте second := first, вы просто переписываете число на второй листок. После этого листки никак не связаны — что бы вы ни делали с первым, второй листок останется нетронутым.\r
\r
## 2. Что такое указатель и оператор &\r
\r
Указатель — это переменная, значением которой является адрес памяти. Вместо того чтобы хранить само число 100, указатель хранит адрес той ячейки памяти, где это число лежит. Указатель создаётся с помощью оператора & (называется «оператор взятия адреса»):\r
\r
\`\`\`go\r
first := 100\r
var second *int = &first\r
first++\r
fmt.Println("First:", first)   // 101\r
fmt.Println("Second:", second) // 0xc000010088\r
\`\`\`\r
\r
_First: 101_\r
_Second: 0xc000010088_\r
\r
second теперь хранит не число, а адрес в памяти — что-то вроде «0xc000010088». Конкретный адрес у вас будет другим — это зависит от того, куда Go решил поместить переменную first при запуске программы. Важны не сами цифры адреса, а то, что second и first теперь связаны: second знает, где живёт first.\r
\r
![alt](/images/2.3-1.png)\r
\r
Обратите внимание на тип переменной second — он записан как *int. Звёздочка перед типом означает «указатель на». То есть *int — это «указатель на значение типа int». Тип указателя фиксирован: если вы создали указатель на int, использовать его для указания на float64 не получится. В Go указатель — это не просто адрес, а адрес конкретного типа данных.\r
\r
> Про арифметику указателей: в языках C и C++ можно прибавить число к адресу и получить доступ к соседней ячейке памяти. Это мощно, но опасно — можно случайно прочитать чужие данные или выйти за пределы памяти. Go намеренно не поддерживает арифметику указателей: компилятор выдаст ошибку при любой попытке. Это делает Go безопаснее.\r
\r
## 3. Следование указателю: оператор \\*\r
\r
Адрес в памяти сам по себе редко нужен. Гораздо важнее уметь получить само значение по этому адресу. Для этого используется оператор \\* перед именем указателя — это называется разыменованием или следованием указателю:\r
\r
\`\`\`go\r
first := 100\r
second := &first  // тип выведется как *int\r
first++\r
fmt.Println("First:", first)    // 101\r
fmt.Println("Second:", *second) // 101 — не адрес, а само значение\r
\`\`\`\r
\r
_First: 101_\r
_Second: 101_\r
\r
Когда мы пишем \\*second, Go идёт по адресу, который хранится в second, и читает значение из той ячейки памяти. Поскольку second указывает на ту же ячейку, что и first, мы получаем актуальное значение first — уже после увеличения.\r
\r
> Частое заблуждение: кажется, что first и *second — одно и то же. Но это не так. first — переменная типа int со значением 101. second — переменная типа *int, её значение — адрес памяти. *second — операция чтения по этому адресу, результат которой равен 101. Значение *int (адрес) можно присваивать, передавать в функции и использовать самостоятельно.\r
\r
![alt](/images/2.3-2.png)\r
\r
## 4. Изменение значения через указатель\r
\r
Оператор * работает в обе стороны: не только читает значение по адресу, но и позволяет его изменить. Если написать *second++, Go пойдёт по адресу и увеличит значение прямо там:\r
\r
\`\`\`go\r
first := 100\r
second := &first\r
first++\r
*second++\r
fmt.Println("First:", first)    // 102\r
fmt.Println("Second:", *second) // 102\r
\`\`\`\r
\r
_First: 102_\r
_Second: 102_\r
\r
Сначала first++ увеличило значение до 101. Затем \\*second++ — снова увеличило то же самое значение до 102. Оба изменения затрагивают одну и ту же ячейку памяти, потому что second указывает именно на неё.\r
\r
## 5. Указатель как самостоятельное значение\r
\r
Указатель — это обычная переменная. Её значение (адрес) можно присвоить другой переменной-указателю. Тогда оба указателя будут ссылаться на одну и ту же ячейку памяти, и изменение через любой из них затронет то же самое значение:\r
\r
\`\`\`go\r
first := 100\r
second := &first\r
first++\r
*second++\r
\r
var myNewPointer *int\r
myNewPointer = second      // оба указателя смотрят на first\r
*myNewPointer++\r
\r
fmt.Println("First:", first)    // 103\r
fmt.Println("Second:", *second) // 103\r
\`\`\`\r
\r
_First: 103_\r
_Second: 103_\r
\r
myNewPointer получил то же значение, что и second — адрес ячейки памяти переменной first. Теперь все три имени — first, *second и *myNewPointer — обращаются к одному и тому же месту в памяти. Увеличение через myNewPointer увеличивает значение, которое видно через все остальные.\r
\r
## 6. Нулевые указатели: nil\r
\r
Указатель, объявленный без присваивания значения, автоматически получает нулевое значение nil — специальное значение, которое означает «никуда не указывает». Это полезно: можно проверить, был ли указатель инициализирован, прежде чем его использовать:\r
\r
\`\`\`go\r
first := 100\r
var second *int\r
fmt.Println(second)   // <nil>\r
second = &first\r
fmt.Println(second)   // 0xc000010088\r
\`\`\`\r
\r
_\\<nil\\>_\r
_0xc000010088_\r
\r
Пока второй указатель равен nil, его можно напечатать — Println выведет \\<nil\\>. После присваивания адреса он начинает указывать на first. Можно также явно проверить, равен ли указатель nil:\r
\r
\`\`\`go\r
fmt.Println(second == nil)  // false — указатель уже инициализирован\r
\`\`\`\r
\r
Но если попытаться разыменовать nil-указатель — то есть прочитать значение по адресу, которого нет — программа аварийно завершится:\r
\r
\`\`\`go\r
var second *int\r
fmt.Println(*second)  // panic: runtime error: nil pointer dereference\r
\`\`\`\r
\r
> Правило: всегда проверяйте указатель на nil перед разыменованием, если есть вероятность что он не был инициализирован. Это одна из самых частых ошибок при работе с указателями.\r
\r
## 7. Указатель на указатель\r
\r
Поскольку указатель — это обычная переменная, можно создать указатель, который хранит адрес другого указателя. Это называется указатель на указатель:\r
\r
\`\`\`go\r
first := 100\r
second := &first   // *int — указатель на int\r
third := &second   // **int — указатель на указатель на int\r
\r
fmt.Println(first)    // 100\r
fmt.Println(*second)  // 100\r
fmt.Println(**third)  // 100\r
\`\`\`\r
\r
_100_\r
_100_\r
_100_\r
\r
Чтобы добраться до значения через third, нужно применить \\*\\* — две звёздочки. Первая следует за third и получает значение second (адрес first). Вторая следует за этим адресом и получает само значение first. В большинстве проектов указатели на указатели не нужны, но этот пример хорошо показывает как работает цепочка следования.\r
\r
Представьте записку «смотри на доску». На доске написано «смотри на шкаф». В шкафу лежит число 100. Третий — это записка. \\*third — это доска. \\*\\*third — это число в шкафу.\r
\r
## 8. Зачем нужны указатели: передача по значению vs по ссылке\r
\r
Легко потеряться в деталях того, как работают указатели, и упустить из виду главное — зачем они вообще нужны. Указатели дают программисту выбор: копировать данные или работать с оригиналом. Вот конкретный пример, который показывает эту разницу.\r
\r
### Без указателя: работа с копией\r
\r
Создаётся массив из трёх имён. Значение на позиции 1 сохраняется в переменную. Затем массив сортируется. Что произойдёт с переменной?\r
\r
\`\`\`go\r
names := [3]string{"Alice", "Charlie", "Bob"}\r
secondName := names[1]  // копируем значение "Charlie"\r
fmt.Println(secondName)\r
sort.Strings(names[:])\r
fmt.Println(secondName)  // всё ещё "Charlie"?\r
\`\`\`\r
\r
_Charlie_\r
_Charlie_\r
\r
Когда мы написали secondName := names[1], Go скопировал строку "Charlie" в новую ячейку памяти. После этого secondName живёт отдельно от массива. Сортировка переставила элементы в массиве, но копия осталась нетронутой — secondName по-прежнему "Charlie".\r
\r
### С указателем: работа с оригиналом\r
\r
Теперь сохраним не копию значения, а указатель на позицию в массиве:\r
\r
\`\`\`go\r
names := [3]string{"Alice", "Charlie", "Bob"}\r
secondPosition := &names[1]  // указатель на позицию 1\r
fmt.Println(*secondPosition)\r
sort.Strings(names[:])\r
fmt.Println(*secondPosition)  // что теперь?\r
\`\`\`\r
\r
_Charlie_\r
_Bob_\r
\r
На этот раз secondPosition хранит адрес ячейки памяти, которая соответствует позиции 1 в массиве. После сортировки массив выглядит так: ["Alice", "Bob", "Charlie"]. Позиция 1 теперь содержит "Bob". Указатель по-прежнему смотрит на позицию 1 — поэтому \\*secondPosition теперь возвращает "Bob", а не "Charlie".\r
\r
Разница между этими двумя примерами — это вся суть указателей. Без указателя вы работаете с копией: безопасно, но данные устаревают. С указателем вы работаете с оригиналом: актуальные данные, но нужно понимать, кто ещё может их менять.\r
\r
### Когда это важно на практике\r
\r
Указатели нужны в двух основных ситуациях. Первая — когда нужно, чтобы изменения в одном месте были видны в другом: например, при работе с базами данных, общими состояниями, сортировками. Вторая — когда данные большие, и копировать их дорого: передача указателя на большой массив намного дешевле, чем копирование всего массива.\r
\r
В других языках этот выбор делается заранее при создании типа данных. В C# структуры передаются по значению, классы — по ссылке, и это нельзя изменить. Go даёт больше гибкости: вы решаете каждый раз, когда используете значение — копировать или передать ссылку. Это требует большего внимания, но позволяет писать более точный и эффективный код.\r
`,T='# 2.4. Операции и преобразования\r\n\r\nВ предыдущей главе мы научились хранить данные в переменных и константах. Теперь разберёмся, что с этими данными можно делать: складывать числа, сравнивать значения, составлять логические условия и соединять строки. В конце главы разберём одну из самых частых трудностей новичков в Go — почему нельзя просто сложить `int` и `float64`, и как это правильно решается.\r\n\r\n## 1. Арифметические операторы\r\n\r\nАрифметические операторы работают с числовыми типами: `int`, `uint`, `float32`, `float64` и их разновидностями. Все они вам уже знакомы по математике и другим языкам:\r\n\r\n| Оператор | Описание                                                               |\r\n| -------- | ---------------------------------------------------------------------- |\r\n| `+`      | Возвращает сумму двух операндов                                        |\r\n| `-`      | Возвращает разницу между двумя операндами                              |\r\n| `*`      | Возвращает произведение двух операндов                                 |\r\n| `/`      | Возвращает частное двух операндов                                      |\r\n| `%`      | Возвращает остаток от деления; может возвращать отрицательные значения |\r\n\r\n```go\r\nprice, tax := 275.00, 27.40\r\nsum := price + tax\r\ndifference := price - tax\r\nproduct := price * tax\r\nquotient := price / tax\r\nfmt.Println(sum, difference, product, quotient)\r\n```\r\n\r\n```output\r\n302.4 247.6 7535 10.036496350364963\r\n```\r\n\r\n### Операторы инкремента и декремента\r\n\r\nДля увеличения или уменьшения значения на единицу используются `++` и `--`. Для изменения на произвольную величину — `+=` и `-=`. Важная особенность Go: `++` и `--` могут быть только постфиксными — то есть пишутся после переменной. Запись `--value` не поддерживается:\r\n\r\n```go\r\nvalue := 10.2\r\nvalue++        // 11.2\r\nfmt.Println(value)\r\nvalue += 2     // 13.2\r\nfmt.Println(value)\r\nvalue -= 2     // 11.2\r\nfmt.Println(value)\r\nvalue--        // 10.2\r\nfmt.Println(value)\r\n```\r\n\r\n```output\r\n11.2\r\n13.2\r\n11.2\r\n10.2\r\n```\r\n\r\n### Оператор остатка %\r\n\r\nОператор `%` возвращает остаток от деления целого числа на другое. Его часто путают с оператором по модулю из Python или других языков — но есть важное отличие: в Go результат может быть отрицательным, если делимое отрицательное:\r\n\r\n```go\r\nposResult := 3 % 2    //  1\r\nnegResult := -3 % 2   // -1\r\nfmt.Println(posResult)\r\nfmt.Println(negResult)\r\n```\r\n\r\n```output\r\n1\r\n-1\r\n```\r\n\r\nЕсли вам нужно абсолютное значение остатка, используйте функцию `math.Abs` — но учтите, она возвращает `float64`:\r\n\r\n```go\r\nabsResult := math.Abs(float64(negResult))\r\nfmt.Println(absResult)  // 1\r\n```\r\n\r\n### Арифметическое переполнение\r\n\r\nGo не сообщает об ошибке при переполнении числовых значений — он просто переносит значение. Для целых чисел это означает неожиданный результат, для чисел с плавающей точкой — бесконечность. Это важно понимать, особенно при работе с небольшими типами вроде `int8`:\r\n\r\n```go\r\nvar intVal = math.MaxInt64\r\nvar floatVal = math.MaxFloat64\r\nfmt.Println(intVal * 2)                   // -2 (перенос)\r\nfmt.Println(floatVal * 2)                 // +Inf\r\nfmt.Println(math.IsInf(floatVal * 2, 0)) // true\r\n```\r\n\r\n```output\r\n-2\r\n+Inf\r\ntrue\r\n```\r\n\r\n> **Подсказка:** пакет `math` предоставляет константы `MaxInt64`, `MaxFloat64` и другие — они полезны когда нужно проверить граничные значения типов.\r\n\r\n## 2. Конкатенация строк\r\n\r\nОператор `+` умеет не только складывать числа — он также объединяет строки в одну. Это называется конкатенацией:\r\n\r\n```go\r\ngreeting := "Hello"\r\nlanguage := "Go"\r\ncombined := greeting + ", " + language\r\nfmt.Println(combined)\r\n```\r\n\r\n```output\r\nHello, Go\r\n```\r\n\r\nРезультатом оператора `+` для строк всегда является новая строка — исходные переменные при этом не изменяются.\r\n\r\n> **Важно:** Go не объединяет строки с числами или другими типами автоматически. Попытка написать `"Total: " + 302.5` вызовет ошибку компилятора. Для составления строк из значений разных типов используется `fmt.Println` или пакет `strconv` — о нём в конце главы.\r\n\r\n## 3. Операторы сравнения\r\n\r\nОператоры сравнения сравнивают два значения и возвращают `bool` — `true` если условие выполнено, `false` если нет. Результат можно сохранить в переменную или использовать напрямую в условии:\r\n\r\n| Оператор | Описание         |\r\n| -------- | ---------------- |\r\n| `==`     | Равно            |\r\n| `!=`     | Не равно         |\r\n| `<`      | Меньше           |\r\n| `<=`     | Меньше или равно |\r\n| `>`      | Больше           |\r\n| `>=`     | Больше или равно |\r\n\r\n```go\r\nfirst := 100\r\nconst second = 200.00  // нетипизированная константа\r\n\r\nfmt.Println(first == second)  // false\r\nfmt.Println(first != second)  // true\r\nfmt.Println(first < second)   // true\r\nfmt.Println(first <= second)  // true\r\nfmt.Println(first > second)   // false\r\nfmt.Println(first >= second)  // false\r\n```\r\n\r\n```output\r\nfalse\r\ntrue\r\ntrue\r\ntrue\r\nfalse\r\nfalse\r\n```\r\n\r\nОбратите внимание: `second` — нетипизированная константа со значением `200.00`. Несмотря на то что это число с плавающей точкой, оно может быть представлено как целое (дробная часть равна нулю), поэтому сравнение с `int` работает. Значение `200.01` уже не сработало бы — потребовалось бы явное преобразование.\r\n\r\n### Сравнение указателей\r\n\r\nУказатели тоже можно сравнивать с помощью `==`. Но здесь важно понимать: сравниваются не значения, а адреса памяти. Два указателя равны только если они указывают на одну и ту же ячейку:\r\n\r\n```go\r\nfirst := 100\r\nsecond := &first\r\nthird := &first\r\nalpha := 100\r\nbeta := &alpha\r\n\r\nfmt.Println(second == third)  // true  — оба указывают на first\r\nfmt.Println(second == beta)   // false — beta указывает на alpha\r\n```\r\n\r\n```output\r\ntrue\r\nfalse\r\n```\r\n\r\n`second` и `third` оба созданы из `&first` — они хранят один и тот же адрес памяти. `beta` создан из `&alpha` — это другая переменная, другой адрес, даже если значение там тоже `100`. Чтобы сравнить именно значения, нужно разыменовать указатели:\r\n\r\n```go\r\nfmt.Println(*second == *third)  // true\r\nfmt.Println(*second == *beta)   // true — значения одинаковы\r\n```\r\n\r\n```output\r\ntrue\r\ntrue\r\n```\r\n\r\n> **В Go нет тернарного оператора.** В других языках можно написать: `max := a > b ? a : b`. В Go для этого используется `if/else` — это многословнее, но Go намеренно избегает конструкций, которые легко написать нечитаемо.\r\n\r\n## 4. Логические операторы\r\n\r\nЛогические операторы работают только с `bool` значениями и возвращают `bool`. Они используются для составления составных условий — например, «больше 18 И есть водительские права»:\r\n\r\n| Оператор | Название | Описание                                                 |\r\n| -------- | -------- | -------------------------------------------------------- |\r\n| `\\|\\|`   | ИЛИ      | `true` если хотя бы один операнд `true`                  |\r\n| `&&`     | И        | `true` только если оба операнда `true`                   |\r\n| `!`      | НЕ       | инвертирует значение: `true` → `false`, `false` → `true` |\r\n\r\n```go\r\nmaxMph := 50\r\npassengerCapacity := 4\r\nairbags := true\r\n\r\nfamilyCar := passengerCapacity > 2 && airbags\r\nsportsCar := maxMph > 100 || passengerCapacity == 2\r\ncanCategorize := !familyCar && !sportsCar\r\n\r\nfmt.Println(familyCar)      // true\r\nfmt.Println(sportsCar)      // false\r\nfmt.Println(canCategorize)  // false\r\n```\r\n\r\n```output\r\ntrue\r\nfalse\r\nfalse\r\n```\r\n\r\n`familyCar` — `true`, потому что оба условия выполнены: `passengerCapacity > 2` и `airbags` равно `true`. `sportsCar` — `false`, потому что `maxMph` не больше 100, а `passengerCapacity` не равен 2 — ни одно из условий не выполнено. `canCategorize` — `false`, потому что `familyCar` равно `true`, а `!familyCar` превращает его в `false`.\r\n\r\n### Короткое вычисление\r\n\r\nGo вычисляет логические выражения минимально необходимым образом. Для оператора `&&` — если первый операнд `false`, второй не вычисляется вообще: результат уже известен. Для оператора `||` — если первый операнд `true`, второй тоже не вычисляется. Это называется коротким вычислением (short-circuit evaluation). На практике это важно когда второй операнд — дорогостоящая операция или вызов функции, который не стоит выполнять без необходимости.\r\n\r\n> **Важно:** Go не преобразует другие типы в `bool` автоматически. В некоторых языках `0` считается `false`, а любое другое число — `true`. В Go это не работает: логические операторы принимают только `bool` значения, и компилятор выдаст ошибку если передать им `int` или `string`.\r\n\r\n## 5. Почему Go не смешивает типы\r\n\r\nЕсли вы пришли из Python или JavaScript, вас поначалу удивит одна вещь: в Go нельзя просто сложить `int` и `float64`. Компилятор откажется:\r\n\r\n```go\r\nkayak := 275\r\nsoccerBall := 19.50\r\ntotal := kayak + soccerBall\r\n// Ошибка: invalid operation: kayak + soccerBall (mismatched types int and float64)\r\n```\r\n\r\n`kayak` получил тип `int`, потому что литерал `275` без точки — целое число. `soccerBall` получил тип `float64`, потому что `19.50` — число с плавающей точкой. Go не преобразует их автоматически, даже если это кажется очевидным.\r\n\r\nЭто не каприз языка — это осознанное решение. Автоматические преобразования типов — частый источник скрытых ошибок: значение незаметно теряет точность, или операция работает не так, как ожидалось. Go делает такие преобразования явными, чтобы вы всегда понимали что происходит.\r\n\r\n## 6. Явное преобразование типов\r\n\r\nРешение проблемы несовместимых типов — явное преобразование. Синтаксис простой: `T(x)`, где `T` — целевой тип, `x` — значение или выражение. Вы явно говорите компилятору: «преобразуй это значение в такой-то тип»:\r\n\r\n```go\r\nkayak := 275\r\nsoccerBall := 19.50\r\ntotal := float64(kayak) + soccerBall\r\nfmt.Println(total)\r\n```\r\n\r\n```output\r\n294.5\r\n```\r\n\r\n`float64(kayak)` создаёт новое значение типа `float64` из переменной `kayak`. Исходная переменная при этом не меняется — она остаётся `int`. Теперь оба операнда в сложении — `float64`, и компилятор доволен.\r\n\r\n### Потеря точности при преобразовании\r\n\r\nЯвное преобразование — мощный инструмент, но с ним нужно быть внимательным. При преобразовании `float64` в `int` дробная часть просто отбрасывается — без округления, без предупреждения:\r\n\r\n```go\r\nkayak := 275\r\nsoccerBall := 19.50\r\ntotal := kayak + int(soccerBall)  // 19.50 → 19\r\nfmt.Println(total)                // 294, а не 294.5\r\n```\r\n\r\n```output\r\n294\r\n```\r\n\r\n`19.50` стало `19` — дробная часть просто исчезла. Переполнение тоже возможно: если преобразовать большое число в маленький тип, результат будет неожиданным:\r\n\r\n```go\r\ntotal := 294\r\nfmt.Println(int8(total))  // 38 — переполнение int8 (максимум 127)\r\n```\r\n\r\n```output\r\n38\r\n```\r\n\r\n### Безопасное преобразование float в int: пакет math\r\n\r\nЕсли нужно преобразовать `float64` в `int` и при этом контролировать округление, используйте функции из пакета `math`. Они возвращают `float64`, который потом можно явно привести к `int`:\r\n\r\n| Функция               | Описание                             | Пример                     |\r\n| --------------------- | ------------------------------------ | -------------------------- |\r\n| `math.Floor(x)`       | Округление вниз (к меньшему целому)  | `Floor(19.9)` → `19`       |\r\n| `math.Ceil(x)`        | Округление вверх (к большему целому) | `Ceil(19.1)` → `20`        |\r\n| `math.Round(x)`       | Округление к ближайшему целому       | `Round(19.5)` → `20`       |\r\n| `math.RoundToEven(x)` | Округление к ближайшему чётному      | `RoundToEven(18.5)` → `18` |\r\n\r\n```go\r\nkayak := 275\r\nsoccerBall := 19.50\r\ntotal := kayak + int(math.Round(soccerBall))\r\nfmt.Println(total)  // 295 — 19.50 округлилось до 20\r\n```\r\n\r\n```output\r\n295\r\n```\r\n\r\n> **Когда что использовать:** `int(x)` — когда дробная часть точно не важна и вы это осознаёте. `math.Floor`/`Ceil`/`Round` — когда нужен контроль над округлением. Самый безопасный подход — сразу работать с `float64` и не преобразовывать в `int` без нужды.\r\n\r\n## 8. Парсинг: строка → число\r\n\r\nВ реальных программах данные часто приходят в виде строк — из пользовательского ввода, файлов, сети. Прежде чем работать с такими данными как с числами, их нужно преобразовать. Для этого используется пакет `strconv`.\r\n\r\n### Паттерн: результат + ошибка\r\n\r\nПрежде чем смотреть на конкретные функции, важно понять одну концепцию Go. Функции парсинга возвращают два значения: результат и ошибку. Это не исключение как в Python — это обычные переменные:\r\n\r\n```go\r\nresult, err := strconv.Atoi("123")\r\nif err == nil {\r\n	fmt.Println("Результат:", result)\r\n} else {\r\n	fmt.Println("Ошибка:", err)\r\n}\r\n```\r\n\r\n```output\r\nРезультат: 123\r\n```\r\n\r\nЕсли парсинг прошёл успешно — `err` равен `nil`, и `result` содержит значение. Если строка не может быть распознана — `err` содержит описание проблемы, а `result` будет нулевым значением. Этот паттерн встречается в Go повсюду — не только в парсинге, но и при работе с файлами, сетью, базами данных.\r\n\r\n> **Сравнение с Python:** Python использует исключения: `try/except ValueError`. Go возвращает ошибку как обычное значение и требует проверить её явно. Это делает обработку ошибок более предсказуемой — вы не можете случайно забыть про исключение.\r\n\r\n### Строка → int: strconv.Atoi\r\n\r\nСамое частое преобразование — строка в целое число. Для этого есть удобная функция `Atoi` (от ASCII to Integer):\r\n\r\n```go\r\nval := "100"\r\nresult, err := strconv.Atoi(val)\r\nif err == nil {\r\n	fmt.Println(result + 5)  // 105\r\n} else {\r\n	fmt.Println("Не число:", err)\r\n}\r\n\r\n// Неудачный парсинг:\r\n_, err2 := strconv.Atoi("abc")\r\nfmt.Println(err2)\r\n```\r\n\r\n```output\r\n105\r\nstrconv.Atoi: parsing "abc": invalid syntax\r\n```\r\n\r\n### Строка → float: strconv.ParseFloat\r\n\r\nДля чисел с плавающей точкой используется `ParseFloat`. Второй аргумент — размер результата: 32 или 64. Функция всегда возвращает `float64`:\r\n\r\n```go\r\nval := "48.95"\r\nresult, err := strconv.ParseFloat(val, 64)\r\nif err == nil {\r\n	fmt.Printf("%.2f\\n", result)\r\n}\r\n\r\n// Поддерживается научная нотация:\r\nresult2, _ := strconv.ParseFloat("4.895e+01", 64)\r\nfmt.Println(result2)\r\n```\r\n\r\n```output\r\n48.95\r\n48.95\r\n```\r\n\r\n### Строка → bool: strconv.ParseBool\r\n\r\n`ParseBool` распознаёт несколько вариантов написания: `"true"`, `"True"`, `"TRUE"`, `"1"` — все дают `true`. `"false"`, `"False"`, `"FALSE"`, `"0"` — все дают `false`:\r\n\r\n```go\r\nb1, _ := strconv.ParseBool("true")\r\nb2, _ := strconv.ParseBool("1")\r\nb3, _ := strconv.ParseBool("FALSE")\r\nfmt.Println(b1, b2, b3)\r\n\r\n_, err := strconv.ParseBool("yes")  // не распознано\r\nfmt.Println(err)\r\n```\r\n\r\n```output\r\ntrue true false\r\nstrconv.ParseBool: parsing "yes": invalid syntax\r\n```\r\n\r\n## 9. Число → строка\r\n\r\nОбратная задача — представить число в виде строки. Самый удобный способ — `fmt.Sprintf`, который мы уже разбирали в прошлой главе — он даёт полный контроль над форматом и подходит для большинства задач. Но если нужно перевести число в другую систему счисления (двоичную, шестнадцатеричную) или нужен максимально лёгкий способ без шаблонов, на помощь приходит пакет `strconv`.\r\n\r\n### Пакет strconv\r\n\r\nПакет `strconv` предоставляет функции для точного контроля над представлением числа в строке:\r\n\r\n| Функция                                     | Описание                                                      |\r\n| ------------------------------------------- | ------------------------------------------------------------- |\r\n| `strconv.Itoa(val)`                         | `int` → строка (десятичная). Самый простой вариант            |\r\n| `strconv.FormatInt(val, base)`              | `int64` → строка с указанием системы счисления (2, 8, 10, 16) |\r\n| `strconv.FormatFloat(val, fmt, prec, size)` | `float64` → строка с контролем формата и точности             |\r\n| `strconv.FormatBool(val)`                   | `bool` → строка `"true"` или `"false"`                        |\r\n\r\n```go\r\n// int → строка\r\nval := 275\r\nfmt.Println(strconv.Itoa(val))                    // "275"\r\nfmt.Println(strconv.FormatInt(int64(val), 2))     // "100010011" (двоичная)\r\nfmt.Println(strconv.FormatInt(int64(val), 16))    // "113" (шестнадцатеричная)\r\n\r\n// float → строка\r\nf := 49.95\r\nfmt.Println(strconv.FormatFloat(f, \'f\', 2, 64))  // "49.95"\r\nfmt.Println(strconv.FormatFloat(f, \'e\', -1, 64)) // "4.995e+01"\r\n\r\n// bool → строка\r\nfmt.Println(strconv.FormatBool(true))             // "true"\r\n```\r\n\r\n```output\r\n275\r\n100010011\r\n113\r\n49.95\r\n4.995e+01\r\ntrue\r\n```\r\n\r\nВторой аргумент `FormatFloat` задаёт формат: `\'f\'` — обычная запись (`49.95`), `\'e\'` — научная нотация (`4.995e+01`), `\'g\'` — автоматически выбирает компактный вариант. Третий аргумент — количество знаков после точки (`-1` означает минимум знаков без потери точности).\r\n\r\n> **Когда что использовать:** `fmt.Sprintf` — когда нужно составить строку из нескольких значений или контролировать формат вывода. `strconv` — когда нужно явно преобразовать одно значение в строку или работать с нестандартными системами счисления.\r\n',_=`# 2.5. Управление потоком выполнения\r
\r
До этого момента наши программы выполнялись строго сверху вниз: каждая строка кода срабатывала ровно один раз, в том порядке, в каком была написана. Это работает для самых простых задач, но реальные программы устроены иначе. Им нужно принимать решения, повторять действия, выбирать из множества вариантов. Это и называется управлением потоком выполнения.\r
\r
В этой главе разберём три основных инструмента: оператор if для условного выполнения, цикл for для повторений и оператор switch для множественного выбора.\r
\r
## 1. Что такое поток выполнения\r
\r
Поток выполнения — это порядок, в котором компьютер выполняет команды программы.\r
\r
![alt](/images/2.5-1.png)\r
\r
По умолчанию команды выполняются последовательно: первая, вторая, третья — и так до конца, после чего программа завершается. Это предсказуемо, но негибко.\r
\r
Представьте кулинарный рецепт. Простая последовательность шагов работает только если у вас всегда одинаковые ингредиенты. Реальный рецепт говорит: «если тесто слишком жидкое — добавьте муки», «перемешивайте 5 минут», «выберите режим духовки в зависимости от типа выпечки». Это и есть управление потоком — рецепт, который адаптируется под обстоятельства.\r
\r
## 2. Условное выполнение: if\r
\r
В жизни мы постоянно принимаем решения по схеме «если — то — иначе». Если идёт дождь — берём зонт, иначе оставляем дома. Если торт остыл — режем, иначе ждём ещё. Программам нужно делать то же самое: реагировать по-разному в зависимости от ситуации. Для этого и существует оператор if.\r
\r
### Самая простая форма\r
\r
Начнём с минимума. Допустим, у нас есть цена каяка, и мы хотим напечатать сообщение, если она больше 100:\r
\r
\`\`\`go\r
kayakPrice := 275.00\r
\r
if kayakPrice > 100 {\r
    fmt.Println("Цена больше 100")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Цена больше 100\r
\`\`\`\r
\r
Разберём строчку за строчкой.\r
\r
![alt](/images/2.5-2.png)\r
\r
Слово if говорит компилятору: «сейчас будет проверка». После if идёт условие — kayakPrice > 100. Это просто сравнение: «kayakPrice больше 100?». Ответ может быть только два — да (true) или нет (false). Если ответ true, выполняется код в фигурных скобках. Если false — этот код пропускается, как будто его и не было.\r
\r
Представьте охранника на входе. Он смотрит в ваш пропуск и проверяет одно условие: «есть ли у вас право войти?». Если да — пропускает дальше. Если нет — разворачивает обратно. if работает точно так же: проверяет условие и решает, выполнять код в скобках или нет.\r
\r
В нашем примере 275.00 действительно больше 100, поэтому условие истинно, и сообщение печатается. Если бы цена была, скажем, 50 — условие оказалось бы ложным, и программа просто пропустила бы блок в скобках, ничего не напечатав.\r
\r
### Что считается условием\r
\r
Условие — это любое выражение, которое даёт ответ true или false. Самые частые условия — это сравнения, которые мы разбирали в прошлой главе:\r
\r
\`\`\`go\r
x > 10               // x больше 10?\r
x == 5               // x равен 5? (два знака равенства!)\r
x != 0               // x не равен 0?\r
x >= 100             // x больше или равен 100?\r
name == "Алиса"      // строки тоже сравниваются\r
\r
// Можно объединять через && (и) и || (или):\r
x > 0 && x < 100         // x больше нуля И меньше 100\r
age < 18 || age > 65     // возраст меньше 18 ИЛИ больше 65\r
\`\`\`\r
\r
> Важно: для сравнения используется == (два знака равенства), а не = (один). Один знак — это присваивание (записать значение в переменную). Два знака — это сравнение (узнать, равны ли значения). Перепутать их — самая частая ошибка новичков во всех языках программирования.\r
\r
### Несколько вариантов: else и else if\r
\r
Часто одного варианта недостаточно. Если условие не выполнено — тоже хочется что-то сделать. Для этого есть ключевое слово else (с английского — «иначе»). А если вариантов больше двух, между if и else можно вставлять промежуточные проверки через else if.\r
\r
Начнём с двух вариантов:\r
\r
\`\`\`go\r
kayakPrice := 50.00\r
\r
if kayakPrice > 100 {\r
    fmt.Println("Дорого")\r
} else {\r
    fmt.Println("Дёшево")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Дёшево\r
\`\`\`\r
\r
Цена 50 не больше 100, условие ложно — первый блок пропускается. Зато выполняется блок после else. Один из двух блоков выполнится обязательно — но никогда оба сразу.\r
\r
Теперь добавим промежуточные варианты через else if. Например, цена может быть «дорогая», «дешёвая», «средняя» или вообще «странная»:\r
\r
\`\`\`go\r
kayakPrice := 275.00\r
\r
if kayakPrice > 500 {\r
    fmt.Println("Дорого")\r
} else if kayakPrice < 100 {\r
    fmt.Println("Дёшево")\r
} else if kayakPrice > 200 && kayakPrice < 300 {\r
    fmt.Println("Средне: 200-300")\r
} else {\r
    fmt.Println("Не подходит ни под одно условие")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Средне: 200-300\r
\`\`\`\r
\r
Условия проверяются строго по порядку, сверху вниз.\r
\r
![alt](/images/2.5-3.png)\r
\r
Первая проверка: kayakPrice > 500? — нет, 275 не больше 500, идём дальше. Вторая: kayakPrice < 100? — нет, 275 не меньше 100, идём дальше. Третья: kayakPrice > 200 && kayakPrice < 300? — да, 275 попадает в этот диапазон. Эта ветка выполняется, и оператор if на этом завершается. Остальные проверки уже не делаются — даже если бы они тоже подошли.\r
\r
Финальный else без условия — это «всё остальное». Он сработает, если ни одна из предыдущих веток не подошла. Этот блок не обязателен — его можно опустить, если для несовпадающих случаев ничего делать не нужно.\r
\r
else if похож на меню в кафе. Кассир спрашивает: «вам кофе?» — нет. «Может, чай?» — нет. «Тогда сок?» — да! Заказ принят, дальше уже не предлагают. С else if так же: первая подходящая ветка выполняется, остальные пропускаются.\r
\r
> Ограничения синтаксиса: фигурные скобки нельзя опускать даже для одного оператора. Открывающую скобку нельзя переносить на следующую строку — она должна быть на одной строке с if. Это связано с автоматической вставкой точек с запятой, о которой мы говорили в главе про создание проекта.\r
\r
### Область видимости: переменные внутри if\r
\r
Каждая ветка if — это отдельная «комната» со своими правилами. Переменные, объявленные внутри ветки, не существуют в других ветках и не существуют после самого if:\r
\r
\`\`\`go\r
kayakPrice := 275.00\r
\r
if kayakPrice > 100 {\r
    discount := 50  // эта переменная живёт только здесь\r
    fmt.Println("Скидка:", discount)\r
}\r
\r
// fmt.Println(discount)  // ошибка! discount тут уже не существует\r
\`\`\`\r
\r
\`\`\`output\r
Скидка: 50\r
\`\`\`\r
\r
discount существует только внутри своей ветки. За пределами фигурных скобок её уже нет — попытка использовать её приведёт к ошибке компиляции. Это полезное правило: оно не даёт случайно использовать временные значения там, где они не должны появляться.\r
\r
### Оператор инициализации\r
\r
У Go есть удобная фишка: можно объявить переменную прямо в самом if. Это особенно полезно когда переменная нужна только для проверки и больше нигде. Синтаксис такой: if переменная := выражение; условие.\r
\r
\`\`\`go\r
priceString := "275"\r
\r
if kayakPrice, err := strconv.Atoi(priceString); err == nil {\r
    fmt.Println("Цена:", kayakPrice)\r
} else {\r
    fmt.Println("Ошибка:", err)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Цена: 275\r
\`\`\`\r
\r
Разберём по частям.\r
\r
![alt](/images/2.5-4.png)\r
\r
strconv.Atoi пытается превратить строку "275" в число. Эта функция возвращает два значения: само число и ошибку (помните паттерн «результат + ошибка» из прошлой главы?). Мы записываем оба значения в переменные kayakPrice и err. После точки с запятой идёт условие — err == nil. nil означает «ничего», «пусто». Если err равно nil — значит ошибки не было, преобразование удалось.\r
\r
Главное удобство: kayakPrice и err живут только внутри этого if — и в ветке then, и в ветке else. После закрывающей скобки их уже не существует. Это намного чище, чем объявлять временные переменные снаружи и захламлять основной код.\r
\r
## 3. Циклы: for\r
\r
Цикл — это способ заставить программу повторять одни и те же действия много раз. Без циклов любая повторяющаяся работа превращалась бы в копирование одной и той же строки десятки или сотни раз. Например, если нужно поздороваться пять раз, без цикла пришлось бы написать fmt.Println пять раз подряд. С циклом — три строки, и не важно, пять повторений или пять тысяч.\r
\r
В Go есть только одно ключевое слово для циклов — for. В других языках обычно есть несколько разных конструкций (while, do-while, foreach), но в Go все эти задачи решает один универсальный for, работающий в нескольких режимах. Разберём их по нарастанию сложности.\r
\r
### Базовая форма: бесконечный цикл, break и continue\r
\r
Самая простая форма for — без условий вообще. Такой цикл работает бесконечно: повторяет код в скобках снова и снова, пока его не прервут изнутри. У нас есть два инструмента, чтобы влиять на ход цикла:\r
\r
break — полностью прерывает цикл. Программа выходит из него и продолжает выполнение дальше. continue — пропускает остаток текущей итерации и сразу переходит к следующей.\r
\r
\`\`\`go\r
counter := 0\r
\r
for {\r
    counter++\r
\r
    if counter == 2 {\r
        continue   // пропустить эту итерацию\r
    }\r
    if counter > 4 {\r
        break      // прервать цикл\r
    }\r
\r
    fmt.Println("Counter:", counter)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Counter: 1\r
Counter: 3\r
Counter: 4\r
\`\`\`\r
\r
Разберём пошагово, что происходит.\r
\r
![alt](/images/2.5-5.png)\r
\r
На первой итерации counter становится равен 1. Ни одно из условий не выполняется, и Println печатает «Counter: 1». На второй итерации counter становится 2 — срабатывает continue, и программа сразу прыгает в начало цикла, не доходя до Println. Поэтому двойка не печатается. Третья и четвёртая итерации проходят как первая. На пятой counter становится равен 5 — срабатывает break, и цикл прекращается.\r
\r
Представьте конвейер на заводе. Он работает бесконечно, пока его не выключат. continue — это «пропустить эту деталь, она бракованная, бери следующую». break — это «всё, выключаем конвейер». Бесконечный цикл с break и continue — основа многих реальных программ: серверы и фоновые процессы работают именно так, пока не получат команду остановки.\r
\r
### Цикл с условием — повторение пока что-то истинно\r
\r
Чаще всего нужно не «бесконечно» повторять, а «пока выполняется условие». Например — пока счётчик меньше десяти, пока пользователь не ввёл правильный пароль, пока в файле есть строки.\r
\r
![alt](/images/2.5-6.png)\r
\r
Можно либо записать условие сразу после for (это аналог while из других языков), либо использовать классическую форму с тремя частями:\r
\r
\`\`\`go\r
// Форма с одним условием (аналог while):\r
counter := 0\r
for counter <= 3 {\r
    fmt.Println("Counter:", counter)\r
    counter++\r
}\r
\r
// Классическая форма с инициализацией, условием и шагом:\r
for counter := 0; counter <= 3; counter++ {\r
    fmt.Println("Counter:", counter)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Counter: 0\r
Counter: 1\r
Counter: 2\r
Counter: 3\r
\`\`\`\r
\r
Обе формы делают одно и то же — печатают числа от 0 до 3. Разница только в записи. В первой форме мы вручную создаём переменную counter перед циклом и вручную увеличиваем её внутри. Во второй форме всё это собрано в одну строку, разделённую точкой с запятой: инициализация, условие, шаг.\r
\r
Что происходит в классической форме по шагам. Сначала один раз выполняется counter := 0 — это начальное значение.\r
\r
![alt](/images/2.5-7.png)\r
\r
Затем проверяется условие counter <= 3. Если истинно — выполняется тело цикла. После каждой итерации выполняется counter++. Затем условие проверяется заново. Когда условие станет ложным — цикл завершится.\r
\r
Когда какую форму использовать. Классическая форма удобнее когда вы заранее знаете сколько раз нужно повторить — например, пройтись по числам от 1 до 100. Форма с одним условием полезнее когда условие сложное и не сводится к простому счётчику — например, читать строки из файла пока они не закончатся.\r
\r
> Не забудьте менять условие! Если внутри цикла никак не повлиять на условие, оно никогда не станет ложным — цикл будет работать вечно. Это частая ошибка: забыть counter++ внутри тела цикла. В таких случаях программу приходится прерывать вручную (Ctrl+C в терминале).\r
\r
### Перебор последовательностей: for ... range\r
\r
Последняя форма цикла — самая удобная, когда нужно пройтись по всем элементам коллекции. Например — по всем буквам строки, по всем числам массива, по всем парам ключ-значение в карте. Для этого есть конструкция for ... range — аналог foreach из других языков. range на каждой итерации даёт два значения: индекс элемента и сам элемент.\r
\r
\`\`\`go\r
product := "Kayak"\r
\r
for index, character := range product {\r
    fmt.Println("Index:", index, "Character:", string(character))\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Index: 0 Character: K\r
Index: 1 Character: a\r
Index: 2 Character: y\r
Index: 3 Character: a\r
Index: 4 Character: k\r
\`\`\`\r
\r
Цикл сам понимает, что строка состоит из 5 символов, и автоматически делает 5 итераций. На каждой итерации index — это номер символа (начиная с 0), а character — сам символ. string(character) преобразует символ в строку для красивого вывода — без этого мы бы получили числовые коды.\r
\r
Иногда нужны не оба значения, а только одно. Если нужны только индексы — можно опустить вторую переменную: for index := range product. Если нужны только значения, без индексов — на месте индекса используется пустой идентификатор _ (мы говорили о нём в главе про переменные): for _, character := range product.\r
\r
range работает не только со строками, но и с массивами, срезами и картами — обо всём этом подробно в следующей главе.\r
\r
## 4. Множественный выбор: switch\r
\r
Когда нужно сравнить значение со множеством вариантов, цепочка из else if быстро становится громоздкой. Представьте, что вы пишете программу, которая определяет день недели по номеру: 1 — понедельник, 2 — вторник и так до семи. Через if/else if это семь повторяющихся блоков. Для таких ситуаций есть оператор switch — короче и читаемее.\r
\r
### Базовая форма\r
\r
Идея простая: switch берёт значение и сравнивает его поочерёдно с каждым вариантом — эти варианты называются case. Если значение совпало с каким-то case — выполняется код этого case. Если ни один не подошёл — срабатывает default (аналог финального else):\r
\r
\`\`\`go\r
product := "Kayak"\r
\r
for index, character := range product {\r
    switch character {\r
    case 'K', 'k':\r
        fmt.Println("K или k на позиции", index)\r
    case 'y':\r
        fmt.Println("y на позиции", index)\r
    default:\r
        fmt.Println("Другой символ на позиции", index)\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output\r
K или k на позиции 0\r
Другой символ на позиции 1\r
y на позиции 2\r
Другой символ на позиции 3\r
K или k на позиции 4\r
\`\`\`\r
\r
В одном case можно перечислить несколько значений через запятую — тогда case сработает для любого из них. В нашем примере case 'K', 'k' срабатывает и для заглавной K, и для строчной k. default не обязателен — если его нет и ни один case не подошёл, switch просто завершается без действий.\r
\r
switch похож на сортировку писем по ящикам. Берём конверт, смотрим адрес, и кладём в подходящий ящик. Если адрес совпал с одним из ящиков — кладём туда. Если ни один не подошёл — кладём в общий ящик «остальное» (это default).\r
\r
### Главное отличие от других языков: нет автоматического fallthrough\r
\r
Если вы знакомы с C, Java или JavaScript — там у switch есть неприятная особенность. Когда case совпадает, программа выполняет его код, а потом продолжает выполнять код следующих case, пока не встретит специальное слово break. Это называется «провал» (fallthrough) и часто становится источником багов: забыл написать break — получил неожиданное поведение.\r
\r
В Go всё наоборот: каждый case завершается сам по себе, писать break не нужно. Это безопаснее. Если же по каким-то причинам провал нужен — есть отдельное ключевое слово fallthrough, которое явно его включает. На практике fallthrough встречается крайне редко.\r
\r
### switch без значения — замена длинной цепочке if/else if\r
\r
У switch есть необычная форма — без сравниваемого значения. В этом случае каждый case содержит уже не значение, а условие. Выполняется первый case, чьё условие истинно. Это просто более красивая запись для длинной цепочки if/else if:\r
\r
\`\`\`go\r
counter := 5\r
\r
switch {\r
case counter == 0:\r
    fmt.Println("Ноль")\r
case counter < 3:\r
    fmt.Println(counter, "< 3")\r
case counter >= 3 && counter < 7:\r
    fmt.Println(counter, ">= 3 && < 7")\r
default:\r
    fmt.Println(counter, ">= 7")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
5 >= 3 && < 7\r
\`\`\`\r
\r
Этот код полностью эквивалентен такому if/else if:\r
\r
\`\`\`go\r
if counter == 0 {\r
    fmt.Println("Ноль")\r
} else if counter < 3 {\r
    fmt.Println(counter, "< 3")\r
} else if counter >= 3 && counter < 7 {\r
    fmt.Println(counter, ">= 3 && < 7")\r
} else {\r
    fmt.Println(counter, ">= 7")\r
}\r
\`\`\`\r
\r
Когда что использовать — дело вкуса. switch без значения часто читается легче, особенно когда веток много.\r
\r
## 5. Метки и goto\r
\r
Последняя возможность управления потоком — оператор goto. Это переход к помеченной строке кода. Метка — это имя с двоеточием перед обычным оператором, а goto переходит к этой метке.\r
\r
На практике goto в Go используется крайне редко. В большинстве случаев for, if и switch справляются лучше, а сложный код с goto тяжело читать. Большинство Go-программистов не используют его никогда. Иногда goto всё же оправдан — например, для выхода из глубоко вложенных циклов или для централизованной обработки ошибок. Но если вы сомневаетесь, нужен ли вам goto — скорее всего нет.\r
`,C='# 2.6. Массивы, срезы и карты\r\n\r\nДо сих пор мы работали с одиночными значениями: одно число, одна строка, один признак true/false. Реальные программы обычно работают со списками: список товаров в магазине, список учеников в классе, список сообщений в чате. Хранить их в десяти разных переменных невозможно — нужны структуры, которые умеют хранить много значений сразу. В Go таких структур три: массивы, срезы и карты. В этой главе разберём каждую из них, а в конце поговорим про необычное поведение строк.\r\n\r\n## 1. Массивы\r\n\r\nМассив — самая простая коллекция. Это набор значений одного типа, у которого есть фиксированная длина — заданная при создании и неизменная. Например, массив из трёх строк: туда можно положить ровно три строки, не больше и не меньше.\r\n\r\nПредставьте коробку для яиц на 10 ячеек. Сколько в ней ячеек — столько и будет, не больше и не меньше. Каждая ячейка пронумерована — можно положить или достать яйцо из конкретной ячейки. Массив устроен точно так же: фиксированное число ячеек, к каждой можно обратиться по номеру.\r\n\r\n### Создание массива\r\n\r\nМассив объявляется ключевым словом var с указанием размера и типа элементов:\r\n\r\n```go\r\nvar names [3]string\r\nnames[0] = "Каяк"\r\nnames[1] = "Спасжилет"\r\nnames[2] = "Весло"\r\nfmt.Println(names)\r\n```\r\n\r\n```output\r\n[Каяк Спасжилет Весло]\r\n```\r\n\r\nЗапись `[3]string` означает «массив из 3 элементов типа string». Сразу после создания все ячейки заполнены нулевым значением для своего типа — для строк это пустая строка `""`, для чисел `0`, для bool — `false`. К ячейке обращаемся через имя массива и номер в квадратных скобках. Нумерация начинается с нуля: первая ячейка — это `[0]`, вторая — `[1]`, третья — `[2]`.\r\n\r\n> Важно: если попытаться обратиться к несуществующей ячейке (например, `names[5]` в массиве из 3 элементов), программа аварийно завершится с ошибкой `index out of range`. Размер массива всегда нужно учитывать.\r\n\r\nЧасто значения нужно задать сразу при создании. Для этого есть короткая запись:\r\n\r\n```go\r\nnames := [3]string{"Каяк", "Спасжилет", "Весло"}\r\nfmt.Println(names)\r\n```\r\n\r\n```output\r\n[Каяк Спасжилет Весло]\r\n```\r\n\r\n### Перебор через range\r\n\r\nЧтобы пройтись по всем элементам массива, используется уже знакомый `for ... range`. На каждой итерации мы получаем индекс элемента и сам элемент:\r\n\r\n```go\r\nnames := [3]string{"Каяк", "Спасжилет", "Весло"}\r\n\r\nfor index, value := range names {\r\n    fmt.Println("Индекс:", index, "Значение:", value)\r\n}\r\n```\r\n\r\n```output\r\nИндекс: 0 Значение: Каяк\r\nИндекс: 1 Значение: Спасжилет\r\nИндекс: 2 Значение: Весло\r\n```\r\n\r\nЕсли индекс не нужен — заменяем его на пустой идентификатор `_` (`for _, value := range names`). Так же можно поступить и со значением, если нужны только индексы.\r\n\r\n> Сравнение и многомерные массивы: массивы можно сравнивать через `==` и `!=` — они равны, если одного типа и содержат одинаковые элементы. Также массивы можно вкладывать друг в друга, получая многомерные структуры — например, `var grid [3][3]int` создаёт «таблицу» 3×3. На практике для коллекций почти всегда используются срезы, а не массивы — о них дальше.\r\n\r\n## 2. Срезы\r\n\r\nУ массивов есть серьёзное ограничение: их размер задан раз и навсегда. Но в реальных задачах часто заранее не известно, сколько элементов будет: сколько товаров добавит пользователь в корзину, сколько строк прочитается из файла, сколько сообщений придёт в чат. Для таких случаев есть срезы — самый используемый тип коллекций в Go.\r\n\r\nЕсли массив — коробка с фиксированным числом ячеек, то срез — это растягивающаяся сумка. В неё можно положить три элемента, потом ещё пять, потом убрать парочку — размер подстроится под содержимое.\r\n\r\n### Создание среза\r\n\r\nСрез создаётся так же, как массив, но без указания размера — пустые квадратные скобки:\r\n\r\n```go\r\nnames := []string{"Каяк", "Спасжилет", "Весло"}\r\nfmt.Println(names)\r\n```\r\n\r\n```output\r\n[Каяк Спасжилет Весло]\r\n```\r\n\r\nВнешне почти как массив, но разница принципиальная: тип среза — это `[]string` (без числа в скобках), и в него можно добавлять и удалять элементы. Длину среза можно узнать в любой момент функцией `len`:\r\n\r\n```go\r\nfmt.Println(len(names))  // 3\r\n```\r\n\r\n```output\r\n3\r\n```\r\n\r\n### Добавление элементов: append\r\n\r\nГлавное преимущество среза — он умеет расти. Для добавления элементов используется встроенная функция `append`:\r\n\r\n```go\r\nnames := []string{"Каяк", "Спасжилет", "Весло"}\r\nnames = append(names, "Шапка")\r\nnames = append(names, "Перчатки", "Сапоги")  // можно несколько сразу\r\nfmt.Println(names)\r\n```\r\n\r\n```output\r\n[Каяк Спасжилет Весло Шапка Перчатки Сапоги]\r\n```\r\n\r\n> Обратите внимание: результат `append` присваивается обратно той же переменной — `names = append(names, ...)`. Это важная особенность: `append` не меняет существующий срез на месте, а возвращает новый. Если забыть присвоение и написать просто `append(names, "Шапка")`, элемент будет добавлен, но срез `names` этого «не увидит» — компилятор выдаст ошибку об неиспользуемом значении.\r\n\r\n### Срезы из срезов и массивов: операция [start:end]\r\n\r\nУ среза есть ещё одна полезная возможность — взять часть существующего среза или массива. Это делается с помощью синтаксиса `[start:end]`, где `start` — индекс первого элемента (включительно), а `end` — индекс последнего (не включая):\r\n\r\n```go\r\nproducts := []string{"Каяк", "Спасжилет", "Весло", "Шапка"}\r\n\r\nsomeNames := products[1:3]   // элементы с 1 по 3 (не включая 3)\r\nallNames  := products[:]     // все элементы\r\nfromTwo   := products[2:]    // с индекса 2 и до конца\r\nfirstTwo  := products[:2]    // от начала до индекса 2 (не включая)\r\n\r\nfmt.Println(someNames)\r\nfmt.Println(allNames)\r\nfmt.Println(fromTwo)\r\nfmt.Println(firstTwo)\r\n```\r\n\r\n```output\r\n[Спасжилет Весло]\r\n[Каяк Спасжилет Весло Шапка]\r\n[Весло Шапка]\r\n[Каяк Спасжилет]\r\n```\r\n\r\nМожно опускать `start` (тогда берётся с начала) или `end` (тогда до самого конца). Важная деталь: при создании среза от среза или массива новая структура использует ту же память, что и оригинал. То есть `someNames` в нашем примере не копирует элементы, а просто «смотрит» на часть исходного массива `products`. Изменения через один из них могут затронуть другой. Это поведение выглядит сложно, но обычно работает интуитивно — основное правило: если вам нужна полностью независимая копия, используйте `append` с пустым срезом или встроенную функцию `copy`.\r\n\r\n> Про длину и ёмкость: у среза есть две характеристики — длина (`len`) и ёмкость (`cap`). Длина — сколько элементов сейчас хранится. Ёмкость — сколько может храниться без выделения новой памяти. Когда срезу не хватает места при `append`, Go автоматически выделяет больше — об этом обычно не нужно задумываться. Просто знайте, что при создании через `make([]string, 3, 10)` можно сразу зарезервировать ёмкость.\r\n\r\n## 3. Карты\r\n\r\nМассивы и срезы используют целые числа как ключи: `products[0]`, `products[1]`, `products[2]`. Но иногда удобнее обращаться к данным по другим признакам. Например, мы хотим хранить цены товаров и обращаться к ним по названию: `prices["Каяк"]`, `prices["Спасжилет"]`. Для этого есть карта (`map`) — структура, которая связывает ключи со значениями.\r\n\r\nКарта — это словарь. Вы открываете словарь на слове «солнце» — и получаете перевод. Ищете не по номеру страницы, а по самому слову. В программировании карты часто называют именно «словари» (как, например, в Python).\r\n\r\n### Создание карты\r\n\r\nТип карты записывается как `map[типКлюча]типЗначения`. Например, `map[string]float64` — карта, где ключи — строки, а значения — числа float64.\r\n\r\n```go\r\nproducts := map[string]float64{\r\n    "Каяк":      279,\r\n    "Спасжилет": 48.95,\r\n    "Шапка":     9.50,\r\n}\r\n\r\nfmt.Println(products)\r\nfmt.Println("Размер:", len(products))\r\n```\r\n\r\n```output\r\nmap[Каяк:279 Спасжилет:48.95 Шапка:9.5]\r\nРазмер: 3\r\n```\r\n\r\nКаждая запись — это пара «ключ: значение», разделитель — двоеточие, между записями запятые. Финальная запятая после последней записи обязательна — это особенность синтаксиса Go.\r\n\r\n### Чтение и добавление значений\r\n\r\nЧтение и добавление работают одинаково: имя карты, в квадратных скобках ключ. Если присваиваем — добавляем или меняем. Если читаем — получаем значение по ключу:\r\n\r\n```go\r\nproducts["Весло"] = 29.50         // добавить новый ключ\r\nproducts["Шапка"] = 12.00         // изменить существующий\r\n\r\nprice := products["Каяк"]\r\nfmt.Println("Цена каяка:", price)\r\n```\r\n\r\n```output\r\nЦена каяка: 279\r\n```\r\n\r\nА что если запросить ключ, которого нет? Карта вернёт нулевое значение для своего типа значений. Для `float64` это `0`, для `string` — пустая строка, для `bool` — `false`:\r\n\r\n```go\r\nfmt.Println(products["Несуществующий"])\r\n```\r\n\r\n```output\r\n0\r\n```\r\n\r\n### Проверка существования: value, ok\r\n\r\nТут возникает проблема: если `0` вернулся — это потому что ключа нет, или потому что у нас в карте действительно лежит `0`? По одному значению не различить. Поэтому в Go есть специальный синтаксис «comma-ok»: при чтении из карты можно получить два значения — само значение и булев флаг, который говорит, был ли ключ в карте:\r\n\r\n```go\r\nproducts := map[string]float64{\r\n    "Каяк":       279,\r\n    "Бесплатное": 0,\r\n}\r\n\r\nvalue, ok := products["Бесплатное"]\r\nif ok {\r\n    fmt.Println("Цена:", value)\r\n} else {\r\n    fmt.Println("Нет такого товара")\r\n}\r\n```\r\n\r\n```output\r\nЦена: 0\r\n```\r\n\r\nИмя `ok` — это просто соглашение, можно использовать любое. Этот же приём часто комбинируют с `if`, объявляя переменные прямо в нём:\r\n\r\n```go\r\nif value, ok := products["Каяк"]; ok {\r\n    fmt.Println("Цена:", value)\r\n}\r\n```\r\n\r\n```output\r\nЦена: 279\r\n```\r\n\r\n### Удаление: delete\r\n\r\nЧтобы убрать ключ из карты, используется встроенная функция `delete`:\r\n\r\n```go\r\ndelete(products, "Шапка")\r\nfmt.Println(products)\r\n```\r\n\r\nЕсли ключа нет — ничего страшного, ошибки не будет, просто ничего не произойдёт.\r\n\r\n### Перебор через range\r\n\r\nКарты тоже перебираются через `for ... range`, но вместо индекса и значения мы получаем ключ и значение:\r\n\r\n```go\r\nfor key, value := range products {\r\n    fmt.Println(key, "стоит", value)\r\n}\r\n```\r\n\r\n> Важно: порядок перебора карты случайный и не гарантирован. Каждый запуск программы может выдавать элементы в разном порядке. Если порядок важен — соберите ключи в срез, отсортируйте их, и перебирайте уже срез ключей.\r\n\r\n## 4. Строки, байты и руны\r\n\r\nВ предыдущих главах мы работали со строками как с цельными значениями: присваивали, печатали, сравнивали. Но иногда нужно работать с отдельными символами строки — например, перебрать буквы или взять первый символ. И тут начинаются особенности Go, про которые важно знать.\r\n\r\nДля Go строка — это последовательность байтов. Для английского текста и латинских букв один символ занимает один байт, и всё работает интуитивно. Но многие символы — кириллица, эмодзи, символы валют вроде `€` — занимают несколько байтов. И тут появляются неожиданности.\r\n\r\n### Индексация даёт байт, а не символ\r\n\r\nЕсли обратиться к строке по индексу — мы получим байт (один из `uint8`), а не символ:\r\n\r\n```go\r\nprice := "Hello"\r\nfmt.Println(price[0])           // числовой код буквы H\r\nfmt.Println(string(price[0]))   // H — преобразовали обратно в строку\r\n```\r\n\r\n```output\r\n72\r\nH\r\n```\r\n\r\nС английскими буквами всё в порядке. Но с символами, которые занимают несколько байтов, индексация даст только часть символа:\r\n\r\n```go\r\nprice := "€48.95"\r\nfmt.Println(len(price))           // символ € занимает 3 байта!\r\nfmt.Println(string(price[0]))     // мусор, только первый байт от €\r\n```\r\n\r\n```output\r\n8\r\nâ\r\n```\r\n\r\n### Решение: руны\r\n\r\nЧтобы работать со строкой посимвольно, её нужно преобразовать в срез рун. Руна (`rune`) — это один символ Unicode независимо от того, сколько байтов он занимает. Преобразование делается так:\r\n\r\n```go\r\nprice := "€48.95"\r\nrunes := []rune(price)\r\nfmt.Println(len(runes))           // символов действительно 6\r\nfmt.Println(string(runes[0]))     // теперь правильно\r\n```\r\n\r\n```output\r\n6\r\n€\r\n```\r\n\r\n### range по строке идёт по рунам\r\n\r\nХорошая новость: цикл `for ... range` по строке работает корректно — он перебирает символы (руны), а не байты:\r\n\r\n```go\r\nfor index, char := range "€48.95" {\r\n    fmt.Println(index, string(char))\r\n}\r\n```\r\n\r\n```output\r\n0 €\r\n3 4\r\n4 8\r\n5 .\r\n6 9\r\n7 5\r\n```\r\n\r\nОбратите внимание: индексы не подряд — после `€` сразу `3`, потому что `€` занимает 3 байта (индексы 0, 1, 2). Но сами значения `char` — это полноценные руны, так что цикл правильно проходит по символам.\r\n\r\n> Простое правило: если работаете только с английскими буквами и цифрами — можно использовать индексацию по байтам. Если в строке могут быть кириллица, эмодзи или другие символы — преобразуйте в `[]rune` или используйте `range`. Так избежите неожиданных багов.\r\n',S=`# 2.7. Определение и использование функций\r
\r
До этого момента весь наш код жил в одной функции — \`main\`. Это удобно для маленьких примеров, но плохо работает для серьёзных программ. В этой главе разберём, как разбивать программу на независимые куски — функции, которые можно вызывать столько раз, сколько нужно, и из разных мест.\r
\r
## 1. Что такое функция\r
\r
Функция — это именованный блок кода, который выполняется только когда его вызовут. Без функций программа выглядит как одна длинная инструкция: компьютер читает её сверху вниз и выполняет всё подряд. С функциями программа становится набором отдельных кусочков, которые можно использовать в любой момент и в любом порядке.\r
\r
Зачем это нужно. Во-первых, чтобы не повторять один и тот же код. Если в программе нужно посчитать налог в десяти местах — без функций пришлось бы десять раз писать одно и то же вычисление. С функцией пишем формулу один раз и потом просто вызываем её по имени. Во-вторых, чтобы программа была понятнее. Хорошо названная функция читается как предложение: \`calculateTax\`, \`sendEmail\`, \`openFile\` — сразу ясно, что делает код, не вникая в детали реализации.\r
\r
Представьте кулинарную книгу. Каждый рецепт — это функция. У него есть название («Шарлотка»), есть ингредиенты которые надо подготовить (параметры), есть результат (сам пирог). Вы не переписываете рецепт каждый раз когда печёте — вы открываете книгу на нужной странице и следуете инструкциям. Программа без функций — как готовка без рецептов: каждый раз с нуля.\r
\r
Мы уже использовали функции — \`fmt.Println\`, \`strconv.Atoi\`, \`math.Round\`. Теперь научимся создавать свои собственные.\r
\r
## 2. Простая функция\r
\r
Начнём с самой простой формы — функция без входных данных и без результата. Просто блок кода, который мы запускаем по имени:\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func printPrice() {\r
    kayakPrice := 275.00\r
    kayakTax := kayakPrice * 0.2\r
    fmt.Println("Price:", kayakPrice, "Tax:", kayakTax)\r
}\r
\r
func main() {\r
    fmt.Println("Сейчас будет вызов функции")\r
    printPrice()\r
    fmt.Println("Функция отработала")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Сейчас будет вызов функции\r
Price: 275 Tax: 55\r
Функция отработала\r
\`\`\`\r
\r
Разберём по частям. Объявление функции начинается со слова \`func\`. Дальше идёт имя — \`printPrice\`. Потом круглые скобки — в них пишутся входные данные функции (если они есть; в нашем случае пусто). И в фигурных скобках — тело функции, тот самый код который выполнится при вызове.\r
\r
Внутри \`main\` мы видим строчку \`printPrice()\`. Это вызов функции — имя плюс пустые скобки. Когда программа доходит до этой строки, выполнение «прыгает» внутрь \`printPrice\`, выполняет весь её код, и потом возвращается обратно — на следующую строку после вызова. Поэтому в выводе сначала «Сейчас будет», потом то что напечатала функция, потом «отработала».\r
\r
Это похоже на чтение книги со ссылками: «см. приложение А». Вы доходите до этой пометки, открываете приложение А, читаете его целиком, и возвращаетесь обратно к месту где остановились. \`main\` — основной текст, \`printPrice\` — приложение, скобки \`()\` — закладка «вернись сюда».\r
\r
> Важно про синтаксис: открывающая фигурная скобка \`{\` должна быть на той же строке что и имя функции — переносить её на новую строку нельзя. Это связано с автоматической вставкой точек с запятой, о которой мы говорили в главе про создание проекта.\r
\r
## 3. Параметры\r
\r
Функция выше делала всегда одно и то же — считала налог именно для каяка ценой 275. Это не очень полезно: обычно мы хотим, чтобы одна функция работала с разными данными. Для этого нужны параметры — входные значения, которые передаются функции при вызове.\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func printPrice(product string, price float64, taxRate float64) {\r
    taxAmount := price * taxRate\r
    fmt.Println(product, "цена:", price, "налог:", taxAmount)\r
}\r
\r
func main() {\r
    printPrice("Каяк", 275, 0.2)\r
    printPrice("Спасжилет", 48.95, 0.2)\r
    printPrice("Мяч", 19.50, 0.15)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Каяк цена: 275 налог: 55\r
Спасжилет цена: 48.95 налог: 9.79\r
Мяч цена: 19.5 налог: 2.925\r
\`\`\`\r
\r
Теперь функция \`printPrice\` работает с любым товаром, ценой и ставкой налога. Внутри скобок при объявлении мы перечислили три параметра: \`product\` типа \`string\`, \`price\` типа \`float64\`, \`taxRate\` типа \`float64\`. Каждый параметр — это пара «имя тип», параметры разделяются запятыми.\r
\r
Внутри тела функции имена параметров работают как обычные переменные — к ним можно обращаться по имени, использовать в вычислениях, передавать дальше. Они существуют только внутри функции — снаружи их не видно.\r
\r
При вызове функции мы передаём конкретные значения — это называется аргументами. \`printPrice("Каяк", 275, 0.2)\` означает: \`product\` получит значение \`"Каяк"\`, \`price\` — \`275\`, \`taxRate\` — \`0.2\`. Порядок важен: первый аргумент идёт в первый параметр, второй во второй и так далее. Типы должны совпадать — нельзя передать строку туда, где ожидается число.\r
\r
Параметры — как ингредиенты в рецепте. Сам рецепт говорит «возьмите муку», но не уточняет какую именно — пшеничную, ржаную, сколько грамм. Эти детали вы подставляете при готовке. Функция работает так же: она описывает что нужно сделать с входными данными, а конкретные данные появляются только при вызове.\r
\r
> Мелкие особенности синтаксиса: если несколько параметров идут подряд и имеют одинаковый тип — тип можно указать только у последнего: \`func f(a, b, c float64)\`. Если параметр объявлен, но не используется внутри функции — вместо имени можно поставить \`_\` (как с переменными). Эти особенности встречаются редко, но могут попасться в чужом коде — теперь вы их узнаете.\r
\r
## 4. Вариативные параметры\r
\r
Иногда заранее не известно, сколько значений придёт в функцию. Например, функция «напечатать список поставщиков для товара» — у одного товара один поставщик, у другого десять. Можно было бы передавать срез (\`[]string\`), но Go предлагает более удобный способ — вариативные параметры. Они позволяют принимать любое количество аргументов одного типа.\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func printSuppliers(product string, suppliers ...string) {\r
    if len(suppliers) == 0 {\r
        fmt.Println("Товар:", product, "Поставщик: (нет)")\r
        return\r
    }\r
    for _, supplier := range suppliers {\r
        fmt.Println("Товар:", product, "Поставщик:", supplier)\r
    }\r
}\r
\r
func main() {\r
    printSuppliers("Каяк", "Acme", "BoatCo", "CanoeMakers")\r
    printSuppliers("Спасжилет", "SailSafe")\r
    printSuppliers("Мяч")  // совсем без поставщиков\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Товар: Каяк Поставщик: Acme\r
Товар: Каяк Поставщик: BoatCo\r
Товар: Каяк Поставщик: CanoeMakers\r
Товар: Спасжилет Поставщик: SailSafe\r
Товар: Мяч Поставщик: (нет)\r
\`\`\`\r
\r
Вариативный параметр обозначается тремя точками перед типом: \`...string\`. При вызове функции можно передать сколько угодно строк — одну, десять, или вообще ни одной. Внутри функции \`suppliers\` — это обычный срез (\`[]string\`), по которому можно пройтись циклом \`range\` или узнать длину через \`len\`.\r
\r
Важная деталь: если ни одного значения не передали, \`suppliers\` будет пустым срезом (длина 0). Цикл \`range\` по такому срезу не выполнится ни разу — поэтому без проверки \`len(suppliers) == 0\` функция просто молча ничего не напечатала бы. Эта проверка — типичный приём при работе с вариативными параметрами.\r
\r
> Ограничения: вариативный параметр должен быть последним в списке параметров — после него нельзя добавлять обычные. И в одной функции может быть только один вариативный параметр. Это логично: иначе компилятор не понимал бы, к какому параметру относятся переданные значения.\r
\r
Если у вас уже есть готовый срез, и вы хотите передать его в вариативный параметр — для этого есть специальный синтаксис. После имени среза ставится многоточие:\r
\r
\`\`\`go\r
names := []string{"Acme", "BoatCo", "CanoeMakers"}\r
printSuppliers("Каяк", names...)  // три точки после имени среза\r
\`\`\`\r
\r
Без многоточия функция получила бы один аргумент — целый срез. С многоточием Go «распакует» срез, и каждый его элемент станет отдельным аргументом.\r
\r
## 5. Возвращаемые значения\r
\r
Пока что наши функции работали по принципу «сделай что-то и напечатай». Но чаще функция должна не печатать результат, а отдавать его обратно — чтобы вызывающий код мог использовать это значение дальше. Это называется возвращаемое значение, или результат функции.\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func calcTax(price float64) float64 {\r
    return price + (price * 0.2)\r
}\r
\r
func main() {\r
    priceWithTax := calcTax(275)\r
    fmt.Println("Итого:", priceWithTax)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Итого: 330\r
\`\`\`\r
\r
Что нового. После закрывающей скобки параметров появился ещё один тип — \`float64\`. Это и есть тип возвращаемого значения: функция обещает вернуть число с плавающей точкой. Внутри функции для этого используется ключевое слово \`return\` — оно завершает выполнение функции и отдаёт значение обратно тому, кто её вызвал.\r
\r
В \`main\` мы записываем результат вызова в переменную: \`priceWithTax := calcTax(275)\`. Здесь происходит так: вызывается функция \`calcTax\` с аргументом 275, она вычисляет 275 + 55 = 330, выполняет \`return 330\`, и это число попадает в \`priceWithTax\`. Дальше \`priceWithTax\` — обычная переменная, её можно печатать, использовать в вычислениях, передавать в другие функции.\r
\r
Результат можно использовать и без промежуточной переменной — прямо внутри выражения:\r
\r
\`\`\`go\r
fmt.Println("Итого:", calcTax(275))  // напечатает 330\r
\`\`\`\r
\r
Здесь Go сначала вычисляет \`calcTax(275)\`, получает \`330\`, и подставляет это значение в \`Println\`.\r
\r
\`return\` — как сдача в магазине. Вы дали кассиру 100 рублей за товар стоимостью 70, кассир посчитал и отдал вам 30 обратно. Вызов функции — момент когда вы платите, возвращаемое значение — сдача, которую вы получаете. И как со сдачей: можно положить её в кошелёк (записать в переменную), а можно сразу потратить (использовать в выражении).\r
\r
> Важно: тип возвращаемого значения должен соответствовать тому, что заявлено в объявлении. Если функция объявлена как \`... float64\`, нельзя вернуть строку — будет ошибка компиляции. Также если функция что-то возвращает, все ветки кода должны заканчиваться на \`return\` — нельзя забыть про какой-то путь.\r
\r
## 6. Несколько возвращаемых значений\r
\r
Одна из самых полезных особенностей Go — функция может возвращать сразу несколько значений. Это решает проблему, которая в других языках обходится через структуры или хитрые трюки. Мы уже встречали такой паттерн: \`strconv.Atoi\` возвращает и результат, и ошибку. Теперь научимся писать такие функции сами.\r
\r
\`\`\`go\r
package main\r
\r
import "fmt"\r
\r
func calcTax(price float64) (float64, bool) {\r
    if price > 100 {\r
        return price * 0.2, true\r
    }\r
    return 0, false\r
}\r
\r
func main() {\r
    taxAmount, taxDue := calcTax(275)\r
    if taxDue {\r
        fmt.Println("Налог:", taxAmount)\r
    } else {\r
        fmt.Println("Налог не нужен")\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Налог: 55\r
\`\`\`\r
\r
Что изменилось. Типы возвращаемых значений теперь стоят в скобках: \`(float64, bool)\`. Это значит «функция возвращает \`float64\` и \`bool\`, в таком порядке». В операторе \`return\` мы перечисляем оба значения через запятую: \`return price * 0.2, true\`. И при вызове получаем оба значения сразу: \`taxAmount, taxDue := calcTax(275)\`.\r
\r
Зачем это нужно. Часто одного значения недостаточно — нужно сообщить и результат, и какую-то дополнительную информацию о нём. Налог посчитан — какова сумма и нужно ли вообще его платить. Строка распарсена — каково число и не было ли ошибки. Файл прочитан — каково содержимое и сколько байт. Без множественных результатов пришлось бы заводить специальные «флажные» значения (например, \`-1\` как «налога нет»), а это путаница и источник багов.\r
\r
### Игнорирование ненужных значений: \\_\r
\r
Иногда нам нужно не всё, что возвращает функция — а только часть. Но Go не позволит просто пропустить значение: каждая объявленная переменная должна использоваться. На помощь приходит знакомый пустой идентификатор \`_\`:\r
\r
\`\`\`go\r
_, taxDue := calcTax(275)  // саму сумму налога мы не сохраняем\r
if taxDue {\r
    fmt.Println("Налог нужен")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Налог нужен\r
\`\`\`\r
\r
Подчёркивание говорит компилятору: «я знаю, что функция вернула значение, но мне оно не нужно». Этот же приём мы видели в циклах \`range\` и при объявлении переменных.\r
\r
### Именованные результаты\r
\r
У возвращаемых значений можно сразу указать имена — тогда они работают как локальные переменные внутри функции, и оператор \`return\` можно использовать без аргументов:\r
\r
\`\`\`go\r
func calcTotalPrice(products map[string]float64) (count int, total float64) {\r
    count = len(products)\r
    for _, price := range products {\r
        total += price\r
    }\r
    return  // вернёт текущие значения count и total\r
}\r
\`\`\`\r
\r
\`count\` и \`total\` автоматически создаются в начале функции с нулевыми значениями. Внутри функции мы их меняем как обычные переменные. Голый \`return\` в конце возвращает их текущие значения. Этот приём полезен для коротких функций — он делает код чище. Для длинных функций лучше возвращать значения явно, чтобы было понятнее что куда уходит.\r
\r
## 7. Указатели в параметрах\r
\r
По умолчанию Go копирует значения когда передаёт их в функцию. Это значит, что функция работает не с оригиналом, а с копией — и любые изменения внутри функции остаются внутри функции, на исходные переменные они не влияют:\r
\r
\`\`\`go\r
func swapValues(first, second int) {\r
    temp := first\r
    first = second\r
    second = temp\r
}\r
\r
func main() {\r
    val1, val2 := 10, 20\r
    swapValues(val1, val2)\r
    fmt.Println(val1, val2)  // обмена не произошло!\r
}\r
\`\`\`\r
\r
\`\`\`output\r
10 20\r
\`\`\`\r
\r
Функция «обменяла» свои локальные копии, но \`val1\` и \`val2\` в \`main\` не изменились. Если нам нужно, чтобы функция меняла оригинальные данные — нужно передавать не сами значения, а указатели на них. С указателями мы знакомились в отдельной главе — здесь их применение в функциях:\r
\r
\`\`\`go\r
func swapValues(first, second *int) {\r
    temp := *first\r
    *first = *second\r
    *second = temp\r
}\r
\r
func main() {\r
    val1, val2 := 10, 20\r
    swapValues(&val1, &val2)  // передаём адреса\r
    fmt.Println(val1, val2)   // обмен произошёл\r
}\r
\`\`\`\r
\r
\`\`\`output\r
20 10\r
\`\`\`\r
\r
Тип параметра теперь \`*int\` (указатель на int), а при вызове передаём \`&val1\` и \`&val2\` (адреса переменных). Внутри функции \`*first\` означает «значение по этому адресу». Когда мы пишем \`*first = *second\`, мы меняем именно те ячейки памяти, на которые ссылались \`val1\` и \`val2\` в \`main\` — поэтому изменения остаются и после возврата.\r
\r
> Когда использовать: указатели в параметрах нужны в двух случаях. Первый — когда функция должна изменить переданные данные. Второй — когда данные большие, и копировать их при каждом вызове дорого (например, большая структура с десятками полей). Для простых небольших значений передача по копии безопаснее и обычно предпочтительнее.\r
\r
## 8. defer\r
\r
У Go есть необычное ключевое слово — \`defer\`. Оно откладывает вызов функции до самого конца — до момента, когда текущая функция уже почти завершилась, прямо перед \`return\`. Нужно это для одной типичной задачи: освобождения ресурсов.\r
\r
Когда программа открывает файл, устанавливает соединение с сетью или базой данных — эти ресурсы потом нужно закрыть. Иначе они будут «висеть» и съедать память. Проблема в том, что между открытием и закрытием обычно много кода — и легко забыть про закрытие, особенно если функция выходит из нескольких разных мест. \`defer\` решает это:\r
\r
\`\`\`go\r
func processFile() {\r
    fmt.Println("Открываем файл")\r
    defer fmt.Println("Закрываем файл")  // выполнится в самом конце\r
\r
    fmt.Println("Работаем с файлом")\r
    fmt.Println("Делаем что-то ещё")\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Открываем файл\r
Работаем с файлом\r
Делаем что-то ещё\r
Закрываем файл\r
\`\`\`\r
\r
Хотя \`defer\` написан вторым, его сообщение появилось последним. Это и есть смысл \`defer\`: «запомни этот вызов, и сделай его прямо перед выходом». Так открытие и закрытие ресурса оказываются рядом в коде, и нет риска забыть про закрытие — оно сработает автоматически, как бы функция ни завершилась.\r
\r
Если в одной функции несколько \`defer\` — они выполняются в обратном порядке: последний \`defer\` срабатывает первым. Это логично: если первый ресурс был открыт первым, а второй вторым — закрывать их безопаснее в обратном порядке, потому что второй мог зависеть от первого.\r
\r
\`defer\` похож на правило «уходя — выключи свет». Когда вы заходите в комнату, вы не думаете «как бы не забыть выключить свет на выходе» — вы просто помните это правило. \`defer\` работает так же: он гарантирует что закрытие произойдёт, что бы дальше ни случилось.\r
`,N=`# 2.8. Структуры\r
\r
До сих пор мы хранили данные в простых типах — числах, строках, булевых значениях. Этого хватает для отдельных значений, но реальные данные обычно состоят из нескольких частей. Задача в списке дел — это название, статус выполнения, срок. Пользователь — это имя, email, возраст. Книга — это автор, название, год издания. Хранить всё это в отдельных переменных неудобно: они никак не связаны между собой. Нужен способ собрать связанные данные в одну сущность. В Go для этого существуют структуры.\r
\r
## 1. Что такое структура\r
\r
Структура — это собственный тип данных, который вы создаёте сами. Она объединяет несколько значений разных типов в одну единицу. Каждое такое значение внутри структуры называется полем. Например, для задачи в списке дел можно сделать структуру \`Task\` с тремя полями: название (строка), признак выполнения (bool) и срок (строка с датой).\r
\r
Структура — это как анкета. У анкеты есть набор полей: имя, фамилия, дата рождения, телефон. Каждое поле имеет своё назначение и свой тип данных — имя это текст, дата рождения это дата, телефон это набор цифр. Когда мы её заполняем, мы создаём один заполненный экземпляр анкеты — все поля относятся к одному человеку и хранятся вместе.\r
\r
Без структур пришлось бы вести три отдельных среза: один для названий задач, другой для статусов, третий для дат. Чтобы получить полную информацию о задаче №5, пришлось бы заглянуть во все три среза по индексу 5. Со структурами всё проще: один срез задач, и каждая задача — это самостоятельная сущность со всей нужной информацией внутри.\r
\r
## 2. Создание структуры\r
\r
Создание структуры состоит из двух шагов. Сначала мы описываем сам тип — говорим, какие у него будут поля и какого они типа. Потом создаём конкретные значения этого типа.\r
\r
### Шаг 1: описание типа\r
\r
Тип структуры определяется ключевыми словами \`type\` и \`struct\`:\r
\r
\`\`\`go\r
type Task struct {\r
    Title    string\r
    Done     bool\r
    Priority int\r
}\r
\`\`\`\r
\r
Разберём по частям. Слово \`type\` говорит «сейчас будет объявлен новый тип». После него идёт имя типа — \`Task\`. Слово \`struct\` означает «этот тип будет структурой». И в фигурных скобках перечислены поля: имя поля, потом его тип. У нас три поля — \`Title\` (заголовок, строка), \`Done\` (выполнено ли, bool), \`Priority\` (приоритет, число).\r
\r
> Про заглавные буквы: имена полей пишутся с заглавной буквы по соглашению Go. На самом деле это не просто стиль — имена с заглавной буквы делают поле «экспортируемым», доступным извне пакета. Имена со строчной буквы видны только внутри текущего пакета. Пока мы пишем всё в одном файле — это не критично, но привыкать к заглавным буквам в полях стоит сразу.\r
\r
### Шаг 2: создание значения\r
\r
Описание типа само по себе ничего не создаёт — это только чертёж, шаблон. Чтобы появилась настоящая задача, нужно создать значение этого типа. Самый понятный способ — указать значения для каждого поля по имени:\r
\r
\`\`\`go\r
task := Task{\r
    Title:    "Купить молоко",\r
    Done:     false,\r
    Priority: 1,\r
}\r
\r
fmt.Println(task)\r
\`\`\`\r
\r
\`\`\`output\r
{Купить молоко false 1}\r
\`\`\`\r
\r
Мы написали имя типа \`Task\`, открыли фигурные скобки и перечислили поля. Каждое поле — это пара «имя: значение», между ними двоеточие, в конце запятая. Запятая после последнего поля обязательна — без неё компилятор выдаст ошибку.\r
\r
> Если значение не указать: каждое поле, для которого мы не задали значение, получит нулевое значение для своего типа — пустую строку для \`string\`, \`0\` для \`int\`, \`false\` для \`bool\`. Можно вообще написать \`task := Task{}\` — получится задача со всеми полями в нулевом значении.\r
\r
## 3. Доступ к полям\r
\r
Когда у нас уже есть значение структуры, мы хотим читать и менять её поля. Делается это через точку — пишем имя переменной, точку, имя поля:\r
\r
\`\`\`go\r
task := Task{\r
    Title:    "Купить молоко",\r
    Done:     false,\r
    Priority: 1,\r
}\r
\r
// Читаем поля:\r
fmt.Println("Задача:", task.Title)\r
fmt.Println("Выполнено:", task.Done)\r
\r
// Меняем поле:\r
task.Done = true\r
fmt.Println("Теперь выполнено:", task.Done)\r
\`\`\`\r
\r
\`\`\`output\r
Задача: Купить молоко\r
Выполнено: false\r
Теперь выполнено: true\r
\`\`\`\r
\r
\`task.Title\` — это значение поля \`Title\` у нашей задачи. \`task.Done = true\` — присваивание нового значения этому полю. Поля структуры ведут себя как обычные переменные: их можно читать, присваивать им новые значения, использовать в выражениях, передавать в функции.\r
\r
Точка — это как «у». \`task.Title\` читается как «у задачи название», \`task.Done\` — «у задачи выполнено». Это подчёркивает, что поле принадлежит конкретному значению структуры. Если у нас две разные задачи \`task1\` и \`task2\`, то \`task1.Title\` и \`task2.Title\` — это разные значения, у каждой задачи свои поля.\r
\r
> Сравнение структур: структуры можно сравнивать через \`==\` и \`!=\` — две структуры равны, если у них совпадают все поля. Это работает только если все поля сравнимы между собой. Например, структуры со срезами внутри сравнить нельзя, потому что срезы сами по себе не сравниваются.\r
\r
## 4. Срез структур\r
\r
Одна задача в программе для списка дел — это редко интересно. Обычно задач много, и нужно их хранить вместе. Вспомним прошлую главу: для коллекций используются срезы. Нет ничего особенного в том, чтобы сделать срез из структур — это работает точно так же, как срез из строк или чисел:\r
\r
\`\`\`go\r
tasks := []Task{\r
    {Title: "Купить молоко", Done: false, Priority: 1},\r
    {Title: "Помыть посуду", Done: true, Priority: 2},\r
    {Title: "Сделать уроки", Done: false, Priority: 3},\r
}\r
\r
fmt.Println("Всего задач:", len(tasks))\r
\`\`\`\r
\r
\`\`\`output\r
Всего задач: 3\r
\`\`\`\r
\r
Тип \`[]Task\` означает «срез задач». Внутри фигурных скобок — задачи через запятую. Заметьте удобство: внутри среза можно не повторять \`Task\` перед каждой задачей — компилятор сам поймёт по типу среза. Просто пишем фигурные скобки и поля задачи.\r
\r
### Добавление новой задачи\r
\r
Чтобы добавить задачу, используем уже знакомый \`append\`:\r
\r
\`\`\`go\r
tasks = append(tasks, Task{\r
    Title:    "Позвонить маме",\r
    Done:     false,\r
    Priority: 1,\r
})\r
\r
fmt.Println("Стало задач:", len(tasks))\r
\`\`\`\r
\r
\`\`\`output\r
Стало задач: 4\r
\`\`\`\r
\r
### Перебор задач\r
\r
Срез структур перебирается через \`for ... range\` — точно так же как любой другой срез. На каждой итерации мы получаем индекс и саму задачу:\r
\r
\`\`\`go\r
for i, task := range tasks {\r
    status := "не выполнено"\r
    if task.Done {\r
        status = "выполнено"\r
    }\r
    fmt.Printf("%d. %s — %s\\n", i+1, task.Title, status)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
1. Купить молоко — не выполнено\r
2. Помыть посуду — выполнено\r
3. Сделать уроки — не выполнено\r
4. Позвонить маме — не выполнено\r
\`\`\`\r
\r
Внутри цикла \`task\` — это копия очередной задачи, с её полями \`task.Title\` и \`task.Done\` можно работать как обычно. Если нужны только задачи без индексов — заменяем \`i\` на \`_\` как обычно: \`for _, task := range tasks\`.\r
\r
## 5. Структуры и функции\r
\r
Структуры удобно передавать в функции — это делает код чище, потому что вместо пяти параметров (название, статус, приоритет и так далее) функция получает один параметр — задачу.\r
\r
\`\`\`go\r
func printTask(task Task) {\r
    fmt.Println("Задача:", task.Title)\r
    fmt.Println("Приоритет:", task.Priority)\r
}\r
\r
func main() {\r
    t := Task{Title: "Купить молоко", Priority: 1}\r
    printTask(t)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Задача: Купить молоко\r
Приоритет: 1\r
\`\`\`\r
\r
### Структура копируется при передаче\r
\r
Очень важная особенность: когда мы передаём структуру в функцию, Go создаёт её копию. Это значит, что любые изменения внутри функции остаются внутри функции — оригинал не меняется. Помните, мы это уже видели с обычными переменными в главе про функции? Со структурами всё то же самое:\r
\r
\`\`\`go\r
func completeTask(task Task) {\r
    task.Done = true   // меняем копию\r
    fmt.Println("Внутри функции:", task.Done)\r
}\r
\r
func main() {\r
    t := Task{Title: "Купить молоко"}\r
    completeTask(t)\r
    fmt.Println("Снаружи:", t.Done)  // оригинал не изменился!\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Внутри функции: true\r
Снаружи: false\r
\`\`\`\r
\r
### Возврат изменённой структуры\r
\r
Самый простой способ обойти это — функция получает копию, меняет её и возвращает обратно. А вызывающий код использует возвращённое значение:\r
\r
\`\`\`go\r
func completeTask(task Task) Task {\r
    task.Done = true\r
    return task\r
}\r
\r
func main() {\r
    t := Task{Title: "Купить молоко"}\r
    t = completeTask(t)              // присваиваем обратно\r
    fmt.Println("Снаружи:", t.Done)  // теперь true\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Снаружи: true\r
\`\`\`\r
\r
Этот подход чистый и предсказуемый — функция явно сообщает: «вот тебе обновлённая задача». Никаких скрытых изменений за спиной у вызывающего кода. Но если структура большая и менять её нужно часто — постоянно копировать может быть неудобно. Тогда на помощь приходят указатели.\r
\r
## 6. Указатели на структуры\r
\r
Если мы хотим, чтобы функция меняла оригинальную структуру, а не её копию, нужно передать указатель на структуру. Концепция та же что и в главе про указатели: передаём не сам объект, а его адрес в памяти.\r
\r
\`\`\`go\r
func completeTask(task *Task) {\r
    task.Done = true\r
}\r
\r
func main() {\r
    t := Task{Title: "Купить молоко"}\r
    completeTask(&t)                 // передаём адрес\r
    fmt.Println("Снаружи:", t.Done)  // true — оригинал изменился\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Снаружи: true\r
\`\`\`\r
\r
Что изменилось. В объявлении функции тип параметра теперь \`*Task\` — «указатель на Task». При вызове передаём \`&t\` — адрес переменной \`t\`. А внутри функции происходит интересное: мы пишем \`task.Done = true\`, будто работаем с обычной структурой, а не с указателем. Go автоматически разыменовывает указатели при доступе к полям через точку — так писать удобнее, чем \`(*task).Done\`.\r
\r
Передача структуры по значению — как сделать ксерокопию документа и отдать её. Что бы с копией ни делали — оригинал в шкафу не пострадает. Передача указателя — как дать адрес шкафа, где лежит оригинал. Теперь функция может вносить изменения прямо в оригинальный документ.\r
\r
> Когда что использовать: если функции нужно изменить структуру — передавайте указатель. Если структура большая и копировать её каждый раз дорого — тоже указатель. В остальных случаях передача по значению безопаснее: гарантия, что оригинал не изменится случайно.\r
`,F=`# 2.9. Методы и интерфейсы\r
\r
В прошлой главе мы научились создавать собственные типы данных через структуры. Структуры умеют хранить связанные данные вместе — это уже большой шаг. Но обычно у структур есть не только данные, но и поведение: товар может посчитать свою цену с налогом, пользователь может изменить пароль, задача может пометить себя выполненной. В этой главе разберём, как привязать поведение к типам — через методы. А в конце познакомимся с интерфейсами — способом описать «что объект умеет делать», не привязываясь к конкретному типу.\r
\r
## 1. Что такое метод\r
\r
Метод — это функция, которая привязана к определённому типу данных. Снаружи он выглядит почти как обычная функция, но вызывается не сам по себе, а через значение того типа, к которому привязан. Если у нас есть структура \`Product\`, то метод этой структуры — это функция, которую можно вызвать так: \`product.someMethod()\`.\r
\r
Представьте обычные предметы и их действия. У ножа есть действие «резать», у книги — «открыть», у машины — «завести». Действие неразрывно связано с предметом: нельзя «резать» само по себе, это всегда что-то режет. Метод устроен так же: это действие, привязанное к конкретному типу. \`product.printDetails()\` — это «у товара выполнить действие напечататься».\r
\r
Зачем это вообще нужно, если есть обычные функции? Главная причина — порядок в коде. Когда у нас есть тип \`Product\` и десять функций которые с ним работают — удобнее видеть их рядом с самим типом, как «способности» этого типа. Тогда любой человек, который смотрит на код, сразу понимает: вот товар, и вот всё что он умеет делать. Это просто лучше структурирует программу.\r
\r
## 2. Метод как функция, связанная с типом\r
\r
Чтобы понять, чем метод отличается от функции — давайте сравним их. Сначала напишем обычную функцию для работы со структурой:\r
\r
\`\`\`go\r
type Product struct {\r
    Name     string\r
    Category string\r
    Price    float64\r
}\r
\r
// Обычная функция:\r
func printDetails(product Product) {\r
    fmt.Println(product.Name, product.Category, product.Price)\r
}\r
\r
func main() {\r
    kayak := Product{"Каяк", "Водный спорт", 275}\r
    printDetails(kayak)  // передаём товар как аргумент\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Каяк Водный спорт 275\r
\`\`\`\r
\r
Здесь \`printDetails\` — обычная функция. Чтобы её вызвать, мы пишем имя функции и в скобках передаём товар. Товар выступает аргументом. Теперь перепишем то же самое как метод:\r
\r
\`\`\`go\r
type Product struct {\r
    Name     string\r
    Category string\r
    Price    float64\r
}\r
\r
// Метод типа Product:\r
func (product Product) printDetails() {\r
    fmt.Println(product.Name, product.Category, product.Price)\r
}\r
\r
func main() {\r
    kayak := Product{"Каяк", "Водный спорт", 275}\r
    kayak.printDetails()  // вызываем метод через точку\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Каяк Водный спорт 275\r
\`\`\`\r
\r
Результат тот же. Что изменилось. Во-первых, в объявлении функции появилась дополнительная пара скобок до имени: \`(product Product)\`. Это и есть привязка к типу — мы говорим, что эта функция теперь не сама по себе, а принадлежит типу \`Product\`. Во-вторых, исчез параметр в обычных скобках после имени \`printDetails()\`. И в-третьих, изменился способ вызова: вместо \`printDetails(kayak)\` мы пишем \`kayak.printDetails()\`. Товар теперь не аргумент, а тот, на ком вызывается метод.\r
\r
> Точка — знакомый знак: мы уже встречали такой синтаксис. \`fmt.Println\` — это вызов функции \`Println\`, привязанной к пакету \`fmt\`. \`kayak.Name\` — это обращение к полю \`Name\` у \`kayak\`. Теперь добавилось третье использование точки: \`kayak.printDetails\` — вызов метода \`printDetails\` у \`kayak\`.\r
\r
## 3. Получатель метода\r
\r
Та самая дополнительная пара скобок до имени метода — \`(product Product)\` — называется получателем метода. Получатель — это особый параметр, через который мы получаем доступ к самому объекту, у которого метод вызвали. Внутри метода мы используем имя получателя как обычную переменную:\r
\r
\`\`\`go\r
func (product Product) printDetails() {\r
    // product — это наш получатель\r
    // через него мы обращаемся к полям того товара,\r
    // у которого вызвали этот метод\r
    fmt.Println(product.Name, product.Category, product.Price)\r
}\r
\`\`\`\r
\r
Получатель состоит из двух частей в скобках: имя и тип. Имя — любое, какое нам удобно. Часто пишут одну букву (\`p Product\`) или сокращение, но осмысленные имена тоже встречаются. Тип — тот тип, которому мы добавляем метод.\r
\r
Получатель похож на «себя» в человеческой речи. Когда я говорю «я подниму руку» — слово «я» это получатель действия. Внутри метода \`product\` играет такую же роль: «через \`product\` я обращаюсь к самому себе, к тому объекту, у которого этот метод вызвали».\r
\r
> Имя получателя — одно для всех методов типа: если у одного метода \`Product\` получатель называется \`product\`, и у другого \`product\` — это нормально и даже желательно. А вот в одном получатель \`product\`, в другом \`p\`, в третьем \`prod\` — это плохой стиль. Договоритесь с собой и используйте одно имя для всех методов одного типа.\r
\r
## 4. Методы с параметрами и возвращаемыми значениями\r
\r
Методы могут принимать параметры и возвращать значения — точно так же, как обычные функции. Параметры пишутся в обычных скобках после имени метода, возвращаемый тип — после параметров. Получатель не считается параметром, он живёт в своих отдельных скобках.\r
\r
\`\`\`go\r
type Product struct {\r
    Name  string\r
    Price float64\r
}\r
\r
// Метод с параметрами и возвращаемым значением:\r
func (product Product) calcTax(rate, threshold float64) float64 {\r
    if product.Price > threshold {\r
        return product.Price + (product.Price * rate)\r
    }\r
    return product.Price\r
}\r
\r
func main() {\r
    kayak := Product{"Каяк", 275}\r
    finalPrice := kayak.calcTax(0.2, 100)\r
    fmt.Println("Цена с налогом:", finalPrice)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Цена с налогом: 330\r
\`\`\`\r
\r
Метод \`calcTax\` принимает два параметра — ставку налога и порог цены, после которого налог применяется. И возвращает \`float64\` — итоговую цену. Когда мы вызываем \`kayak.calcTax(0.2, 100)\`, внутри метода: \`product\` — это сам \`kayak\`, \`rate\` равно \`0.2\`, \`threshold\` равно \`100\`. Метод проверяет цену, при необходимости добавляет налог и возвращает результат.\r
\r
Заметьте, как естественно это читается: «у \`kayak\` вычислить налог по ставке \`0.2\` с порогом \`100\`». Это и есть главное преимущество методов перед обычными функциями — они читаются ближе к человеческому языку.\r
\r
## 5. Получатель по значению\r
\r
Помните из главы про функции: когда мы передаём структуру в функцию по значению, Go создаёт её копию. Функция работает с копией, и любые изменения остаются внутри неё. С методами действует то же правило — если получатель объявлен по значению, метод работает с копией.\r
\r
\`\`\`go\r
type Product struct {\r
    Name  string\r
    Price float64\r
}\r
\r
func (product Product) discount() {\r
    product.Price = product.Price * 0.9  // меняем копию\r
    fmt.Println("Внутри метода:", product.Price)\r
}\r
\r
func main() {\r
    kayak := Product{"Каяк", 275}\r
    kayak.discount()\r
    fmt.Println("Снаружи:", kayak.Price)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Внутри метода: 247.5\r
Снаружи: 275\r
\`\`\`\r
\r
Хотя внутри метода \`discount\` цена изменилась, после возврата из метода оригинальный товар остался прежним. Метод работал с копией. Это безопасно — невозможно случайно сломать данные через метод. Но если задача именно в том чтобы изменить оригинал — нужен другой тип получателя.\r
\r
## 6. Получатель по указателю\r
\r
Если в скобках получателя написать звёздочку перед типом — получатель становится указателем. Метод теперь работает не с копией, а с самим объектом, и любые изменения остаются после возврата:\r
\r
\`\`\`go\r
type Product struct {\r
    Name  string\r
    Price float64\r
}\r
\r
// Получатель — указатель: *Product вместо Product\r
func (product *Product) discount() {\r
    product.Price = product.Price * 0.9\r
}\r
\r
func main() {\r
    kayak := Product{"Каяк", 275}\r
    kayak.discount()\r
    fmt.Println("Снаружи:", kayak.Price)\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Снаружи: 247.5\r
\`\`\`\r
\r
Что изменилось. Получатель теперь \`(product *Product)\` — звёздочка означает «указатель». Внутри метода всё по-прежнему: пишем \`product.Price\`, обращаемся к полям через точку. Go сам разбирается, что за точкой стоит указатель, и автоматически разыменовывает его — нам не нужно писать \`(*product).Price\`. Это удобство, заточенное специально под методы.\r
\r
> И при вызове ничего не меняется: мы по-прежнему пишем \`kayak.discount()\`, хотя метод требует указатель. Go автоматически берёт адрес у переменной перед вызовом — нам не нужно писать \`(&kayak).discount()\`. Это тоже специальное удобство для методов.\r
\r
Получатель по значению — как ксерокопия документа. Метод что-то делает с копией: можно посмотреть, можно даже зачеркнуть в копии — оригинал не пострадает. Получатель по указателю — как доступ к оригиналу: метод реально меняет тот документ, на котором был вызван.\r
\r
## 7. Когда использовать значение, а когда указатель\r
\r
Тут возникает разумный вопрос: какой получатель выбирать? Простое правило: если метод должен изменить объект, на котором его вызвали — нужен получатель по указателю. Если метод просто читает данные и ничего не меняет — подходит получатель по значению.\r
\r
\`\`\`go\r
// Метод только читает — получатель по значению:\r
func (product Product) printName() {\r
    fmt.Println(product.Name)\r
}\r
\r
// Метод изменяет — получатель по указателю:\r
func (product *Product) applyDiscount(percent float64) {\r
    product.Price = product.Price * (1 - percent/100)\r
}\r
\`\`\`\r
\r
Есть и второй критерий — размер структуры. Если структура большая (много полей или вложенные данные), каждое копирование при передаче по значению занимает время и память. Поэтому для больших структур принято использовать указатели даже в методах, которые только читают данные. Но для маленьких структур разница незаметна, и можно следовать первому правилу.\r
\r
> Важное правило стиля: если у одного типа несколько методов, и хотя бы один из них использует получатель по указателю — обычно делают указатель и для всех остальных. Это для единообразия: смешивать получатели по значению и по указателю для одного типа считается плохим стилем. Кроме того, такое смешивание создаёт проблемы при работе с интерфейсами, о которых пойдёт речь дальше.\r
\r
## 8. Что такое интерфейс\r
\r
Представьте себе ситуацию. У нас есть две разные структуры: \`Product\` (товар) и \`Service\` (услуга). У них разные поля, разное устройство. Но у обоих есть общая черта: и товар, и услуга стоят денег. У обоих можно спросить «сколько ты стоишь» и получить число в ответ. Хотелось бы написать функцию \`calcTotal\` которая считает общую сумму расходов — и которой не важно, что именно в списке: товары, услуги или и то и другое. Главное чтобы у каждого элемента был метод «сколько стоишь».\r
\r
Эту задачу решают интерфейсы. Интерфейс — это описание набора методов, которые должен иметь тип. Интерфейс не описывает поля или реализацию — только сигнатуры методов: их имена, параметры и возвращаемые значения. Любой тип, у которого есть все указанные методы, автоматически считается реализующим этот интерфейс.\r
\r
Интерфейс — как требование к вакансии. Объявление гласит: «Требуется человек, умеющий водить машину и говорить по-английски». Не важно, какого он роста, цвета волос, какой у него характер — если он умеет эти две вещи, он подходит. В Go так же: интерфейс описывает «что объект должен уметь», а уж как именно он это делает — его внутреннее дело.\r
\r
## 9. Реализация интерфейса\r
\r
Интерфейс объявляется ключевыми словами \`type\` и \`interface\`. Внутри фигурных скобок перечисляются методы — только их сигнатуры, без тела:\r
\r
\`\`\`go\r
type Expense interface {\r
    GetName() string\r
    GetCost() float64\r
}\r
\`\`\`\r
\r
Тип \`Expense\` (расход) описывает: «у любого расхода должны быть два метода: \`GetName\`, который возвращает строку, и \`GetCost\`, который возвращает число». Никакого тела у методов нет — это пока только описание требований.\r
\r
Теперь создадим два типа структур, у которых будут эти методы:\r
\r
\`\`\`go\r
type Product struct {\r
    Name  string\r
    Price float64\r
}\r
\r
func (p Product) GetName() string {\r
    return p.Name\r
}\r
\r
func (p Product) GetCost() float64 {\r
    return p.Price\r
}\r
\r
type Service struct {\r
    Description    string\r
    MonthlyFee     float64\r
    DurationMonths int\r
}\r
\r
func (s Service) GetName() string {\r
    return s.Description\r
}\r
\r
func (s Service) GetCost() float64 {\r
    return s.MonthlyFee * float64(s.DurationMonths)\r
}\r
\`\`\`\r
\r
У \`Product\` оба метода реализованы: \`GetName\` возвращает имя, \`GetCost\` — цену. У \`Service\` тоже оба метода: \`GetName\` возвращает описание, \`GetCost\` — общую стоимость услуги за весь срок. Сами реализации совершенно разные, но сигнатуры (имена, параметры, возвращаемые значения) совпадают с тем, что требует интерфейс \`Expense\`.\r
\r
> Главная особенность Go: мы нигде не написали «\`Product\` реализует \`Expense\`» или «\`Service\` реализует \`Expense\`». В большинстве других языков это нужно объявлять явно — например, в Java пишется \`implements Expense\`. В Go это происходит автоматически: если у типа есть все методы, требуемые интерфейсом — он этот интерфейс реализует. Достаточно просто определить методы.\r
\r
## 10. Использование интерфейса\r
\r
Раз у \`Product\` и \`Service\` есть нужные методы — они оба реализуют интерфейс \`Expense\`. Это значит, что мы можем хранить товары и услуги в одном срезе типа \`Expense\`, и работать с ними через общие методы:\r
\r
\`\`\`go\r
expenses := []Expense{\r
    Product{Name: "Каяк", Price: 275},\r
    Service{Description: "Страховка", MonthlyFee: 89.50, DurationMonths: 12},\r
    Product{Name: "Спасжилет", Price: 48.95},\r
}\r
\r
for _, expense := range expenses {\r
    fmt.Println(expense.GetName(), "—", expense.GetCost())\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Каяк — 275\r
Страховка — 1074\r
Спасжилет — 48.95\r
\`\`\`\r
\r
Срез \`[]Expense\` может хранить любые значения, реализующие интерфейс \`Expense\` — и \`Product\`, и \`Service\`, и любые будущие типы которые мы добавим. В цикле мы вызываем \`GetName\` и \`GetCost\` — это методы интерфейса. Какая именно реализация сработает (из \`Product\` или из \`Service\`) — Go разбирается автоматически в зависимости от того, что там лежит.\r
\r
### Интерфейс как параметр функции\r
\r
Интерфейс можно использовать как тип параметра функции. Тогда функция сможет принимать значения любого типа, реализующего этот интерфейс:\r
\r
\`\`\`go\r
func calcTotal(expenses []Expense) float64 {\r
    var total float64\r
    for _, e := range expenses {\r
        total += e.GetCost()\r
    }\r
    return total\r
}\r
\r
func main() {\r
    expenses := []Expense{\r
        Product{Name: "Каяк", Price: 275},\r
        Service{Description: "Страховка", MonthlyFee: 89.50, DurationMonths: 12},\r
    }\r
    fmt.Println("Итого:", calcTotal(expenses))\r
}\r
\`\`\`\r
\r
\`\`\`output\r
Итого: 1349\r
\`\`\`\r
\r
Функция \`calcTotal\` не знает и не должна знать, что именно лежит в срезе — товары, услуги, что-то ещё. Она знает только одно: у каждого элемента есть метод \`GetCost\`, который вернёт число. Этого достаточно. Если завтра мы добавим новый тип \`Subscription\` и реализуем у него \`GetName\` и \`GetCost\` — \`calcTotal\` будет с ним работать без изменений. Это и есть главная сила интерфейсов: они позволяют писать код, который работает с разными типами через общее поведение.\r
`,D=Object.assign({"../content/intro.md":x,"../content/lesson-1.1.md":G,"../content/lesson-1.2.md":h,"../content/lesson-2.1.md":v,"../content/lesson-2.2.md":b,"../content/lesson-2.3.md":y,"../content/lesson-2.4.md":T,"../content/lesson-2.5.md":_,"../content/lesson-2.6.md":C,"../content/lesson-2.7.md":S,"../content/lesson-2.8.md":N,"../content/lesson-2.9.md":F});function c(t){return typeof t=="string"?t:Array.isArray(t)?t.map(c).join(""):t?.props?.children?c(t.props.children):""}function w({children:t,title:e="Код"}){const[l,o]=p.useState(!1),i=c(t).replace(/\n$/,""),r=()=>{navigator.clipboard.writeText(i),o(!0),setTimeout(()=>{o(!1)},1500)},a=e==="Вывод";return n.jsxs("div",{className:"my-5 overflow-hidden rounded-lg border border-base-300 bg-base-100",children:[n.jsxs("div",{className:"flex items-center justify-between bg-base-200 px-4 py-2 text-sm",children:[n.jsx("span",{className:"text-base-content/60",children:e}),!a&&n.jsx("button",{type:"button",onClick:r,className:"text-xs text-base-content/70 hover:text-base-content",children:l?"Скопировано":"Скопировать код"})]}),n.jsx("pre",{className:"m-0 overflow-x-auto bg-base-100 p-4 text-sm",children:t})]})}function B({children:t}){return n.jsx("div",{className:"my-5 border-l-4 border-[#5B7FFF] bg-[#5B7FFF]/5 p-4",children:n.jsx("div",{className:"text-sm text-slate-700 [&>p]:m-0",children:t})})}const V=f(function(){const{slug:e}=m(),l=`../content/${e}.md`,o=D[l],i=e?k(e,"lesson"):null;return o?n.jsxs("div",{children:[n.jsx("div",{className:"navbar bg-base-100 border-b border-base-300",children:n.jsx("div",{className:"w-full max-w-4xl mx-auto px-4",children:n.jsx(u,{to:"/",className:"text-sm text-base-content/70 hover:text-base-content",children:"← Назад"})})}),n.jsxs("main",{className:"max-w-4xl mx-auto px-4 py-6",children:[n.jsx("article",{className:"text-base leading-7",children:n.jsx(d,{remarkPlugins:[P],rehypePlugins:[g],components:{h1({children:r}){return n.jsx("h1",{className:"mb-6 text-3xl font-bold leading-tight",children:r})},h2({children:r}){return n.jsx("h2",{className:"mt-10 mb-4 text-2xl font-semibold",children:r})},h3({children:r}){return n.jsx("h3",{className:"mt-7 mb-3 text-xl font-semibold",children:r})},p({children:r}){return n.jsx("p",{className:"my-4 text-base-content/85",children:r})},ul({children:r}){return n.jsx("ul",{className:"my-4 list-disc pl-6 space-y-1",children:r})},li({children:r}){return n.jsx("li",{className:"text-base-content/85",children:r})},hr(){return n.jsx("hr",{className:"my-8 border-base-300"})},pre({children:r}){const s=(r?.props?.className||"").includes("language-output");return n.jsx(w,{title:s?"Вывод":"Код",children:r})},code({className:r,children:a,...s}){return r?.includes("language-")?n.jsx("code",{className:r,...s,children:a}):n.jsx("code",{className:"rounded bg-base-200 px-1.5 py-0.5 text-sm",...s,children:a})},blockquote({children:r}){return n.jsx(B,{children:r})},table({children:r}){return n.jsx("div",{className:"my-6 overflow-x-auto",children:n.jsx("table",{className:"table table-zebra border border-base-300",children:r})})}},children:o})}),i&&n.jsx("div",{className:"mt-10 flex justify-end",children:n.jsx(u,{to:i.path,className:"btn border-[#5B7FFF] bg-[#5B7FFF]/10 text-[#3556D8] hover:border-[#5B7FFF] hover:bg-[#5B7FFF]/15",children:"Далее"})})]})]}):n.jsx("div",{className:"p-6",children:"Урок не найден"})});export{V as default};
